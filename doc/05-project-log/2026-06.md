# 2026-06 项目日志

## 2026-06-01 — V2-090B Closeout all-command coverage

- 工作包：V2-090B（Closeout all-command coverage，收尾全命令覆盖）
- 关键产出：`src/boardroom_os/closeout/gate.py`、`tests/negative/test_run_manifest_command_coverage.py`；同步更新 `tests/closeout/test_closeout_gate.py`、`tests/closeout/test_git_audit_hardening.py`、`tests/closeout/test_git_version_audit.py`、`tests/closeout/test_process_audit.py`、`tests/closeout/test_process_audit_artifacts.py`、`tests/closeout/test_process_audit_fact_chain.py`、`tests/negative/test_process_audit_construction_loop_rejected.py`、`tests/negative/test_v2_070_audit_report_p2_regressions.py`、`tests/proving/fixtures/tiny_closeout.py`、`tests/proving/test_tiny_closeout.py`。
- 关键实现：`CloseoutGate`（收尾门禁）按 `command_id` 要求 `final_command_bindings`（最终命令证据绑定）覆盖 `RunManifest.commands`（运行清单命令集合）；缺任一 declared run/test command（声明运行/测试命令）最终证据时 fail closed（失败关闭），blocker code（阻断代码）为 `COMMAND_EVIDENCE_NOT_FINAL`，message（消息）包含 `RUN_MANIFEST_COMMAND_UNVERIFIED`，`related_ref` 指向缺失 command id。保留既有校验：`VerificationRun`（验证运行）必须 passed，binding ref（绑定引用）必须匹配 `RunManifest`（运行清单）/ `PackageContract`（包合同），命令、cwd（工作目录）和 kind（类型）必须一致。
- Negative tests：新增 `tests/negative/test_run_manifest_command_coverage.py`，证明 `run-app` + `test-app` 只提供 `test-app` evidence（证据）时被阻断；当前 V2-080 failure package（失败包）缺 `run-backend` / `run-frontend` evidence 时作为 regression negative（回归负例）被阻断；blocker（阻断项）可审计且包含缺失 command id。
- Happy path：`tests/closeout/test_closeout_gate.py` 的 ready fixture（就绪夹具）为 `run-app` 与 `test-app` 分别构造真实形状的 `VerificationRun`（验证运行）和 `CloseoutCommandEvidenceBinding`（收尾命令证据绑定），只有所有 declared commands（声明命令）都有最终证据时才通过。
- Tiny closeout 调整：`tests/proving/fixtures/tiny_closeout.py` 不再为缺 run command evidence 的 V2-080 样例构造 passed `CloseoutPackage`（通过收尾包）；`tests/proving/test_tiny_closeout.py` 改为断言 V2-080 failure package 被 `CloseoutGate` 阻断。不重建、不删除 `examples/generated-workspaces/tiny-fullstack/`，该目录继续作为 regression negative material（回归负例素材）。
- 边界说明：V2-090B 只完成 CloseoutGate gate enforcement（收尾门禁强制规则），不补齐当前 V2-080 failure package（失败包）的 `run-backend` / `run-frontend` final evidence（最终证据），也不重建 golden sample（黄金样例）。service startup/readiness（服务启动/就绪）与 live blackbox evidence（真实黑盒证据）留给 V2-090C~F。
- 验证：`$env:PYTHONPATH='src;.'; python -m pytest tests/negative/test_run_manifest_command_coverage.py tests/closeout/test_closeout_gate.py -q --tb=short --basetemp .pytest-tmp-v2090b-closeout-final2` 通过（6 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_closeout.py -q --tb=short --basetemp .pytest-tmp-v2090b-tiny-closeout-final2` 通过（32 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_run_manifest.py tests/proving/test_workspace_evidence_export.py tests/execution/test_command_runner.py tests/evidence/test_final_evidence_table.py -q --tb=short --basetemp .pytest-tmp-v2090b-regression-final2` 通过（95 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/closeout -q --tb=short --basetemp .pytest-tmp-v2090b-closeout-all-final2` 通过（226 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/negative -q --tb=short --basetemp .pytest-tmp-v2090b-negative-all-final2` 通过（435 passed）。Full suite（完整套件）在断网环境下尝试两次，均因 `tests/proving/test_tiny_package_assembly.py` 默认 provider/package assembly（模型供应商/包组装）路径长时间运行而超时，未作为通过证据；`git diff --check` 通过。

## 2026-06-01 — V2-090C ServiceRunEvidence

- 工作包：V2-090C（ServiceRunEvidence，服务运行证据）
- 关键产出：新增 `src/boardroom_os/evidence/service_run.py`、`tests/negative/test_service_run_evidence_fail_closed.py`、`tests/negative/test_service_run_closeout_gate.py`、`tests/evidence/test_service_run_evidence.py`、`tests/execution/test_service_runner.py`；同步更新 `src/boardroom_os/adapters/process_runner.py`、`src/boardroom_os/evidence/claim.py`、`src/boardroom_os/evidence/verifier.py`、`src/boardroom_os/closeout/gate.py`、`src/boardroom_os/workspace/evidence_export.py`、`tests/closeout/test_closeout_gate.py`、`tests/proving/test_workspace_evidence_export.py`。
- 关键实现：`ServiceRunEvidence`（服务运行证据）记录 declared `PackageCommand`（包命令）启动后的 process id、readiness URL（就绪 URL）、HTTP probe status/body sha256（HTTP 探针状态/内容哈希）、stdout/stderr refs（标准输出/错误引用）和时区时间；`ServiceRunner`（服务运行器）真实启动子进程并等待 HTTP readiness probe（HTTP 就绪探针）通过后生成服务证据，进程提前退出、错误端口/path、非 UTF-8 输出和时钟回退均 fail closed（失败关闭）。
- Evidence / closeout 接入：`EvidenceClaimSourceKind.SERVICE_RUN`（服务运行来源）和 `build_evidence_claim_from_service_run`（服务运行证据声明构建器）接入 `EvidenceVerifier`（证据验证器）；`required_artifact_type="service_run"` 必须解析到真实 `ServiceRunEvidence`（服务运行证据），且 artifact refs（制品引用）必须等于 canonical service stdout/stderr refs（规范服务标准输出/错误引用）。`CloseoutGate`（收尾门禁）要求 `RUN`（运行命令）绑定 `ServiceRunEvidence`，`TEST`（测试命令）继续绑定 `VerificationRun`（验证运行）；缺 service readiness（服务就绪）时返回 `COMMAND_EVIDENCE_NOT_FINAL`，message（消息）包含 `RUN_MANIFEST_SERVICE_NOT_READY`，`related_ref` 指向 command id（命令编号）。
- Workspace export：`WorkspaceEvidenceBundle`（工作区证据包）新增 `service_run_refs`（服务运行引用）和 artifact kind（制品类型）`SERVICE_RUNS`，固定导出路径 `20-evidence/tests/service-runs.json`，并拒绝 orphan service run（孤儿服务运行证据）。
- Negative tests：证明只有 process id 无 readiness probe、服务启动后立即退出、probe 非 2xx、缺时区时间、`required_artifact_type="service_run"` 却来自 `VerificationRun`（验证运行）、`RUN` command 仅有 `VerificationRun` 或缺 service readiness 时均不可通过。
- Happy path：标准库 HTTP server fixture（HTTP 服务夹具）返回 `/health` 200 后，`ServiceRunner`（服务运行器）记录 readiness URL、probe body sha256、stdout/stderr refs 与时间；`build_evidence_claim_from_service_run`（服务运行声明构建器）产出的 claim（证据声明）可被 `EvidenceVerifier` 验证为 `service_run`；workspace bundle（工作区证据包）保留 `service_run_refs` 并包含 `service-runs.json`。
- 边界说明：V2-090C 只实现 service run evidence infrastructure（服务运行证据基础设施）和 closeout/evidence/export gate（收尾/证据/导出门禁）；不修 tiny-fullstack contract（微型全栈合同），不做 CRUD/SQLite/live frontend-backend 黑盒业务验证，这些留给 V2-090D/E。
- 验证：`$env:PYTHONPATH='src;.'; python -m pytest tests/negative/test_service_run_evidence_fail_closed.py tests/negative/test_service_run_closeout_gate.py -q --tb=short --basetemp .pytest-tmp-v2090c-negative-final2` 通过（11 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/evidence/test_service_run_evidence.py tests/execution/test_service_runner.py -q --tb=short --basetemp .pytest-tmp-v2090c-service-final2` 通过（2 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/closeout/test_closeout_gate.py tests/proving/test_workspace_evidence_export.py -q --tb=short --basetemp .pytest-tmp-v2090c-closeout-workspace-final2` 通过（21 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/execution/test_command_runner.py tests/evidence/test_evidence_claim.py tests/evidence/test_evidence_verifier.py tests/negative/test_synthetic_evidence_rejected.py -q --tb=short --basetemp .pytest-tmp-v2090c-regression-final2` 通过（148 passed）；补充回归 `$env:PYTHONPATH='src;.'; python -m pytest tests/negative/test_run_manifest_command_coverage.py tests/negative/test_closeout_fail_closed.py tests/negative/test_service_run_evidence_fail_closed.py tests/negative/test_service_run_closeout_gate.py -q --tb=short --basetemp .pytest-tmp-v2090c-review-negative-all` 通过（38 passed）；`git diff --check` 通过。

## 2026-06-02 — V2-090D Tiny contract recovery

- 工作包：V2-090D（Tiny contract recovery，微型合同整改）
- 关键产出：`tests/fixtures/contracts/tiny_fullstack_contract.py`、`tests/proving/fixtures/tiny_provider_attempts.py`、`tests/proving/fixtures/tiny_package_assembly.py`、`tests/negative/test_tiny_contract_recovery_fail_closed.py`；同步更新 `tests/proving/test_tiny_contracts.py`、`tests/proving/test_tiny_provider_attempts.py`、`tests/proving/test_tiny_package_assembly.py`、`src/boardroom_os/closeout/gate.py`、`tests/negative/test_closeout_fail_closed.py`、`tests/proving/fixtures/tiny_closeout.py`、`tests/proving/test_tiny_closeout.py`。
- 关键实现：tiny-fullstack `AcceptanceContract`（验收合同）拆分出 backend startup（后端启动）、HTTP CRUD、SQLite persistence via HTTP（经 HTTP 的 SQLite 持久化）、frontend startup（前端启动）、live frontend-backend integration（真实前后端集成）与 all command final evidence（全部命令最终证据）blocking refs（阻断验收引用）；`PackageContract`（包合同）将 `run-backend` 固定为 `python -m backend.app`，source surfaces（源码实现面）与 integration boundaries（集成边界）明确 standard-library HTTP service（标准库 HTTP 服务）、frontend static service（前端静态服务）和 SQLite via HTTP workflow（经 HTTP 的 SQLite 工作流）。
- Negative tests：新增 `tests/negative/test_tiny_contract_recovery_fail_closed.py`，证明 `uvicorn backend.app:app` 与 standard-library-only prompt（仅标准库提示词）冲突必须失败；只声明 “fetch backend API” 但缺 live HTTP integration evidence（真实 HTTP 集成证据）必须失败；只声明 run/test commands（运行/测试命令）但缺 final command evidence（最终命令证据）必须失败；provider prompt（模型提示词）缺 `http.server` / `BaseHTTPRequestHandler` / SQLite via HTTP / fakeFetch-only 禁止语义必须失败。
- Happy path：`tests/proving/test_tiny_contracts.py` 与 `tests/proving/test_tiny_provider_attempts.py` 证明合同和提示词均走 `python -m backend.app` 标准库 HTTP route（路线），并要求 `/health`、`/books`、checkout/return/delete endpoints（端点）、SQLite persistence via HTTP 与 live integration evidence。`tests/proving/fixtures/tiny_package_assembly.py` 仅保留 V2-090D 可证明的结构/证据义务，不构造完整 `WorkspaceEvidenceBundle`（工作区证据包）或 passed `CloseoutPackage`（通过收尾包）。
- 边界说明：V2-090D 不重建 `examples/generated-workspaces/tiny-fullstack/`，不实现 live blackbox runner（真实黑盒运行器）。缺 `WorkspaceEvidenceBundle` 时 `CloseoutGate`（收尾门禁）返回 `WORKSPACE_EVIDENCE_BUNDLE_NOT_READY`，避免把 incomplete final evidence table（未完成最终证据表）伪装成 closeout-ready（可收尾）。
- 验证：`$env:PYTHONPATH='src;.'; python -m pytest tests/negative/test_tiny_contract_recovery_fail_closed.py tests/negative/test_tiny_fullstack_contract_fixture_fail_closed.py -q --tb=short --basetemp .pytest-tmp-v2090d-negative-final` 通过（11 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_contracts.py tests/proving/test_tiny_provider_attempts.py -q --tb=short -k "not real_provider_records_attempts_for_every_tiny_implementation_ticket" --basetemp .pytest-tmp-v2090d-provider-final-no-real` 通过（28 passed, 1 deselected）；`$env:PYTHONPATH='src;.'; python -m pytest tests/contracts tests/proving/test_tiny_contracts.py tests/negative/test_tiny_contract_recovery_fail_closed.py -q --tb=short --basetemp .pytest-tmp-v2090d-contracts-final2` 通过（90 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/negative/test_closeout_fail_closed.py tests/closeout/test_closeout_gate.py tests/proving/test_tiny_closeout.py tests/proving/test_workspace_evidence_export.py tests/evidence/test_final_evidence_table.py tests/evidence/test_evidence_verifier.py -q --tb=short --basetemp .pytest-tmp-v2090d-regression-final` 通过（111 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_missing_run_manifest_artifact tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_missing_delete_book_api tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_non_sqlite_persistence tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_missing_sqlite_test_import tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_accepts_sqlite_cleanup_evidence_without_schema_query tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_regex_only_frontend_integration_test tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_frontend_integration_that_reports_delete_only_calls tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_frontend_integration_that_excludes_pytest_tmp_root tests/proving/test_tiny_package_assembly.py::test_tiny_package_assembly_rejects_delete_test_that_refetches_deleted_book -q --tb=short --basetemp .pytest-tmp-v2090d-package-final` 通过（9 passed）；真实 provider（模型供应商）单用例 `$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_provider_attempts.py::test_real_provider_records_attempts_for_every_tiny_implementation_ticket -q --tb=short --basetemp .pytest-tmp-v2090d-real-provider-rerun3` 通过（1 passed in 710.37s）；`git diff --check` 通过。
- 未作为通过证据：早先包含真实 provider（模型供应商）调用的完整 provider/proving 集合在 184 秒外层等待超时；原因是 V2-090D 源码交付设置会把 provider timeout（模型供应商超时）提升到至少 240 秒、`max_retries` 提升到至少 2，真实 provider 单用例完整跑完耗时 710.37 秒。默认真实 provider package assembly（模型供应商包组装）路径仍未在本轮完整重跑。超时 pytest 残留进程已清理，未清理会话开始前已存在的 `python -m http.server 5173 --directory frontend`。

## 2026-06-02 — V2-090E Live blackbox integration

- 工作包：V2-090E（Live blackbox integration，真实黑盒集成）
- 关键产出：新增 `src/boardroom_os/evidence/live_blackbox.py`、`tests/evidence/test_live_blackbox_evidence.py`、`tests/negative/test_live_blackbox_integration_fail_closed.py`、`tests/execution/test_service_runner_environment.py`、`tests/proving/test_tiny_live_blackbox_integration.py`；同步更新 `src/boardroom_os/adapters/process_runner.py`、`src/boardroom_os/evidence/service_run.py`、`src/boardroom_os/evidence/claim.py`、`src/boardroom_os/evidence/verifier.py`、`src/boardroom_os/workspace/evidence_export.py`、`tests/proving/fixtures/tiny_package_assembly.py`、`tests/proving/test_workspace_evidence_export.py`。
- 关键实现：`LiveBlackboxIntegrationEvidence`（真实黑盒集成证据）记录 backend CRUD probe（后端增删改查探针）、SQLite HTTP persistence probe（经 HTTP 的 SQLite 持久化探针）和 frontend live probe（前端真实探针）；`LiveBlackboxIntegrationVerifier`（真实黑盒集成验证器）显式消费 `PackageContract`（包合同）、backend/frontend `ServiceRunEvidence`（服务运行证据）和 probe results（探针结果），校验 service run refs（服务运行引用）、command id（命令编号）、declared command（声明命令）、readiness（就绪）、backend CRUD URL（后端增删改查地址）、probe URL（探针地址）、frontend request method/path trace（前端请求方法/路径轨迹）和 `BOOKS_DB_PATH`（数据库路径环境变量）均绑定到同一组运行中服务。`EvidenceVerifier`（证据验证器）新增 `LIVE_BLACKBOX`（真实黑盒来源），并以 `EvidenceObligation.required_verifier="live_blackbox"`（证据义务要求真实黑盒验证器）为权威源，要求该类义务只能由 live blackbox evidence（真实黑盒证据）满足。
- Service runner：`ServiceRunnerInput`（服务运行器输入）新增 `environment_overrides`（环境变量覆盖），用于 backend/frontend dynamic ports（后端/前端动态端口）和临时 SQLite path（路径）；`ServiceRunner.run`（服务运行器运行）新增 `after_ready_probe`（就绪后探针），确保 backend/frontend service（后端/前端服务）仍运行时执行 HTTP/SQLite/frontend probes（探针）。最终复核发现固定 frontend readiness URL（前端就绪地址）可能误命中旧服务后，补充 fail-closed（失败关闭）门禁：readiness URL 在启动前已 2xx 时拒绝生成 `ServiceRunEvidence`（服务运行证据），ready 后也复查子进程仍存活；tiny frontend run command（微型前端运行命令）改为可由 `FRONTEND_PORT`（前端端口）环境变量覆盖。
- Workspace export：`WorkspaceEvidenceBundle`（工作区证据包）新增 `live_blackbox_evidence_refs`（真实黑盒证据引用）和 `20-evidence/tests/live-blackbox.json` artifact（制品），并拒绝 live blackbox evidence（真实黑盒证据）绑定的 backend/frontend service run refs（服务运行引用）无法解析。
- Negative tests：证明 fakeFetch-only（仅模拟 fetch）不能满足 live frontend/backend integration（真实前后端集成）；缺 delete / checkout / return 任一 HTTP operation（HTTP 操作）失败；SQLite 只由函数单测落盘失败；`required_verifier="live_blackbox"`（要求真实黑盒验证器）的 evidence obligation（证据义务）不能由普通 `VerificationRun`（验证运行）冒充；frontend service run command mismatch（前端服务运行命令不匹配）、backend CRUD URL / frontend/backend probe URL（后端增删改查地址 / 前后端探针地址）不匹配、SQLite path（SQLite 路径）不匹配、frontend live probe（前端真实探针）缺 DELETE request trace（删除请求轨迹）和 pre-existing readiness service（预先存在的就绪服务）均失败。
- Happy path：`build_tiny_live_blackbox_fixture`（微型真实黑盒夹具）通过 `PORT` / `FRONTEND_PORT` / `BOOKS_DB_PATH`（后端端口 / 前端端口 / 数据库路径）真实启动 backend/frontend，完成 `/health` readiness（就绪）、create/list/checkout/return/delete HTTP workflow（HTTP 工作流）、SQLite schema/data/state transition（模式/数据/状态转换）和 frontend-to-backend live probe（真实前后端探针），并让 `FinalEvidenceTable`（最终证据表）满足 V2-090E 覆盖的 live integration rows（真实集成行）。
- 边界说明：V2-090E 不重建 `examples/generated-workspaces/tiny-fullstack/`，不生成 passed `CloseoutPackage`（通过收尾包）；golden sample rebuild（黄金样例重建）留给 V2-090F。测试使用 `allow_test_provider_transport`（允许测试模型传输）只用于构造 provider attempt（模型调用尝试）形状，不作为真实 HTTP/SQLite/frontend 行为证据。
- 验证：`$env:PYTHONPATH='src;.'; python -m pytest tests/evidence/test_live_blackbox_evidence.py tests/negative/test_live_blackbox_integration_fail_closed.py tests/proving/test_tiny_live_blackbox_integration.py tests/proving/test_tiny_evidence_verification.py -q --tb=short --basetemp .pytest-tmp-v2090e-request-trace-core1` 通过（28 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/execution/test_service_runner.py tests/execution/test_service_runner_environment.py tests/evidence/test_service_run_evidence.py tests/evidence/test_live_blackbox_evidence.py tests/negative/test_live_blackbox_integration_fail_closed.py tests/negative/test_service_run_evidence_fail_closed.py tests/negative/test_service_run_closeout_gate.py tests/proving/test_tiny_live_blackbox_integration.py tests/proving/test_tiny_evidence_verification.py tests/proving/test_workspace_evidence_export.py -q --tb=short --basetemp .pytest-tmp-v2090e-request-trace-combined1` 通过（67 passed）；最终组合验证 `$env:PYTHONPATH='src;.'; python -m pytest tests/execution/test_service_runner.py tests/execution/test_service_runner_environment.py tests/evidence/test_service_run_evidence.py tests/evidence/test_live_blackbox_evidence.py tests/negative/test_live_blackbox_integration_fail_closed.py tests/negative/test_service_run_evidence_fail_closed.py tests/negative/test_service_run_closeout_gate.py tests/proving/test_tiny_live_blackbox_integration.py tests/proving/test_tiny_evidence_verification.py tests/proving/test_workspace_evidence_export.py tests/contracts/test_tiny_fullstack_contract_fixture.py tests/proving/test_tiny_contracts.py tests/negative/test_tiny_contract_recovery_fail_closed.py tests/negative/test_run_manifest_command_coverage.py -q --tb=short --basetemp .pytest-tmp-v2090e-final-combined` 通过（95 passed）；`git diff --check` 通过；进程残留检查未发现 backend/frontend/tiny live blackbox（后端/前端/微型真实黑盒）进程。
- 未作为通过证据：`tests/proving/test_tiny_package_assembly.py` 默认真实 provider/package assembly（模型供应商/包组装）路径本轮未完整重跑；该路径包含真实 provider 调用，已在 V2-090D 日志记录过耗时风险。

## 2026-06-03 — V2-090F Golden sample rebuild 阻塞记录

- 工作包：V2-090F（Golden sample rebuild，黄金样例重建）
- 当前状态：BLOCKED（阻塞），不得标记 DONE（完成），Phase 9（第 9 阶段）保持 5 / 6。
- 阶段性产出：`scripts/build_tiny_closeout_sample.py`（构建微型收尾样例脚本）改为 V2-090F 入口，支持 locked provider replay（锁定模型产物重放）、`--check`（检查模式）只比较生成树、复制 `30-audit/agent-context-index.json`（智能体上下文索引）以保留 provider context window（模型供应商上下文窗口）；`tests/proving/fixtures/tiny_closeout.py`（微型收尾夹具）开始接入 live blackbox fixture（真实黑盒夹具）和 workspace evidence bundle（工作区证据包）；`src/boardroom_os/providers/openai_adapter.py`（OpenAI 模型适配器）新增 `BOARDROOM_OPENAI_CONTEXT_WINDOW`（模型供应商上下文窗口）默认 `400000`，并要求 `BOARDROOM_OPENAI_TIMEOUT_SECONDS`（模型请求超时）显式配置；`.env.example` 与 `.env.template` 同步 600 秒和 400000 窗口。
- Negative / fail-closed 进展：旧 V2-080F failure package（失败包）缺 role prompt hook lineage（角色提示词钩子来源链）或缺 run command evidence（运行命令证据）时被阻断；缺 `run-backend` / `run-frontend` service evidence（服务运行证据）或 run manifest binding（运行清单绑定）时 `CloseoutGate`（收尾门禁）blocked；样例树包含未登记 `__pycache__` / `.pytest*` / 临时 SQLite 文件时 fail closed（失败关闭）；locked provider artifact（锁定模型产物）篡改或 context window（上下文窗口）不一致时被拒绝。
- Happy path 局部证据：fake provider transport（模拟模型传输）只用于测试传输形状，已证明 closeout fixture（收尾夹具）能把 service run evidence（服务运行证据）、live blackbox evidence（真实黑盒证据）、FinalEvidenceTable（最终证据表）和 closeout package（收尾包）串接到同一组 refs（引用）；locked replay（锁定重放）可保留 `agent-context-index.json` 中的 `context_window`（上下文窗口）并避免本机 `.env` 改动破坏离线校验。
- 真实阻塞：真实 provider-backed source delivery（模型供应商支撑的源码交付）当前不是自主 agent work（智能体工作）。实际请求是每个 ticket（任务）一次 LLM request（大模型请求），让模型一次性返回 JSON 文件内容；它不会自主读取 workspace（工作区）、写文件、运行命令、观察失败、循环修复或归档 command evidence（命令证据）。这暴露出框架仍缺最基本的 AgentWorkExecutor（智能体工作执行器）/ agent loop executor（智能体循环执行器）。因此 ProviderAttempt（模型调用尝试记录）只能作为模型请求事实，不能作为 agent team（智能体团队）自治实施完成证据。
- 超时与配置结论：本地 ignored `.env`（环境文件）已将 `BOARDROOM_OPENAI_TIMEOUT_SECONDS` 设为 `600`、`BOARDROOM_OPENAI_CONTEXT_WINDOW` 设为 `400000`；调试请求体落在 ignored `.evidence/debug/v2-090f-provider-request-bodies.json`（调试请求体快照），不提交。600 秒仍超时时，根因不能只归类为 provider slow（模型供应商慢）；`BOARDROOM_OPENAI_TIMEOUT_SECONDS` 是 LLM request timeout（大模型请求超时），不是 agent task deadline（智能体任务期限）。当前脚本把同一值用作 provider-backed generation subprocess deadline（模型供应商生成子进程期限）只是阻塞期间暴露的临时边界，后续应拆分。
- 已运行验证：`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_closeout.py tests/proving/test_tiny_live_blackbox_integration.py tests/negative/test_run_manifest_command_coverage.py -q --tb=short` 通过（55 passed in 212.88s）；`$env:PYTHONPATH='src;.'; python -m pytest tests/execution/test_openai_provider_adapter.py tests/proving/test_tiny_provider_attempts.py tests/proving/test_tiny_closeout.py::test_locked_provider_replay_uses_sample_context_window tests/proving/test_tiny_closeout.py::test_locked_provider_replay_rejects_inconsistent_context_window tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_copy_provider_lock_preserves_context_index tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_script_rebuild_is_stable tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_check_does_not_write_output_root -q --tb=short -k "not real_provider_records_attempts_for_every_tiny_implementation_ticket" --basetemp .pytest-tmp-context-window-related2` 通过（43 passed, 1 deselected）；提交前补跑 `$env:PYTHONPATH='src;.'; python -m pytest tests/execution/test_openai_provider_adapter.py -q --tb=short --basetemp .pytest-tmp-v2090f-provider-config-final` 通过（16 passed）；`$env:PYTHONPATH='src;.'; python -m pytest tests/proving/test_tiny_closeout.py::test_locked_provider_replay_uses_sample_context_window tests/proving/test_tiny_closeout.py::test_locked_provider_replay_rejects_inconsistent_context_window tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_copy_provider_lock_preserves_context_index tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_script_rebuild_is_stable tests/proving/test_tiny_closeout.py::test_tiny_closeout_sample_check_does_not_write_output_root -q --tb=short --basetemp .pytest-tmp-v2090f-locked-replay-final` 通过（5 passed）；`git diff --check` 通过。
- 未作为通过证据：`python scripts/build_tiny_closeout_sample.py` 真实 provider-backed build（模型供应商支撑构建）没有形成可提交 passed golden sample（通过黄金样例），曾出现 600 秒 deadline（期限）超时，随后暴露 `ticket-tiny-frontend-ui` provider source delivery（模型供应商源码交付）不满足 execution package（执行包）要求。提交前运行 `$env:PYTHONPATH='src;.'; python scripts/build_tiny_closeout_sample.py --check` 失败，原因是现有样例锁内 ProviderAttempt（模型调用尝试记录）缺 `role_prompt_hook_ref` / `role_prompt_hook_version` / `role_prompt_hook_sha256`（角色提示词钩子引用 / 版本 / 哈希），即旧 V2-080F failure package（失败包）缺 role prompt hook lineage（角色提示词钩子来源链）。该失败是阻塞证据，不是 Phase 9 完成证据。

## 2026-06-10 — V2-090G Atomic-agent package/import integration

- 工作包：V2-090G（Atomic-agent package/import integration，原子智能体包导入集成）
- 关键产出：`src/boardroom_os/execution/atomic_agent.py`、`tests/execution/test_atomic_agent_invocation_compiler.py`、`tests/execution/test_atomic_agent_result_projection.py`、`tests/negative/test_atomic_agent_integration_fail_closed.py`、`tests/proving/test_tiny_atomic_agent_bridge.py`、`README.md`；同步更新 `src/boardroom_os/execution/__init__.py`、`tests/evidence/test_evidence_verifier.py`、`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md`。
- 关键实现：Boardroom OS 通过 `AtomicAgentPort` / `AtomicAgentPackageAdapter`（原子智能体端口 / 包适配器）调用外部 atomic-agent 的 `AgentRuntimePort.invoke(AgentInvocation)`（智能体运行端口）；`AtomicInvocationCompiler`（原子调用编译器）把 `ExecutionPackage`（执行包）映射为 atomic-agent `AgentInvocation`（智能体调用）；`AtomicAgentResultValidator`（原子结果校验器）复用 atomic-agent event stream verifier（事件流验证器）校验事件 schema（结构）、sequence（序号）、previous hash（前序哈希）、单事件 hash（事件哈希）和 bytes hash（字节哈希），并拒绝缺包、缺事件流、缺工作区变更、越权路径、越权命令、缺 artifact sha256（产物哈希）、缺 tool attempt lineage（工具尝试来源链）和治理字段注入；`AtomicResultProjector`（原子结果投影器）校验 `ProviderAttempt`（模型调用尝试记录）与 `ExecutionPackage`（执行包）绑定后，再把 completed `AgentRunResult`（智能体运行结果）投影为 Boardroom `WorkProductSubmission`（工作产物提交）和 SourceInventory lineage input（源码清单来源链输入）。
- Negative tests：覆盖 missing package fallback（缺包降级）、缺可审计 package version（包版本）或 `unknown` 版本、非 `AgentRunResult` 返回、伪造同名 `AgentRunResult`、failed/interrupted result（失败/中断结果）、缺 workspace mutation（工作区变更）、治理字段注入、allowed_write_set（允许写入集合）越界、POSIX / Windows 绝对路径和 `..` 逃逸、command cwd（命令工作目录）越界、文件路径目录扩权、result-side 与 event stream `command_results`（事件流命令结果）未声明 `command_id`（命令编号）、result.run_id（结果运行 ID）与 event stream run_id（事件流运行 ID）不一致、event stream hash mismatch（事件流哈希不一致）、event hash chain mismatch（事件哈希链不一致）、event_stream_root（事件流根目录）缺失或越界、artifact 缺 sha256、workspace mutation 缺 tool attempt lineage（工具尝试来源链）和 provider attempt / execution package mismatch（模型调用尝试 / 执行包不匹配）、result 侧 workspace mutation/artifact 与 event stream summary（事件流摘要）不一致。TDD RED（测试驱动红灯）阶段先运行新增测试，确认因缺 `boardroom_os.execution.atomic_agent`（原子智能体执行模块）失败；review RED（复核红灯）阶段确认补强负例能暴露类型校验、事件流边界、路径匹配、命令策略、run_id 绑定和跨平台路径缺口。
- Happy path：fake `AgentRuntimePort`（模拟智能体运行端口）返回 completed `AgentRunResult` 后，Boardroom 能验证 event stream（事件流）、生成 work product draft（工作产物草稿）和 source lineage input（源码来源链输入）；tiny backend worker execution package（微型后端实施执行包）可编译为 atomic-agent invocation（原子智能体调用）。
- 验证：`python -m pip install -e ../atomic-agent` 成功从同级 `/Users/bill/projects/atomic-agent` editable install（可编辑安装）；`python -c "from atomic_agent import AgentRuntimePort; from atomic_agent.models import AgentInvocation, AgentRunResult, AgentRunStatus; ..."` 输出字段与 V2-090G spec（规范）一致；`PYTHONPATH=src:. python -m pytest tests/execution/test_atomic_agent_invocation_compiler.py tests/execution/test_atomic_agent_result_projection.py tests/negative/test_atomic_agent_integration_fail_closed.py tests/proving/test_tiny_atomic_agent_bridge.py -q --tb=short --basetemp .pytest-tmp-v2090g-targeted-final` 通过（36 passed）；`PYTHONPATH=src:. python -m pytest tests/execution/test_execution_package_schema.py tests/execution/test_execution_package_compiler.py tests/execution/test_work_product_submission.py tests/evidence/test_evidence_claim.py tests/evidence/test_evidence_verifier.py tests/reducers/test_completion_gate_with_evidence.py -q --tb=short --basetemp .pytest-tmp-v2090g-regression-final` 通过（146 passed）；`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_contracts.py tests/proving/test_tiny_ticket_graph.py tests/proving/test_tiny_live_blackbox_integration.py tests/proving/test_workspace_evidence_export.py tests/proving/test_tiny_atomic_agent_bridge.py -q --tb=short --basetemp .pytest-tmp-v2090g-phase9-final` 通过（47 passed）。
- 边界说明：V2-090G 不复制 atomic-agent 源码、不调用 examples CLI（示例命令行）、不服务化，也不让 `AgentRunResult.status == completed`（智能体运行完成）直接映射为 `TICKET_COMPLETED`（任务完成）或 `CloseoutPackage.passed`（收尾包通过）。V2-090G 不重建 golden sample（黄金样例）；V2-090F 仍为 BLOCKED（阻塞），必须等待人工评审 V2-090G 证据后再决定是否恢复。

## 2026-06-10 — V2-090H Atomic-agent executor switch 立项纠偏

- 工作包：V2-090H（Atomic-agent executor switch，原子智能体执行器切换）
- 纠偏结论：V2-090G 只完成 atomic-agent package/import boundary（原子智能体包导入边界）、compiler（编译器）、validator（校验器）和 projector（投影器）；当前 implementation ticket（实施任务）主执行路径仍是 `ProviderExecutor`（模型供应商执行器）+ `OpenAIProviderTransport`（OpenAI 模型传输）的单次 LLM request（大模型请求），不能证明 agent loop executor（智能体循环执行器）已接入。
- 文档更新：`doc/04-implementation/backlog.md` 将 Phase 9（第九阶段）从 7 个工作包扩展为 8 个，当前未完成工作包改为 V2-090H；`doc/04-implementation/acceptance-criteria.md` 新增 atomic-agent 主执行路径 checkbox（复选项），并要求 V2-090F（黄金样例重建）必须等 V2-090H 真实 atomic execution evidence（原子执行证据）人工评审后才可恢复。
- 下一步验收口径：V2-090H 必须构造并派发一个 atomic implementation ticket（原子实施任务），由接入真实 provider（模型供应商）的 atomic-agent executor 通过 `AgentRuntimePort.invoke(AgentInvocation)`（智能体运行端口调用）产生 event stream（事件流）、workspace mutation（工作区变更）、command evidence（命令证据）和 source lineage input（源码来源链输入）；`AgentRunResult.status == completed`（智能体运行完成）仍不得直接映射为 `TICKET_COMPLETED`（任务完成）或 closeout passed（收尾通过）。

## 2026-06-10 — V2-090H Atomic-agent executor switch 实施完成

- 工作包：V2-090H（Atomic-agent executor switch，原子智能体执行器切换）
- 关键产出：新增 `config/boardroom-runtime.example.yaml`、`config/boardroom-providers.example.yaml`、`config/boardroom-roles.example.yaml`、`doc/06-reference/atomic-agent-contract-lock.md`、`src/boardroom_os/config/boardroom.py`、`src/boardroom_os/execution/atomic_executor.py`、`tests/config/test_boardroom_config.py`、`tests/negative/test_boardroom_config_fail_closed.py`、`tests/execution/test_atomic_agent_executor.py`、`tests/negative/test_atomic_agent_executor_fail_closed.py`、`tests/proving/test_tiny_atomic_agent_executor.py`、`tests/proving/test_tiny_atomic_agent_executor_script.py`、`scripts/run_tiny_atomic_agent_executor.py`；同步更新 `.env.template`、`.env.example`、`README.md`、`src/boardroom_os/execution/atomic_agent.py`、`src/boardroom_os/execution/__init__.py` 和 V2-090H 相关文档状态。
- 关键实现：`.env`（环境文件）瘦身为 config paths（配置路径）、secret（密钥）和 bootstrap path（启动路径），真实 `.env` 已备份到 `.env.backups/.env.v2-090h.20260610T082631Z.bak` 后迁移；三文件配置模型按 runtime/providers/roles（运行时/供应商/角色）分离，拒绝旧 `BOARDROOM_OPENAI_*` 执行配置键、未知 provider type（供应商类型）、role inline provider 参数（角色内联供应商参数）、缺 contract lock（契约锁）、`.worktrees/atomic-agent` 路径和预算越界。`AtomicInvocationCompiler.compile_with_settings`（配置感知原子调用编译）从 settings（配置）解析 provider profile（供应商配置档）、role slot（角色席位）、tools（工具）、budgets（预算）和 config hashes（配置哈希）。`AtomicAgentExecutor`（原子智能体执行器）调用真实 `AgentRuntimePort`（智能体运行端口），校验 event stream（事件流）、provider turn facts（模型轮次事实）、command evidence（命令证据）、workspace mutation（工作区变更）和 source lineage input（源码来源链输入），并投影 Boardroom `ProviderAttempt`（模型调用尝试记录）与 `WorkProductSubmission`（工作产物提交）。
- Negative tests：证明 `ProviderExecutor`（模型供应商执行器）不能满足 implementation ticket evidence（实施任务证据）；缺 runtime port（运行时端口）、缺 provider turn facts、缺 command evidence、fake provider transport（模拟供应商传输）、治理字段注入、CRLF event stream（CRLF 事件流）、workspace mutation 后重试、相对 event stream ref（事件流引用）越界、目录 allowed_write_set（允许写入集合）不匹配、缺写后 hash（哈希）和 submitted artifact（提交产物）缺失均 fail closed（失败关闭）。
- Happy path：`scripts/run_tiny_atomic_agent_executor.py` 构造最小 atomic implementation ticket（原子实施任务），经真实 provider-backed atomic-agent executor（模型供应商支撑原子智能体执行器）写入 `work/real-provider-output.txt`、执行声明命令 `cmd.check-output`，并产出 canonical JSONL event stream（规范 JSONL 事件流）、workspace mutation、command evidence、provider attempt projection（模型调用尝试投影）和 source lineage input。实现过程中发现并修正 Boardroom 侧对相对 `event_stream_ref`、目录写入策略、atomic-agent `after_hash` 字段和 extra audit artifacts（额外审计产物）的过严/不一致校验；这些均已补回归测试。
- 验证：`python -m pip install -e "${BOARDROOM_ATOMIC_AGENT_PATH:-../atomic-agent}"` 成功；atomic-agent active contract 字段与锁定 hash 匹配；`PYTHONPATH=src:. python -m pytest tests/config/test_boardroom_config.py tests/negative/test_boardroom_config_fail_closed.py tests/execution/test_atomic_agent_invocation_compiler.py tests/execution/test_atomic_agent_executor.py tests/negative/test_atomic_agent_executor_fail_closed.py tests/execution/test_atomic_agent_result_projection.py tests/negative/test_atomic_agent_integration_fail_closed.py tests/proving/test_tiny_atomic_agent_bridge.py -q --tb=short --basetemp .pytest-tmp-v2090h-targeted` 通过（68 passed）；`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_atomic_agent_executor_script.py -q --tb=short --basetemp .pytest-tmp-v2090h-script` 通过（1 passed）；`set -a; source .env; set +a; PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_atomic_agent_executor.py -q --tb=short --basetemp .pytest-tmp-v2090h-proving` 通过（1 passed in 52.23s）。
- 评审风险：真实 provider（模型供应商）曾多次返回非法 JSON 或旧 `action_envelope`（动作信封）结构；外部 atomic-agent provider system prompt（供应商系统提示）与 parser（解析器）实际 top-level action schema（顶层动作结构）存在不一致迹象。V2-090H 通过显式任务约束和 `max_parse_failures` 预算上限完成最小 proving，但该 provider steering（模型引导）稳定性需要专家评审。V2-090F 仍为 BLOCKED，不得自动恢复。

## 2026-06-10 — V2-090H Retry-rate probe 观察

- 观察口径：新增 `scripts/run_atomic_agent_retry_rate_probe.py` 与 `tests/proving/test_atomic_agent_retry_rate_probe.py`，按 AgentLoop（代理循环）事件流统计 `action.rejected / provider.turn.completed`，不做 whole-ticket rerun（整票重派）统计。
- 三个完全不同的 atomic ticket（原子任务）均由真实 provider-backed atomic-agent executor（模型供应商支撑原子智能体执行器）完成：`ticket.retry-rate.note` 写入 `work/retry-rate-note.txt` 并通过 `cmd.check-note`；`ticket.retry-rate.json` 写入 `work/retry-rate-data.json` 并通过 `cmd.check-json`；`ticket.retry-rate.python` 写入 `work/retry_rate_calc.py` 并通过 `cmd.check-python`。三票 terminal event（终止事件）均为 `run.completed`，命令 exit code 均为 0，均产生 workspace mutation（工作区变更）和 source lineage input（源码来源链输入）。
- 最终独立观测运行：`ticket.retry-rate.note` run id 为 `boardroom-atomic.retry-rate.note.20260610T093947Z.528e7819`，event stream 为 `boardroom-atomic.retry-rate.note.20260610T093947Z.528e7819.jsonl`，provider turns 6、action rejections 3、retry rate 0.5；`ticket.retry-rate.json` run id 为 `boardroom-atomic.retry-rate.json.20260610T094041Z.1e530f21`，event stream 为 `boardroom-atomic.retry-rate.json.20260610T094041Z.1e530f21.jsonl`，provider turns 5、action rejections 2、retry rate 0.4；`ticket.retry-rate.python` run id 为 `boardroom-atomic.retry-rate.python.20260610T094116Z.64f8c9c6`，event stream 为 `boardroom-atomic.retry-rate.python.20260610T094116Z.64f8c9c6.jsonl`，provider turns 7、action rejections 4、retry rate 0.5714285714。
- 汇总结果：3 / 3 ticket 成功，provider turns 合计 18，action rejections 合计 9，retryable action rejections 合计 9，整体 retry rate 为 0.5。该结果确认当前真实 provider steering（模型引导）仍存在较高 parse/action retry（解析/动作重试）成本，但 retry budget（重试预算）足以完成本轮三个不同 atomic implementation ticket（原子实施任务）。
- 验证：`set -a; source .env; set +a; PYTHONPATH=src:. python -m pytest tests/proving/test_atomic_agent_retry_rate_probe.py -q --tb=short --basetemp .pytest-tmp-v2090h-retry-rate` 通过（1 passed in 172.69s）；`set -a; source .env; set +a; PYTHONPATH=src:. python scripts/run_atomic_agent_retry_rate_probe.py` 返回 exit 0，并生成上述三票 JSON report（报告）。

## 2026-06-10 — V2-090I Resettable medium implementation scenario 阻塞

- 工作包：V2-090I（Resettable medium implementation scenario，可重置中等复杂实施场景）
- 关键产出：新增 `scripts/run_v2_090i_medium_scenario.py`、`tests/proving/test_v2_090i_medium_scenario_script.py`、`tests/proving/test_v2_090i_medium_scenario.py`；同步更新 `scripts/README.md`、`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md`。
- 关键实现：V2-090I runner（运行脚本）使用 marked workspace（标记工作区）`.evidence/atomic-agent/v2-090i-medium-scenario-workspace/`，`--reset` 只允许删除带 `.boardroom-v2-090i-workspace.json` 的场景目录。ExecutionPackage（执行包）要求真实 provider-backed atomic-agent executor 生成 `forecast_engine` 多文件 Python package/CLI（Python 包/命令行工具），包含 weighted moving average（加权移动平均）、linear regression forecast（线性回归预测）、max drawdown（最大回撤）和 risk analysis（风险分析）等多步算法逻辑，并通过 `cmd.check-medium-scenario` 外部验证。
- Negative / fail-closed：证明 reset 拒绝缺 marker 目录；缺 provider secret 时脚本 fail closed；真实 provider proving test（真实供应商证明测试）在未设置 `BOARDROOM_RUN_REAL_PROVIDER_PROVING=1` 时默认跳过；validator command（验证命令）独立检查 required files（必需文件）、跨文件 import（导入依赖）、API 算法结果、unittest 和 CLI JSON 输出。`PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090i_medium_scenario_script.py -q --tb=short` 通过（9 passed）；`env -u OPENAI_API_KEY PYTHONPATH=src:. python scripts/run_v2_090i_medium_scenario.py --reset` 返回 exit 1，stderr 为 `ValueError: provider api key env is required`。
- 真实 provider 阻塞证据：三次显式 opt-in 真实 provider run 均未通过 `tests/proving/test_v2_090i_medium_scenario.py`，因此 V2-090I 当时不得标记 DONE。第一次 `boardroom-atomic.v2-090i.medium.20260610T145623Z.cbc1565e` 运行 610.35s 后失败，事件流包含多次 invalid JSON / `action_envelope` schema mismatch，写出 `__init__.py` 与 `statistics.py` 后 `run.failed`。第二次 `boardroom-atomic.v2-090i.medium.20260610T150942Z.6856ed07` 运行 287.48s 后失败，provider 选择 `apply_patch` 一次写多文件，被 permission policy 拒绝为 `invalid_path_type_denied`。第三次 `boardroom-atomic.v2-090i.medium.20260610T151602Z.f22b41dc` 运行 849.23s 后失败；禁用 `apply_patch` 后写出 `__init__.py`、`statistics.py`、`risk.py`、`cli.py`，产生 11 个 provider turns、7 个 action rejections、4 个 workspace mutations，但未生成 `work/tests/test_forecast_engine.py`，未运行 `cmd.check-medium-scenario`，terminal event 为 `run.failed`，原因 `action_parse_failed` / invalid JSON。
- 外部评审记录：完整实施流水、三次真实 provider run 事件计数、自审结论和评审关注点已单独记录在 `doc/05-project-log/v2-090i-implementation-run-record.md`。
- 当时验证结论：V2-090I runner 和负例门禁已实现，但真实 provider-backed atomic-agent executor 当时未能顺利完成中等复杂多文件 implementation ticket（实施任务）并通过验收门禁。该结果需要专家评审 provider steering（模型引导）、action protocol（动作协议）、任务拆分粒度和工具策略；V2-090F 仍为 BLOCKED，不得恢复。

## 2026-06-11 — V2-090J Atomic action protocol repair 计划修订

- 工作包：V2-090J（Atomic action protocol repair，原子动作协议修复）
- 当时状态：TODO（计划已形成，尚未实施）。V2-090I 当时保持 BLOCKED（阻塞），V2-090F 保持 BLOCKED，不得因计划文档存在而恢复 golden sample rebuild（黄金样例重建）。2026-06-11 后续复跑已将 V2-090I 更新为 DONE。
- 根因结论：V2-090I 的主因不是 Boardroom OS（董事会系统）证据门禁整体过严，而是 atomic-agent action protocol（原子智能体动作协议）对中等复杂任务不稳：provider（模型供应商）能生成实现内容，但不能稳定按“一轮一个顶层 JSON action（动作对象）”执行；同时存在 `apply_patch` 工具可见性与权限策略错配，以及 validator command（验证命令）未作为执行循环 checkpoint（检查点）前置调度。
- 计划产出：新增 `docs/superpowers/specs/2026-06-11-v2-090j-atomic-action-protocol-repair-design.md` 和 `docs/superpowers/plans/2026-06-11-v2-090j-atomic-action-protocol-repair.md`，明确 `AgentActionBatch`（智能体动作批次）只接受 `protocol="agent-action-batch-v1"`，缺协议的 batch-like output（批次形状输出）以 `batch_like_without_protocol` fail closed；裸数组、串联 JSON、Markdown 包裹 JSON 和旧 `action_envelope` 不做静默兼容。
- 评审修订：`max_actions_per_turn` 必须进入 Boardroom budget profiles（预算档）与 budget caps（预算上限），V2-090J runner 必须证明解析后值大于 1；checkpoint `max_auto_runs` 必须来自 `atomic_agent.checkpoints.required_output.max_auto_runs` 或同等单一配置源，不得硬编码；当前 `PackageCommand`（包命令）无 metadata/validator marker（元数据/验证器标记），所以 V2-090J 只支持单声明命令 checkpoint，多命令包 fail closed。
- 工具策略：`allow_apply_patch` 字段落点明确为 `atomic_agent.filesystem.allow_apply_patch` / `AtomicFilesystemConfig.allow_apply_patch`。如果 runtime 或 role tools（运行时或角色工具）暴露 `apply_patch` 但 runtime policy（运行时策略）未启用，`AtomicInvocationCompiler`（原子调用编译器）必须在 provider invocation（模型调用）前 fail closed。V2-090I 脚本局部过滤 `apply_patch` 的 workaround（绕过）必须被正式配置/解析器策略取代。
- 验收边界：V2-090J 单元测试可使用 test provider double（测试替身）验证 parser/loop/checkpoint semantics（解析器/循环/检查点语义），但不能作为 proving success（证明成功）。V2-090J `DONE` 仍要求显式 opt-in 真实 provider proving test 通过，并保留 command evidence、workspace mutation、source lineage input 和 provider turn facts 硬门禁。

## 2026-06-11 — V2-090J Atomic action protocol repair 非 provider 实施

- 工作包：V2-090J（Atomic action protocol repair，原子动作协议修复）
- 当时状态：BLOCKED。atomic-agent（原子智能体）与 Boardroom OS（董事会系统）非 provider 路径已实现并通过 targeted tests（目标测试）；真实 provider proving gate（真实供应商证明门禁）三次运行未稳定通过，因此当时 V2-090J 验收 checkbox 保持未勾选，V2-090F 继续 BLOCKED。
- atomic-agent 产出：新增 `AgentActionBatch`（智能体动作批次）与 `ParsedAgentTurn`（解析后轮次）模型，`parse_agent_turn`（轮次解析器）仅接受显式 `protocol="agent-action-batch-v1"` 的 batch；裸数组、串联 JSON、缺协议 batch-like output（批次形状输出）保持 fail closed。`AgentLoop`（智能体循环）按顺序执行 batch actions，执行 `max_actions_per_turn` 上限，并在 required outputs（必需产物）齐备后通过 declared command（声明命令）触发 checkpoint（检查点），但不自动 `submit_result`。
- Boardroom 产出：`AgentExecutionBudgetConfig`（智能体执行预算配置）/ `BudgetCapsConfig`（预算上限配置）加入 `max_actions_per_turn`；`AtomicCheckpointConfig`（原子检查点配置）加入 `required_output.max_auto_runs`；`AtomicInvocationCompiler`（原子调用编译器）输出 `action_protocol` metadata（元数据）、batch budget（批次预算）和 `required_output_checkpoint`；`apply_patch` 可见但 runtime policy（运行时策略）未启用时编译期 fail closed；`AtomicAgentResultValidator`（原子结果校验器）要求 declared command evidence（声明命令证据）真实出现。
- 新增/修改文件：`scripts/run_v2_090j_medium_scenario.py`、`tests/proving/test_v2_090j_medium_scenario_script.py`、`tests/proving/test_v2_090j_medium_scenario.py`、`config/boardroom-runtime.example.yaml`、`config/boardroom-roles.example.yaml`、`src/boardroom_os/config/boardroom.py`、`src/boardroom_os/execution/atomic_agent.py`、`src/boardroom_os/execution/atomic_executor.py`，以及外部 `/Users/bill/projects/atomic-agent` 的 parser/loop/docs/tests。
- 已跑验证：atomic-agent targeted tests：`python -m pytest tests/test_action_parser.py tests/test_agent_loop.py tests/test_evidence.py tests/test_runtime_port.py -q --tb=short`，结果 `83 passed`；Boardroom targeted tests：`PYTHONPATH=src:. python -m pytest tests/config/test_boardroom_config.py tests/negative/test_boardroom_config_fail_closed.py tests/execution/test_atomic_agent_invocation_compiler.py tests/execution/test_atomic_agent_executor.py tests/execution/test_atomic_agent_result_projection.py tests/negative/test_atomic_agent_executor_fail_closed.py tests/proving/test_v2_090i_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-targeted`，结果 `80 passed, 1 skipped`；V2-090J non-provider proving tests：`PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090j_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-non-provider`，结果 `8 passed, 1 skipped`。
- 真实 provider 证据：第一次 `boardroom-atomic.v2-090j.medium.20260610T183832Z.eb20d2d6` 产生 `run.completed`、8 个 provider turns、9 个 workspace mutations、4 次 `cmd.check-medium-scenario`（最终 exit 0）和 `result.submitted`，但暴露 Boardroom validator 对多次重写同一 produced path（产物路径）的 mutation summary（变更摘要）比较错误；修复后该事件流可验证通过。第二次 `boardroom-atomic.v2-090j.medium.20260610T185459Z.40fd49fe` 以 `action_parse_failed` 终止，14 个 provider turns、9 个 workspace mutations、4 次 command completed，但无 `result.submitted`。第三次 `boardroom-atomic.v2-090j.medium.20260610T191431Z.593beabf` 以 `action_parse_failed` 终止，9 个 provider turns、6 个 workspace mutations、2 次 command completed，无 `result.submitted`。
- 中间阻塞结论：V2-090J 当时已证明 batch/checkpoint/config/validator 非 provider 语义和一次真实 completed event stream 可被修复后验证，但尚未证明真实 provider-backed executor（模型供应商支撑执行器）稳定完成 medium package/CLI。后续需继续收紧 provider output steering（模型输出引导）或引入 provider-native structured output/tool calling（供应商原生结构化输出/工具调用），而不是放松 evidence gates（证据门禁）。

## 2026-06-11 — V2-090J Atomic action protocol repair 完成

- 工作包：V2-090J（Atomic action protocol repair，原子动作协议修复）
- 当前状态：DONE。V2-090J 真实 provider proving gate（真实供应商证明门禁）已通过；V2-090I 保留为修复前失败证据，V2-090F 仍需人工评审后决定是否恢复 golden sample rebuild（黄金样例重建）。
- 追加修复：外部 `/Users/bill/projects/atomic-agent` 的 `OpenAICompatibleProviderAdapter`（OpenAI 兼容供应商适配器）移除 system prompt（系统提示词）中的旧 `action_envelope` 示例，改为展示合法 `AgentAction`（智能体动作）与 `AgentActionBatch`（智能体动作批次）示例；prompt 明示 `max_actions_per_turn`（每轮最大动作数）和 required-output checkpoint（必需产物检查点）语义，不放宽 parser（解析器）或 evidence gates（证据门禁）。
- 已跑验证：atomic-agent targeted tests：`python -m pytest tests/test_action_parser.py tests/test_agent_loop.py tests/test_openai_compatible_provider.py tests/test_evidence.py tests/test_runtime_port.py -q --tb=short`，结果 `106 passed`；Boardroom targeted tests：`PYTHONPATH=src:. python -m pytest tests/config/test_boardroom_config.py tests/negative/test_boardroom_config_fail_closed.py tests/execution/test_atomic_agent_invocation_compiler.py tests/execution/test_atomic_agent_executor.py tests/execution/test_atomic_agent_result_projection.py tests/negative/test_atomic_agent_executor_fail_closed.py tests/proving/test_v2_090i_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-targeted-after-prompt`，结果 `81 passed, 1 skipped`。
- 真实 provider 证据：`BOARDROOM_RUN_REAL_PROVIDER_PROVING=1 PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-medium-real-after-prompt`，结果 `1 passed in 534.17s`。run id 为 `boardroom-atomic.v2-090j.medium.20260610T193727Z.f00e1709`，事件流包含 5 个 provider turns、10 个 workspace mutations、5 次 `cmd.check-medium-scenario` command evidence（最终 exit 0）、1 个 `result.submitted` 和 `run.completed`。
- 验收边界：V2-090J 只证明 action protocol repair 后中等复杂 executor path（执行路径）恢复，不自动解除 V2-090F BLOCKED；恢复 golden sample rebuild 前仍需人工评审 V2-090H、V2-090I 与 V2-090J 真实执行证据。

## 2026-06-11 — V2-090I/090J 中等复杂场景复跑自审

- 工作包：V2-090I（Resettable medium implementation scenario，可重置中等复杂实施场景）复跑；先用 V2-090J（Atomic action protocol repair，原子动作协议修复）后的 runner（运行器）验证修复路径，再复跑 V2-090I 原 runner。
- 结论：中等复杂 `forecast_engine` Python package/CLI（Python 包 / 命令行工具）任务可由真实 provider-backed atomic-agent executor（模型供应商支撑原子智能体执行器）完成并通过验收门禁；V2-090I 旧三次失败保留为 pre-repair failure evidence（修复前失败证据），V2-090I 状态更新为 DONE。V2-090F 仍为 BLOCKED，等待专家评审后再决定是否恢复 golden sample rebuild（黄金样例重建）。
- 新鲜非 provider 验证：Boardroom targeted tests（董事会目标测试）`PYTHONPATH=src:. python -m pytest tests/config/test_boardroom_config.py tests/negative/test_boardroom_config_fail_closed.py tests/execution/test_atomic_agent_invocation_compiler.py tests/execution/test_atomic_agent_executor.py tests/execution/test_atomic_agent_result_projection.py tests/negative/test_atomic_agent_executor_fail_closed.py tests/proving/test_v2_090i_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario_script.py tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090-rerun-targeted`，结果 `81 passed, 1 skipped in 0.67s`；atomic-agent targeted tests（原子智能体目标测试）`python -m pytest tests/test_action_parser.py tests/test_agent_loop.py tests/test_openai_compatible_provider.py tests/test_evidence.py tests/test_runtime_port.py -q --tb=short`，结果 `106 passed in 2.55s`。
- 真实 provider 复跑证据一：`BOARDROOM_RUN_REAL_PROVIDER_PROVING=1 PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-rerun-medium-real` 失败，耗时 `211.92s`。run id `boardroom-atomic.v2-090j.medium.20260611T054851Z.7c27fddc`，事件流显示第一轮 provider output（模型输出）包含可完成任务的 `AgentActionBatch`，但同时串联额外 JSON action/batch，被 parser（解析器）按 fail-closed 规则拒绝为 `invalid_json`；第二轮发生 provider SDK connection error（供应商 SDK 连接错误），terminal event 为 `run.failed`。
- 真实 provider 复跑证据二：再次运行 `BOARDROOM_RUN_REAL_PROVIDER_PROVING=1 PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090j_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090j-rerun-medium-real-2` 通过，结果 `1 passed in 505.05s`。run id `boardroom-atomic.v2-090j.medium.20260611T055454Z.016a2a2a`，事件流包含 `run.completed`、5 个 provider turns（模型轮次）、9 个 workspace mutations（工作区变更）、5 次 `cmd.check-medium-scenario` command evidence（命令证据，最终 exit 0）和 `result.submitted`。
- 真实 provider 复跑证据三：独立运行 `PYTHONPATH=src:. python scripts/run_v2_090j_medium_scenario.py --reset` 并保存 report（报告）到 ignored `.evidence/atomic-agent/v2-090j-rerun-report.json`，exit 0。run id `boardroom-atomic.v2-090j.medium.20260611T060341Z.b564b20d`，JSON report 声明 `action_protocol="agent-action-batch-v1"`、`max_actions_per_turn=8`、`checkpoint_max_auto_runs=3`、`provider_transport_kind="real"`、`provider_turn_completed_count=8`、`action_rejected_count=4`、`retry_rate=0.5`、`command_exit_codes["cmd.check-medium-scenario"]=0`、`success=true`；事件流包含 11 个 workspace mutations、6 次 command evidence（最终 exit 0）、source lineage input（源码来源链输入）和 `run.completed`。
- 真实 provider 复跑证据四：V2-090I 原 runner 的 pytest gate `BOARDROOM_RUN_REAL_PROVIDER_PROVING=1 PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090i_medium_scenario.py -q --tb=short --basetemp .pytest-tmp-v2090i-medium-real-rerun` 通过，结果 `1 passed in 222.99s`。run id `boardroom-atomic.v2-090i.medium.20260611T155920Z.5f0c3ba9`，事件流包含 `run.completed`、7 个 provider turns、5 个 workspace mutations、2 次 `cmd.check-medium-scenario` command evidence（先 exit 3，最终 exit 0）和 `result.submitted`。
- 真实 provider 复跑证据五：独立运行 `PYTHONPATH=src:. python scripts/run_v2_090i_medium_scenario.py --reset`，exit 0。run id `boardroom-atomic.v2-090i.medium.20260611T160350Z.9f07d150`，JSON report 声明 `provider_transport_kind="real"`、`provider_turn_completed_count=10`、`action_rejected_count=0`、`retry_rate=0.0`、`command_exit_codes["cmd.check-medium-scenario"]=0`、`success=true`；事件流包含 9 条 workspace mutations、5 条 source lineage inputs、最终 command evidence exit 0 和 `run.completed`。
- 自审结论：本轮没有放松 command evidence、workspace mutation、source lineage、provider turn facts 或 parser fail-closed（解析器失败关闭）门禁；通过证据来自真实 provider transport（真实模型供应商传输）和真实 runner command（运行器命令）。风险仍在 provider output steering（模型输出引导）和 provider connection（模型连接）稳定性：同一任务仍可能先输出串联 JSON 并触发 retry（重试），且真实 provider connection error 可中断运行。建议专家评审重点检查是否需要 provider-native structured output/tool calling（供应商原生结构化输出 / 工具调用）或更强 retry policy（重试策略），而不是降低 evidence gates（证据门禁）。

## 2026-06-12 — V2-090F PRD-to-delivery agent team spec/plan 修订

- 工作包：V2-090F（Golden sample rebuild，黄金样例重建）
- 当前状态：BLOCKED（阻塞）。V2-090H/V2-090I/V2-090J 专家评审已通过，确认 agent team（智能体团队）基础能力具备进入 V2-090F 的条件；但旧 V2-090F 代码和文档仍以 provider artifact lock（模型产物锁）、provider-backed generation subprocess（模型供应商支撑生成子进程）和单次 JSON source delivery（源码交付）为核心，早期修订又偏向 runner 预拆 backend/frontend/integration tickets（后端/前端/集成任务），不能证明从 PRD 到交付的 agent team autonomy（智能体团队自治）。
- 关键产出：新增 `docs/superpowers/specs/2026-06-12-v2-090f-atomic-golden-sample-rebuild-design.md` 与 `docs/superpowers/plans/2026-06-12-v2-090f-atomic-golden-sample-rebuild.md`；同步更新 `doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md`、`doc/04-implementation/INDEX.md`、`doc/05-project-log/INDEX.md`、`examples/README.md` 和 `scripts/README.md`。
- 新设计口径：V2-090F 必须改为 short PRD 输入，由 CEO/Architect/Worker/Tester/Checker/Closeout seats（决策/架构/实施/测试/检查/收尾席位）分别加载自己的 RolePromptHook（角色提示词钩子）、RoleProfile（角色模板）和 skill context（技能上下文），自治产生 BoardDirective（董事会指令）、contracts（合同）、ticket graph（任务图）、implementation tickets（实施任务）、verification plan（验证计划）、checker verdict（检查结论）和 closeout/audit artifacts（收尾/审计产物）。
- 专家评审建议采纳：Task 7 Real Agent Team Orchestration（真实智能体团队编排）拆为 7A CEO/Architect/Tester 生成合同和任务图、7B Worker 执行 implementation tickets、7C Tester/Checker/Closeout 与 Boardroom gates 完成验收；同时新增 bounded reference examples（受限参考示例），只提供验收和任务图形状示例，不提供固定 ticket refs 或执行顺序。
- 配置基线修订：V2-090F 使用独立 `config/boardroom-runtime.v2-090f.yaml`、`config/boardroom-providers.v2-090f.yaml` 和 `config/boardroom-roles.v2-090f.yaml`，所有角色采用 high reasoning（高推理）和高预算 `agent_team.v2_090f.fullstack` 或等价 profile。预算、timeout（超时）和 retry policy（重试策略）必须进入 `00-boardroom/v2-090f-baseline.json`，不得通过 `.env` 临时调参；允许提高 provider timeout，禁止 workspace mutation（工作区变更）后整票重试。
- 废弃边界：`ProviderAttempt`（模型调用尝试记录）只证明 LLM request（大模型请求）事实，不等于 autonomous agent work（自主智能体工作）；`scripts/build_tiny_closeout_sample.py --check`（构建脚本检查模式）未来只能验证已发布样例的 PRD sha256、baseline hash（基线哈希）、角色上下文快照、manifest（清单）、hash（哈希）、证据引用和 closeout payload（收尾载荷），不得调用 provider（模型供应商）、不得写 output root（输出根目录）、不得单独作为端到端成立证据。
- 计划验收：实施计划从 fail-closed negative tests（失败关闭负例测试）开始，覆盖缺 PRD、空 PRD、runner 预置固定任务图、所有角色复用 `seat.worker.implementation`、缺 required seats、RolePromptHook / skill context mismatch、非 worker 越权写源码、旧 provider lock、单 ProviderAttempt、V2-080 failure package、缺 atomic run evidence、缺 command/service/live blackbox/source lineage evidence 和未登记 runtime files（运行时文件）。Happy path（正向路径）必须经过真实 provider opt-in proving（选择加入证明）、clean rebuild（干净重建）、`--check` 无写入验证和 `CloseoutGate`（收尾门禁）passed。
- 自审结论：本轮没有实施 V2-090F 代码，没有重建 `examples/generated-workspaces/tiny-fullstack/`，没有勾选 Phase 9 V2-090F 验收项。修订后的 spec/plan 遵守 Contract first（合同优先）、Reducer first（归约器优先）、Evidence first（证据优先）、Fail closed（失败关闭）、Runtime bounded（运行时有界）和 Negative tests first（负例优先）；下一步等待人工评审后再恢复实施。

## 2026-06-12 — V2-090K Agent team autonomy remediation spec/plan

- 工作包：V2-090K（Agent team autonomy remediation，智能体团队自治整改）
- 当前状态：TODO。用户已提交 V2-090F first run（首次运行）成果；专家评审认可真实 provider worker implementation chain（真实模型供应商支撑的工人实施链路）跑通，但不认可 full agent team autonomy（完整智能体团队自治）成立。原因是 runner/prompt/validator/closeout helper（运行器/提示词/校验器/收尾辅助器）把 `app/server.py`、`python -m app.server`、`LIBRARY_API_*`、固定五票任务图、`/books` 行为探针、`AC-V2-090F-*` 静态验收引用、路径前缀源码面映射和程序化 checker/closeout verdict（检查/收尾结论）塞进规划、证据构造与收尾。
- 用户整改方向：不要求交付包使用固定名称或固定启动命令，只要求它必须能启动；“可启动、可探测、可发布”属于 DevOps/Release（运维/发布）或等价 agent role（智能体角色）的职责，不应由 runner（运行器）硬编码。
- 关键产出：新增并修订 `docs/superpowers/specs/2026-06-12-v2-090k-agent-team-autonomy-remediation-design.md` 和 `docs/superpowers/plans/2026-06-12-v2-090k-agent-team-autonomy-remediation.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/05-project-log/INDEX.md`、`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 和 `doc/05-project-log/decisions.md`。
- 介入点归因：spec（规格）把 090F first run 的介入点分为两类。可通过 RolePromptHook（角色提示词钩子）推动的包括 Architect/Tester/Checker/Closeout/DevOps-Release（架构/测试/检查/收尾/运维发布）职责增强、RunManifest（运行清单）目标意识、前后端拓扑、环境映射和行为探针设计意识。必须改框架/门禁/协议的包括 RunManifest service contract（运行清单服务合同）、BehavioralProbePlan（行为探针计划）schema、动态 closeout runner（动态收尾运行器）、FinalEvidenceTable（最终证据表）/SourceInventory（源码清单）权威源、Checker/Closeout ProviderAttempt（检查/收尾模型调用尝试记录）、默认样例发布和 baseline hash（基线哈希）。
- 计划口径：090K 先写负例，证明 active source/tests/config（活动源码/测试/配置）不再强制 `app/server.py`、`python -m app.server`、`LIBRARY_API_*`、固定五票任务图、`/books` 业务域探针、`AC-V2-090F-*` 静态验收引用、路径前缀源码面映射或 helper-written checker/closeout verdict。随后扩展既有 `workspace.run_manifest`（工作区运行清单）和 `ServiceRunner`（服务运行器），让 agent-generated AcceptanceContract/PackageContract/RunManifest/BehavioralProbePlan（智能体生成验收合同/包合同/运行清单/行为探针计划）声明 service commands（服务命令）、env bindings（环境绑定）、readiness probes（就绪探针）、frontend/backend topology（前后端拓扑）、behavioral probe steps（行为探针步骤）、acceptance criteria（验收条目）和 source surfaces（源码面），closeout 按声明真实启动、探测和构造证据表。
- 状态边界：本轮只制定 spec/plan，没有实施 090K 代码、没有重跑真实 provider full test（完整真实模型测试）、没有发布默认 `examples/generated-workspaces/tiny-fullstack` 通过样例，也没有勾选 V2-090F 或 V2-090K 验收项。V2-090F first run 保持 REVIEW_REQUIRED / BLOCKED，等待 090K 整改后复判。

## 2026-06-13 — V2-090K spec/plan 评审后再修订

- 工作包：V2-090K（Agent team autonomy remediation，智能体团队自治整改）
- 当前状态：TODO。收到新一轮专家评审：前次修订已采纳行为探针声明化和验收/源码面去硬编码，但 plan（计划）又引入 dict-only AcceptanceContract/PackageContract/SourceSurface（仅字典验收合同/包合同/源码面）辅助链路，绕开现有强类型 pydantic contract（契约）和 FinalEvidenceTableBuilder（最终证据表构建器），违反 no duplicate implementations（禁止重复实现）和 no second source of truth（无第二权威源）。
- 修订内容：`docs/superpowers/specs/2026-06-12-v2-090k-agent-team-autonomy-remediation-design.md` 明确 agent JSON artifact（智能体 JSON 产物）只作为输入格式，进入 gate（门禁）前必须强类型解析为现有 AcceptanceContract（验收合同）、PackageContract（包合同）、SourceSurface（源码面）、RunManifest（运行清单）和 SourceLineageRecord（源码来源链记录），并通过 validate_contract_gate（合同门禁校验）；FinalEvidenceTable（最终证据表）必须由 FinalEvidenceTableBuilder（最终证据表构建器）构造，SourceInventory（源码清单）必须由 build_source_inventory（源码清单构建器）构造，禁止 dict-only 平行实现。
- plan 修订：Task 2 修正 role prompt hook test（角色提示词钩子测试）路径为 `tests/execution/test_role_prompt_hooks.py`，并新增 `load_boardroom_settings`（董事会设置加载器）预检，验证 `seat.release.devops`（发布运维席位）、`role.release.devops.v2-090f`、`skill.release.run-manifest`、`role_category: integration`、provider/budget refs（供应商/预算引用）可加载。Task 4 改为 typed contract authority loader（强类型契约权威加载器）+ validate_contract_gate + FinalEvidenceTableBuilder + build_source_inventory。Task 5 改为返回 V2_090KPlanningContracts（强类型规划合同容器），不返回裸 dict。Task 6 补齐 BehavioralProbe executor（行为探针执行器）语义：受限 JSON path（JSON 路径）提取 capture（捕获）、`${}` interpolation（占位符替换）、`json_equals` / `json_contains` / `field_equals` / `field_absent` 断言和 fail-closed（失败关闭）测试。
- 同步更新：`doc/05-project-log/decisions.md`、`doc/04-implementation/backlog.md` 和 `doc/04-implementation/acceptance-criteria.md` 已补充强类型 Contract/Evidence（契约/证据）唯一权威源与行为探针执行语义要求。
- 状态边界：本轮仍只修订 spec/plan 和项目文档；未实施 090K 代码，未运行真实 provider full test（完整真实模型测试），未发布默认 sample（样例），不得勾选 V2-090K 或复判 V2-090F DONE。

## 2026-06-13 — V2-100 Agent-team rework loop 批次规划

- 工作包：V2-100（Agent-team Rework Loop Hardening，智能体团队返工循环强化）
- 当前状态：TODO，已规划但未启动。V2-090F 仍保持 REVIEW_REQUIRED / BLOCKED（需复判/阻塞）；V2-100 不替代 090K 的去硬编码整改，而是在 090K fail-closed（失败关闭）现场基础上补齐团队返工收敛能力。
- 用户方向：agent team（智能体团队）的核心不是单 ticket（工单）一次性执行成功，而是默认局部产物可能不达标，由 CEO（项目经理/治理角色）识别具体问题、规划返工路线、更新 TicketGraph（工单图），再让团队沟通、返工、重验并留下完整审计痕迹。
- 关键文档更新：`doc/03-architecture/domain-model.md` 新增 ReworkCycle / ReworkRequest / ReworkPlan / ReworkAttempt / ReworkOutcome（返工循环/请求/计划/尝试/结果）；`doc/03-architecture/contract-and-evidence-model.md` 明确返工必须重新进入 EvidenceVerifier（证据验证器）、FinalEvidenceTableBuilder（最终证据表构建器）、SourceInventory（源码清单）、Checker（检查者）和必要的 CloseoutGate（收尾门禁）；`doc/03-architecture/execution-and-runtime-boundary.md` 明确 runtime/atomic-agent（运行时/原子智能体）只能执行返工事实，不能规划或接受返工。
- Backlog/验收更新：`doc/04-implementation/backlog.md` 新增 Phase 10 / V2-100A~E：Rework domain model（返工领域模型）、Rework event taxonomy + reducer（返工事件分类与归约器）、CEO rework planner boundary（CEO 返工规划边界）、Rework evidence/checker reintegration（返工证据/检查重接入）、Multi-round rework proving scenario（多轮返工证明场景）。`doc/04-implementation/acceptance-criteria.md` 新增 AC-V2-REWORK-001~005 和 Phase 10 验收段。
- 决策记录：新增 `DEC-0024`，确认 V2-100 将 agent team 从 single-pass fail-closed（单轮失败关闭）升级为 CEO-governed rework loop（项目经理治理返工循环）。
- 状态边界：本轮只做文档规划和架构验收更新，未实施 V2-100 代码，未勾选 Phase 10 验收项，未清理或回滚 `examples/generated-workspaces/tiny-fullstack/` 中的真实 provider artifacts（模型供应商产物）。

## 2026-06-13 — V2-090K 失败现场保护与 V2-100 入口修订

- 工作包：V2-090K / V2-100A（智能体团队自治整改 / 返工领域模型）
- 当前状态：V2-090K 标记为 DONE，但 DONE 语义是“去除 runner/helper 外力介入、agent-generated contracts 成为权威源、真实 full run 可 fail closed 并保留结构化失败现场”，不是 single-pass passed（单轮通过）。V2-090F golden sample 仍为 REVIEW_REQUIRED / BLOCKED。当前未完成工作包切换为 V2-100A。
- 现场保护：新增 `examples/generated-workspaces/tiny-fullstack/30-audit/v2-090k-failure-snapshot/`，精选保留 29 个文件（约 336K），覆盖 `00-boardroom` agent artifacts（智能体产物）、`10-project` 后端实现/测试/运行说明、`20-evidence` 最终证据/源码清单/运行清单投影、`30-audit` 流程审计/重放/Git 审计。未复制 330 个 raw provider artifacts（原始模型产物）和 SQLite runtime state（运行时状态）。
- 失败分类：`failure-summary.json` 记录四类 V2-100 输入 blocker（阻塞项）：BehavioralProbePlan（行为探针计划）期待 `$.title` / `$.id` 但 backend 返回 `{"ok": true, "book": {...}, "id": ...}`；RunManifest（运行清单）声明 `HOST` / `PORT` / `DATABASE_PATH` 但实现仍读取 `LIBRARY_DB_PATH` 等额外 env；FinalEvidenceTable（最终证据表）仍使用旧 `AC-TINY-*` 引用；closeout/audit（收尾/审计）仍引用 `run-v2-080f`。
- 文档修订：`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 和 `doc/05-project-log/decisions.md` 统一采用“090K 完成自治整改，V2-100 负责返工收敛，090F 在二者之后复判”的叙事。V2-100C 要求严格 BlockerReport schema（阻塞报告结构）和有限决策枚举：`fix_implementation`、`fix_contract_or_probe`、`split_ticket`、`reorder_dependencies`、`escalate_human_review`。V2-100E 要求同时使用真实 failure snapshot（失败快照）和 resettable failing fixture（可重置失败夹具）。
- 状态边界：本轮不提交，不清理真实 provider artifacts，不把 `examples/generated-workspaces/tiny-fullstack/` 宣称为 passed golden sample。后续评审通过后再由用户决定如何提交和是否继续 V2-100A 实施。

## 2026-06-14 — 外部审计 V2-100 评审包采纳

- 工作包：V2-100（Agent-team Rework Loop Hardening，智能体团队返工循环强化）
- 当前状态：TODO，采纳外部审计评审包作为 V2-100A~E 的实施基线；本轮不实施 V2-100 代码，不勾选 Phase 10 验收项。
- 关键产出：新增 `doc/04-implementation/current-architecture-review-2026-06-13.md` 和 `doc/04-implementation/v2-100-agent-team-rework-loop-spec.md`；更新 `doc/04-implementation/INDEX.md`；强化七个 baseline RolePromptHook（角色提示词钩子）模板；新增 `tests/execution/test_role_prompt_hooks_rework_governance.py` 锁定多角色 graph patch review（图补丁审查）、返工治理、旧证据/旧 run 拒绝等 prompt 约束。
- 审计采纳修订：确认 CEO（项目经理/治理角色）只负责返工意图和路线，不是 TicketGraphPatch（工单图补丁）的唯一审查者或提交者；RunManifest / service / behavioral probe failure（运行清单 / 服务 / 行为探针失败）必须先被投影为 verified blocker（已验证阻塞项）才能创建 ReworkRequest（返工请求）；graph patch review policy（图补丁审查策略）属于治理配置，应进入 config bundle / governance policy（配置包 / 治理策略），不能只作为 runtime YAML（运行时 YAML）的第二权威源；事件提交表述调整为 governance command handler（治理命令处理器）在 reducer / validator（归约器 / 校验器）校验通过后追加事件。
- 状态边界：role prompt（角色提示词）部分已决定采用；本轮只同步外部审计改进和测试约束，不评审 atomic-agent（原子智能体）内部执行器实现，因为 V2-100 设计将其限定为 ExecutionPackage（执行包）事实执行器。

## 2026-06-14 — V2-100A Rework domain model 完成

- 工作包：V2-100A（Rework domain model，返工领域模型）
- 当前状态：DONE。V2-100A 已把 V2-090K fail-closed（失败关闭）现场转换为可审计返工语言；当前未完成工作包推进到 V2-100B（Rework event taxonomy + reducer，返工事件分类与归约器）。
- 关键产出：新增 `src/boardroom_os/rework/model.py`、`src/boardroom_os/rework/blocker_projection.py`、`src/boardroom_os/rework/__init__.py`、`tests/rework/test_rework_model.py`、`tests/rework/test_v2_090k_failure_snapshot_projection.py`、`tests/negative/test_rework_model_fail_closed.py`；新增 `doc/04-implementation/v2-100a-rework-domain-model-implementation-plan.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/v2-100-agent-team-rework-loop-spec.md`、`doc/04-implementation/backlog.md` 和 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：`ReworkIssue`（返工问题）、`ReworkRequest`（返工请求）、`ReworkPlan`（返工计划）、`ReworkAttempt`（返工尝试）、`ReworkOutcome`（返工结果）和 `ReworkCycle`（返工循环）均为 frozen Pydantic model（冻结 Pydantic 模型），具备 canonical hash（规范哈希）、typed refs（类型化引用）、severity/action/status enums（严重级别/动作/状态枚举）和 fail-closed validator（失败关闭校验器）。`BlockerReport`（阻塞报告）统一投影 `FinalEvidenceTable`（最终证据表）、`CheckerVerdict`（检查结论）、`CloseoutGateResult`（收尾门禁结果）和 V2-090K failure summary（失败摘要），并拒绝缺 verified source（已验证来源）的返工请求。
- Negative tests：`tests/negative/test_rework_model_fail_closed.py` 证明无 verified blocker（已验证阻塞项）、自由文本异常/评审备注、缺 issue refs（问题引用）、缺 acceptance/source/artifact refs（验收/源码面/产物引用）、缺 planner ProviderAttempt（规划者模型调用尝试）、缺 TicketGraphPatch（工单图补丁）、缺 ExecutionPackage / provider attempt / command evidence / source evidence（执行包 / 模型调用 / 命令证据 / 源码证据）和无结果终止等路径均失败关闭。
- Happy path：`tests/rework/test_rework_model.py` 证明 FinalEvidenceTable missing/failed row（最终证据表缺失/失败行）、Checker blocker（检查阻塞项）和 CloseoutGate failure（收尾门禁失败）可形成结构化 ReworkRequest；ReworkAttempt / ReworkOutcome / ReworkCycle 可序列化、排序、hash 和审计。`tests/rework/test_v2_090k_failure_snapshot_projection.py` 证明 090K failure snapshot 中 `probe-response-shape-mismatch`、`env-binding-not-converged`、`final-evidence-uses-old-acceptance-refs` 和 `closeout-audit-references-old-run` 可投影为结构化 ReworkIssue。
- 相关回归修复：宽回归中 193 个失败经定位可快速修复，主要来自 `ServiceRunner`（服务运行器）测试启动 race、`ProcessAuditBuilderInput`（流程审计构建输入）未传入 service runs（服务运行证据）以及旧 closeout/process-audit fixture（收尾/流程审计夹具）未同步 service/live evidence refs（服务/黑盒证据引用）。修复未放宽门禁，只补齐真实 evidence linkage（证据链路）和 raw dict fail-closed 类型错误。
- 验证：`PYTHONPATH=src python -m pytest tests/negative/test_rework_model_fail_closed.py tests/rework/test_rework_model.py tests/rework/test_v2_090k_failure_snapshot_projection.py -q` 通过（20 passed）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/closeout tests/negative -q` 通过（1463 passed）；`git diff --check` 通过。
- 状态边界：V2-100A 只完成返工领域模型和 blocker projection（阻塞项投影）。尚未实现 Rework event taxonomy / reducer（返工事件分类 / 归约器）、CEO rework planner boundary（项目经理返工规划边界）、返工后 evidence/checker reintegration（证据/检查重接入）或 multi-round proving scenario（多轮证明场景）；V2-090F golden sample（黄金样例）仍保持 REVIEW_REQUIRED / BLOCKED。

## 2026-06-14 — V2-100B Rework event taxonomy + reducer 完成

- 工作包：V2-100B（Rework event taxonomy + reducer，返工事件分类与归约器）
- 当前状态：DONE。当前未完成工作包推进到 V2-100C（CEO rework planner boundary，CEO 返工规划边界）。
- 产出：新增 `src/boardroom_os/reducers/rework.py`、`tests/reducers/test_rework_reducer.py`、`tests/negative/test_rework_reducer_fail_closed.py`；扩展 `src/boardroom_os/events/types.py`、`src/boardroom_os/execution/runtime_executor.py`、`tests/reducers/test_event_record.py`、`tests/execution/test_runtime_executor_boundary.py` 和 `tests/rework/test_rework_model.py`。
- 负例：runtime/executor/atomic-agent（运行时/执行器/原子智能体）直接提交 `REWORK_ACCEPTED` / `REWORK_ESCALATED` / `REWORK_EXHAUSTED` 失败；无 `REWORK_REQUESTED` 直接 `REWORK_PLANNED` 失败；GraphPatchReviewGate（图补丁审查门禁）缺必需审查域或存在 rejected review（拒绝审查）失败；返工工单扩大 allowed_write_set（允许写集合）或引用 patch 外 acceptance/source/evidence（验收/源码面/证据）失败。
- 正例：`REWORK_REQUESTED -> REWORK_PLANNED -> REWORK_GRAPH_PATCH_APPROVED -> REWORK_TICKET_CREATED -> REWORK_ATTEMPT_SUBMITTED -> REWORK_REVIEWED -> REWORK_ACCEPTED` 可回放为 ReworkProjection（返工投影），并保留 checked_refs（已检查引用）和 committed_event_refs（已提交事件引用）。
- 验证：`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/closeout tests/rework tests/negative -q` 通过（1496 passed）。
- 状态边界：V2-100B 只实现返工事件、runtime boundary（运行时边界）、GraphPatchReviewGate 和 ReworkReducer（返工归约器）。尚未实现 CEO provider-backed planner（项目经理模型支撑规划）、返工后 evidence/checker reintegration（证据/检查重接入）或 multi-round proving scenario（多轮证明场景）；V2-090F golden sample（黄金样例）仍保持 REVIEW_REQUIRED / BLOCKED。

## 2026-06-14 — V2-100C CEO rework planner boundary 完成

- 工作包：V2-100C（CEO rework planner boundary，CEO 返工规划边界）
- 当前状态：DONE。当前未完成工作包推进到 V2-100D（Rework evidence/checker reintegration，返工证据与检查重接入）。
- 关键产出：新增 `src/boardroom_os/rework/planner.py`、`src/boardroom_os/rework/ticket_graph_patch.py`、`src/boardroom_os/rework/reviewer.py`、`tests/rework/test_ceo_rework_planner.py`、`tests/rework/test_multi_role_graph_patch_reviews.py`、`tests/negative/test_ceo_rework_planner_fail_closed.py`、`tests/negative/test_multi_role_graph_patch_reviews_fail_closed.py`；扩展 `src/boardroom_os/rework/__init__.py` 和 `tests/execution/test_role_prompt_hooks_rework_governance.py`。
- 负例：缺 CEO ProviderAttempt（项目经理模型调用尝试记录）、fallback ProviderAttempt（降级模型调用尝试记录）、错误 RolePromptHook（角色提示词钩子）、未映射 blocker_refs（阻塞引用）、planner input 合同引用与 ReworkRequest（返工请求）不一致、`narrow_scope` 决策、旧 `AC-TINY-*` acceptance refs（验收引用）、prose-only CEO/reviewer output（仅散文输出）、错误审查 role/domain（角色/领域）和缺 checked invariants（已检查不变量）均 fail closed。
- 正例：090K failure snapshot（失败快照）的 probe response shape mismatch（探针响应形状错配）、env binding not converged（环境绑定未收敛）、final evidence old acceptance refs（最终证据旧验收引用）和 closeout old run refs（收尾旧运行引用）均可生成 CEO ReworkPlan（返工计划）、TicketGraphPatch（工单图补丁）、必需 GraphPatchReviewDomain（图补丁审查域）和 reducer-backed ReworkTicket commit（归约器支撑返工工单提交）。
- 验证：`PYTHONPATH=src:. python -m pytest tests/negative/test_ceo_rework_planner_fail_closed.py tests/negative/test_multi_role_graph_patch_reviews_fail_closed.py tests/rework/test_ceo_rework_planner.py tests/rework/test_multi_role_graph_patch_reviews.py tests/execution/test_role_prompt_hooks_rework_governance.py -q` 通过（31 passed）；`PYTHONPATH=src:. python -m pytest tests/rework/test_rework_model.py tests/rework/test_v2_090k_failure_snapshot_projection.py tests/reducers/test_rework_reducer.py tests/negative/test_rework_model_fail_closed.py tests/negative/test_rework_reducer_fail_closed.py -q` 通过（32 passed）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/closeout tests/rework tests/negative -q` 通过（1521 passed）。
- 状态边界：V2-100C 只实现 provider-backed planner/reviewer/patch proposal boundary（模型支撑规划/审查/补丁提议边界）。不执行 ReworkAttempt（返工尝试），不重建 SourceInventory（源码清单）、FinalEvidenceTable（最终证据表）或 CheckerVerdict（检查结论），不执行 CloseoutGate（收尾门禁）；这些仍属于 V2-100D/E。V2-090F golden sample（黄金样例）仍保持 REVIEW_REQUIRED / BLOCKED。

## 2026-06-14 — V2-100D Rework evidence/checker reintegration 完成

- 工作包：V2-100D（Rework evidence/checker reintegration，返工证据与检查重接入）
- 当前状态：DONE。当前未完成工作包推进到 V2-100E（Multi-round rework proving scenario，多轮返工证明场景）。
- 关键产出：新增 `src/boardroom_os/rework/evidence.py`、`tests/rework/fixtures/rework_evidence.py`、`tests/negative/test_rework_evidence_fail_closed.py`、`tests/negative/test_rework_closeout_fail_closed.py`、`tests/rework/test_rework_evidence_recheck.py` 和 `tests/rework/test_rework_closeout_fact_chain.py`；扩展 `src/boardroom_os/evidence/table.py`、`src/boardroom_os/rework/__init__.py`、`tests/evidence/test_final_evidence_table.py`；同步更新 `doc/04-implementation/backlog.md` 和 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：`ReworkEvidenceNamespace`（返工证据命名空间）为每轮 `run_id + cycle_id + rework_attempt_id + graph_version` 生成唯一 EvidenceNamespaceRef（证据命名空间引用）；`ReworkEvidenceRecheckInput`（返工证据重验输入）要求 active AcceptanceContract / PackageContract（活跃验收合同 / 包合同）、PackageAssembly（包装配）、SourceLineageRecord（源码来源链记录）、EvidenceVerificationResult（证据验证结果）、RunManifest（运行清单）和 environment usage（环境变量使用）均为真实 typed models（类型化模型）。`recheck_rework_attempt()` 重新构造 SourceInventory（源码清单）、FinalEvidenceTable（最终证据表）、CheckerVerdict（检查结论），并在提供 closeout context（收尾上下文）时调用 CloseoutGate（收尾门禁）。
- 负例：旧 FinalEvidenceTable、缺 ProviderAttempt、缺 source lineage、CheckerVerdict 早于当前证据验证、遗漏 BehavioralProbe failure（行为探针失败）、run command 缺 service contract、未声明 env usage、旧 RunManifest、旧 SourceInventory 和旧 CheckerVerdict 均 fail closed。
- 正例：当前轮 ReworkAttempt 可重建 SourceInventory / FinalEvidenceTable / CheckerVerdict / CloseoutGateResult（源码清单 / 最终证据表 / 检查结论 / 收尾门禁结果），且 ReworkOutcome.accepted_blocker_refs（返工结果已接受阻塞项引用）只来自 target_blocker_refs（目标阻塞项引用）。
- 验证：`PYTHONPATH=src:. python -m pytest tests/negative/test_rework_evidence_fail_closed.py tests/negative/test_rework_closeout_fail_closed.py tests/rework/test_rework_evidence_recheck.py tests/rework/test_rework_closeout_fact_chain.py -q` 通过（12 passed）；`PYTHONPATH=src:. python -m pytest tests/rework tests/reducers/test_rework_reducer.py tests/negative/test_rework_model_fail_closed.py tests/negative/test_rework_reducer_fail_closed.py tests/negative/test_ceo_rework_planner_fail_closed.py tests/negative/test_multi_role_graph_patch_reviews_fail_closed.py tests/negative/test_rework_evidence_fail_closed.py tests/negative/test_rework_closeout_fail_closed.py -q` 通过（68 passed）；`PYTHONPATH=src:. python -m pytest tests/evidence/test_final_evidence_table.py tests/evidence/test_checker_verdict.py tests/proving/test_source_inventory.py tests/workspace/test_run_manifest_behavioral_probe.py tests/workspace/test_run_manifest_service_contract.py tests/closeout/test_closeout_gate.py tests/negative/test_source_inventory_ref_only_rejected.py tests/negative/test_run_manifest_command_coverage.py tests/negative/test_missing_acceptance_map_blocks_closeout.py -q` 通过（141 passed）。
- 状态边界：V2-100D 不执行 provider、atomic-agent 或本地命令，只消费真实记录事实并调用既有权威组件；多轮 proving scenario（证明场景）、resettable failing fixture（可重置失败夹具）和 process audit/replay（流程审计/重放）导出仍属于 V2-100E。

## 2026-06-15 — V2-100E Multi-round rework proving scenario 完成

- 工作包：V2-100E（Multi-round rework proving scenario，多轮返工证明场景）
- 当前状态：DONE。Phase 10（Agent-team Rework Loop Hardening，智能体团队返工循环强化）5 / 5 完成；V2-090F golden sample（黄金样例）仍保持 REVIEW_REQUIRED / BLOCKED，等待 V2-100E 专家评审后复判。
- 关键产出：新增 `src/boardroom_os/proving/v2_100_rework_loop.py`、`src/boardroom_os/proving/v2_100_resettable_fixture.py`、`scripts/run_v2_100_rework_loop_scenario.py`、`tests/proving/test_v2_100_rework_loop.py`、`tests/proving/test_v2_100_rework_loop_provider_integration.py`、`tests/negative/test_v2_100_rework_loop_fail_closed.py` 和 `tests/proving/fixtures/v2_100_resettable_rework.py`；扩展 `src/boardroom_os/proving/__init__.py`；补入 `doc/04-implementation/v2-100e-multi-round-rework-proving-scenario-expert-review-plan.md` 并更新 `doc/04-implementation/INDEX.md`。
- 关键实现：`run_v2_100_rework_loop_scenario()`（V2-100 返工循环场景运行器）默认要求真实 provider（模型供应商），通过 YAML role/profile（角色/供应商配置档）解析 CEO/reviewer/worker（项目经理/审查者/实施者）轮次输出；`run_v2_100_rework_round()`（单轮返工运行器）在 reducer event（归约事件）、payload manifest（载荷清单）、ProviderAttempt manifest（模型调用尝试清单）和 ReworkEvidenceRecheckInput（返工证据重验输入）之间建立 fail-closed binding（失败关闭绑定）；audit export（审计导出）记录 events（事件）、payload refs（载荷引用）、provider attempt refs（模型调用尝试引用）、command evidence refs（命令证据引用）、SourceInventory hash（源码清单哈希）和 terminal decision（终态决策）。
- Negative tests：证明无 verified blocker（已验证阻塞项）、fake provider success（模拟供应商成功）、helper-written verdict（辅助器写结论）、旧 evidence/closeout ref（证据/收尾引用）复用、重绑定预制 accepted evidence（通过证据）为真实 worker evidence（实施者证据）、命令成功但证明命题错误、runtime 直接 accepted（运行时直接接受）和预算耗尽无 escalation/termination decision（升级/终止决策）均 fail closed。
- Happy path：090K curated failure snapshot（精选失败快照）可投影为 ReworkRequest（返工请求）；resettable failing fixture（可重置失败夹具）可稳定复现 response-shape mismatch（响应形状不匹配）、env binding mismatch（环境绑定不一致）、stale acceptance refs（陈旧验收引用）和 stale closeout run refs（陈旧收尾运行引用）；多轮返工经 CEO ReworkPlan（项目经理返工计划）、GraphPatchReview（图补丁审查）、ReworkReducer（返工归约器）、ReworkAttempt（返工尝试）、EvidenceVerifier / SourceInventory / FinalEvidenceTable / Checker / CloseoutGate（证据验证器 / 源码清单 / 最终证据表 / 检查者 / 收尾门禁）重验后 accepted（接受），或在预算耗尽时生成显式终止决策。
- 真实 provider 证明：使用主仓库 ignored env（忽略的环境文件）`.tmp/v2-100e-real-provider.env`，配置指向 `config/boardroom-runtime.v2-090f.yaml`、`config/boardroom-providers.v2-090f.yaml` 和 `config/boardroom-roles.v2-090f.yaml`，未提交密钥。CLI 运行 `scripts/run_v2_100_rework_loop_scenario.py --snapshot examples/generated-workspaces/tiny-fullstack/30-audit/v2-090k-failure-snapshot/failure-summary.json --env .tmp/v2-100e-real-provider.env --export-root examples/generated-workspaces/tiny-fullstack/20-evidence/v2-100e-rework-loop --max-rounds 2` 通过，terminal_status=`accepted`，CloseoutGate（收尾门禁）`passed`，审计导出位于 `examples/generated-workspaces/tiny-fullstack/20-evidence/v2-100e-rework-loop`。
- 复核加固：resettable / real-provider package（可重置/真实模型供应商项目包）不再写空 `package-contract.json` / `run-manifest.json`；audit export（审计导出）的 SourceInventory hash（源码清单哈希）必须与实际 package 文件 sha256 一致；accepted round（接受轮次）必须包含 passed CloseoutGateResult（通过收尾门禁结果）。
- 验证：V2-100 focused regression `111 passed, 1 skipped, 1 warning`；evidence/checker/closeout regression `180 passed`；V2-100E local suite `42 passed, 1 skipped, 1 warning`；真实 provider opt-in suite `BOARDROOM_RUN_REAL_PROVIDER_TESTS=1 ... tests/proving/test_v2_100_rework_loop_provider_integration.py -q -s` 通过（4 passed, 1 warning in 104.32s）；真实 CLI proof 通过；`git diff --check` 通过。
- 状态边界：V2-100E 证明“发现问题后团队可治理返工并可审计收敛或升级”，不等于 V2-090F golden sample（黄金样例）已经通过；下一步应等待专家评审并据此复判 V2-090F。

## 2026-06-16 — V2-090F rerun rework-entry validation spec

- 工作包：V2-090F（Golden sample rebuild，黄金样例重建）
- 当前状态：REVIEW_REQUIRED / BLOCKED。V2-100E 已完成后，V2-090F 下一步不再等待抽象评审，而是按新 spec 执行真实 rerun rework-entry validation（重跑返工入口验证）后复判。
- 关键产出：新增 `doc/04-implementation/v2-090f-rerun-rework-entry-validation-spec.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 和 `doc/05-project-log/decisions.md`。
- 方法论决策：新增 `DEC-0025`，确认先真实重跑 V2-090F 观察 gate failure（门禁失败）形态；无阻断则按原样 closeout candidate（收尾候选），有阻断则必须形成 verified blocker（已验证阻塞项）、ReworkRequest（返工请求）、TicketGraphPatch（工单图补丁）和 TicketGraph before/after（工单图更新前后）证据。只有真实重跑证明无法投影成 ReworkRequest 时，才允许补最小 rework-entry adapter（返工入口适配器）。
- Spec 重点：重跑仍以 single user PRD（单份用户需求文档）为唯一业务输入，使用 V2-090F 专用 runtime/providers/roles config（运行时 / 供应商 / 角色配置）；禁止先新增大规模 orchestration glue（编排胶水）、预制 ReworkPlan（返工计划）、固定 ticket graph（工单图）或复制 FinalEvidenceTableBuilder / SourceInventory / Checker / CloseoutGate（最终证据表构建器 / 源码清单 / 检查者 / 收尾门禁）规则。
- 自审结论：spec 已内置 placeholder scan（占位扫描）、scope check（范围检查）、Contract first（合同优先）、Reducer first（归约器优先）、Evidence first（证据优先）、Fail closed（失败关闭）、Runtime bounded（运行时有界）和 no second source of truth（无第二权威源）检查项。
- 状态边界：本轮只写入 spec 和治理文档，没有执行真实 provider V2-090F rerun（真实模型供应商重跑），没有生成新的 golden sample（黄金样例），没有勾选 Phase 9 V2-090F 验收项，也没有把 V2-090F 标记 DONE。

## 2026-06-16 — V2-090F rerun rework-entry validation implementation plan

- 工作包：V2-090F（Golden sample rebuild，黄金样例重建）
- 当前状态：REVIEW_REQUIRED / BLOCKED。已根据通过的 spec 写入实施计划，等待评审；本轮不实施代码、不执行真实 provider rerun（模型供应商重跑）、不生成新的 golden sample（黄金样例）。
- 关键产出：新增 `doc/04-implementation/v2-090f-rerun-rework-entry-validation-implementation-plan.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/backlog.md` 和 `doc/04-implementation/acceptance-criteria.md`。
- 计划口径：实施必须从 observation run（观察重跑）开始，先运行现有 V2-090F full stage（完整阶段）观察 closeout/gate failure（收尾/门禁失败）形态；若无阻断则导出 `passed_without_rework_candidate`（无返工通过候选），若有结构化阻断则投影为 ReworkRequest（返工请求）并调用 V2-100 continuation（返工继续），若只有 raw exception/free text（原始异常/自由文本）则终止为 `blocked_by_missing_rework_entry`（缺返工入口阻断）。
- 计划边界：新增模块只能作为 `v2_090f_rework_entry.py`（V2-090F 返工入口验证薄层）读取现有产物、导出 TicketGraph before/after（工单图更新前后）和调用 V2-100 API（接口）；不得复制 EvidenceVerifier / SourceInventory / FinalEvidenceTable / Checker / CloseoutGate（证据验证器 / 源码清单 / 最终证据表 / 检查者 / 收尾门禁），不得预制 ReworkPlan（返工计划）或固定 ticket graph（工单图）。
- 自审结论：计划覆盖 spec 的四类结果、TicketGraph graph_version（图版本）递增要求、after graph 绑定 TicketGraphPatch（工单图补丁）要求、真实 provider opt-in（显式启用）验证和 completion-only docs（仅完成后文档）边界；V2-090F checkbox 与 Phase 9 前置项保持未勾选。

## 2026-06-16 — V2-090F rerun rework-entry validation implementation result

- 工作包：V2-090F（Golden sample rebuild，黄金样例重建）
- 当前状态：REVIEW_REQUIRED / BLOCKED，保持不变。本轮实现 rework-entry validation（返工入口验证）薄层并执行真实 provider rerun（模型供应商重跑），结果为 `blocked_by_missing_rework_entry`（缺返工入口阻断），未更新 V2-090F DONE 状态，未勾选 Phase 9 验收项。
- 关键产出：新增 `src/boardroom_os/proving/v2_090f_rework_entry.py`、`tests/proving/test_v2_090f_rework_entry_validation.py` 和 `tests/negative/test_v2_090f_rework_entry_fail_closed.py`；扩展 `src/boardroom_os/proving/v2_100_rework_loop.py` 的 `run_v2_100_rework_loop_for_request()`（从已验证返工请求启动返工循环包装函数）；扩展 `scripts/run_v2_090f_prd_agent_team.py` 的 `--stage rework-entry`；更新 `src/boardroom_os/proving/__init__.py` 和相关 V2-090F/V2-100 测试。
- 真实观察：使用 V2-090F 专用 config（配置）和 `.env` 中真实 provider secret（供应商密钥）运行 `BOARDROOM_RUN_REAL_PROVIDER_PROVING=1 PYTHONPATH=src:. python scripts/run_v2_090f_prd_agent_team.py --prd examples/directives/tiny-fullstack-prd.md --reset --stage rework-entry`。运行生成真实 planning/worker artifacts（规划/实施产物），但在结构化 CloseoutGateResult（收尾门禁结果）前失败，raw error（原始错误）为 `ValueError: unsupported RunManifest behavior assertion type: json_array_contains_field`。
- 复判产物：`examples/generated-workspaces/tiny-fullstack/00-boardroom/ticket-graph.before-rework.json`、`examples/generated-workspaces/tiny-fullstack/00-boardroom/ticket-graph.before-rework.md`、`examples/generated-workspaces/tiny-fullstack/20-evidence/rework-entry/rework-terminal.json` 和 `examples/generated-workspaces/tiny-fullstack/30-audit/rework-entry-validation.md` 已导出；terminal status（终态）为 `blocked_by_missing_rework_entry`，未生成 ReworkRequest（返工请求）或 after graph（返工后图）。
- 结论边界：当前阻断不是 verified blocker（已验证阻断项），不能进入 V2-100 ReworkCycle（返工循环）或创建 TicketGraphPatch（工单图补丁）。后续只能按 spec 补最小 gate-failure adapter（门禁失败适配器）或修正 RunManifest behavior assertion（运行清单行为断言）结构化失败输出，且不得复制 evidence/checker/closeout（证据/检查/收尾）实现。
- 验证：`PYTHONPATH=src:. python -m pytest tests/proving/test_v2_090f_rework_entry_validation.py tests/negative/test_v2_090f_rework_entry_fail_closed.py tests/proving/test_v2_090f_prd_agent_team_script.py tests/proving/test_v2_100_rework_loop.py tests/negative/test_v2_100_rework_loop_fail_closed.py -q` 通过（92 passed）；graph evidence check（图证据检查）通过并确认无 after graph 时仍可验证 before graph 导出。

## 2026-06-16 — V2-100F RunManifest tolerant ingestion and autonomous blackbox spec

- 工作包：V2-100F（RunManifest tolerant ingestion and rework entry，运行清单宽容摄取与返工入口）
- 当前状态：REVIEW_REQUIRED。本轮只提升问题批次并写入 spec（规格），等待专家评审；未实施代码、未执行新的 provider rerun（模型供应商重跑）、未改变 V2-090F BLOCKED 状态。
- 触发原因：V2-090F 真实 rerun 的 `blocked_by_missing_rework_entry` 源于 RunManifest behavior assertion（运行清单行为断言）摄取阶段 raw crash（原始崩溃）：`unsupported RunManifest behavior assertion type: json_array_contains_field`。继续补单个 alias（别名）会陷入模型词汇随机性的无穷适配循环，不能支撑 agent team autonomy（智能体团队自治）。
- 关键产出：新增 `doc/04-implementation/v2-100f-manifest-tolerant-ingestion-rework-entry-spec.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 和 `doc/05-project-log/decisions.md`。
- 方法论决策：新增 `DEC-0026`（Proposed），提出 RunManifest 摄取层采用 tolerant semantic ingestion（宽容语义摄取）：未知或变体 assertion 不 raw crash、不被忽略、不允许 closeout passed（收尾通过），而是保留 raw payload（原始载荷）并进入 CEO-governed verify-blackbox hook（项目经理治理黑盒验证钩子），由 CEO 派工后的 AgentSeat（智能体席位）生成 provider-backed BlackboxVerificationPlan（模型支撑黑盒验证计划）。
- 评审修订：根据用户确认，V2-100F 改为完整自治测试路线：RunManifest（运行清单）只是上下文和运行承诺，不是唯一测试脚本；黑盒验证动作由 CEO 派工后的 AgentSeat 自主决定；runner（运行器）只执行 agent-owned plan（智能体拥有的计划）并记录 command / HTTP / browser / tool facts（命令 / HTTP / 浏览器 / 工具事实）；不新增 standalone ManifestInterpreter（独立运行清单解释器）。
- 验收边界：V2-100F 不放宽完成门槛，不新增第二套 EvidenceVerifier / FinalEvidenceTableBuilder / Checker / CloseoutGate（证据验证器 / 最终证据表构建器 / 检查者 / 收尾门禁），不直接 patch generated sample（生成样例工程）。目标是把 raw exception 转成 agent-owned verification（智能体拥有的验证）和可返工事实。
- 文档状态：Phase 10 进度从 5 / 5 调整为 5 / 6；新增 AC-V2-REWORK-006 并保持未勾选；V2-100F implementation plan（实施计划）已于 2026-06-17 写入，代码实施必须等待计划评审。

## 2026-06-17 — V2-100F implementation plan 写入

- 工作包：V2-100F（RunManifest tolerant ingestion and rework entry，运行清单宽容摄取与返工入口）
- 当前状态：REVIEW_REQUIRED，保持不变。本轮只根据 spec 和专家问题制定 implementation plan（实施计划）；未实施代码、未执行 provider rerun（模型供应商重跑）、未勾选 AC-V2-REWORK-006，也未改变 V2-090F golden sample（黄金样例）的 REVIEW_REQUIRED / BLOCKED 状态。
- 关键产出：新增 `doc/04-implementation/v2-100f-manifest-tolerant-ingestion-rework-entry-implementation-plan.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/backlog.md` 和 `doc/05-project-log/decisions.md`。
- 已确认决策：`BlackboxVerificationPlan`（黑盒验证计划）替代 `RunManifest.behavioral_probes`（运行清单行为探针）的直接执行路径；`behavioral_probes` 保留为上下文和运行承诺。`verify-blackbox` ticket（黑盒验证工单）作为当前版本 RunManifest 后的 verification/rework hook（验证/返工钩子），未来可扩展到 roadmap milestone gate（路线图里程碑门禁）通过前。
- 治理边界：黑盒验证实施者不硬编码 Tester（测试者）。CEO（项目经理）通过 TicketGraph / SeatDemand（工单图 / 席位需求）决定被派工 AgentSeat（智能体席位），该 seat 产生 provider-backed BlackboxVerificationPlan（模型支撑黑盒验证计划）。Framework（框架）只能记录 hook request（钩子请求）和校验 plan/fact lineage（计划/事实来源链），不得绕过 CEO 创建工单或派工。
- 返工路由边界：计划采用 `RUN_MANIFEST_ERROR`（运行清单错误）作为新 active routing code（活跃路由代码），保留 `RUN_MANIFEST_MISMATCH`（运行清单不匹配）为 deprecated legacy code（弃用旧代码）。新增 `ReworkSuspectedDomain.MANIFEST`（运行清单疑似域）只能由 typed verifier/checker/gate（类型化验证/检查/门禁）或 provider-backed governance output（模型支撑治理输出）声明，framework 不得根据 raw text（原始文本）、异常消息或 manifest fragment（清单片段）自动判定域归属。
- 计划结构：分为 tolerant ingestion context（宽容摄取上下文）、BlackboxVerificationPlan formal model（黑盒验证计划正式模型）、CEO-governed verification hook（项目经理治理验证钩子）、runner approved-plan gate（运行器已批准计划门禁）、live blackbox evidence lineage（真实黑盒证据来源链）、rework routing（返工路由）、prompt/context wiring（提示词/上下文接线）、V2-090F rework-entry integration（返工入口集成）和 real provider opt-in（真实模型显式启用）验证。
- 自审结论：计划覆盖专家提出的五个问题；没有把 alias list（别名列表）、固定业务端点、框架自造探针、框架自判 suspected domain（疑似域）或 plan prose（计划散文）写成成功路径。

## 2026-06-18 — V2-100F implementation plan P0 boundary 修订

- 工作包：V2-100F（RunManifest tolerant ingestion and rework entry，运行清单宽容摄取与返工入口）
- 当前状态：REVIEW_REQUIRED，保持不变。本轮清理未评审 V2-100F 实现尝试并修订原 implementation plan（实施计划）；未实施新代码、未执行 provider rerun（模型供应商重跑）、未勾选 AC-V2-REWORK-006，也未改变 V2-090F golden sample（黄金样例）的 REVIEW_REQUIRED / BLOCKED 状态。
- 安全备份：清理前将当前 diff/status 保存到 `.tmp/v2-100f-abandoned.patch` 和 `.tmp/v2-100f-abandoned-status.txt`。随后按用户要求恢复 `examples/generated-workspaces/tiny-fullstack/30-audit/v2-090k-failure-snapshot/` 历史 fixture（夹具），用于保留 V2-100A~E historical regression（历史回归）输入。
- 关键修订：`doc/04-implementation/v2-100f-manifest-tolerant-ingestion-rework-entry-implementation-plan.md` 新增 P0 architectural boundary（架构边界）：V2-100F 可复用 V2-100 domain models / reducers / validators（领域模型 / 归约器 / 校验器），但不得复用 V2-100E proving scenario orchestration（证明场景编排）、`run_v2_100_rework_loop_for_request`、resettable fixture（可重置夹具）、minimal package stub（最小占位包）或 accepted audit shortcut（接受审计捷径）。
- Snapshot 边界：`v2-090k-failure-snapshot`（V2-090K 历史失败快照）仅可由 V2-100A~E historical regression（历史回归）读取；V2-100F active path（活跃路径）、fixtures（夹具）和 real-provider opt-in（真实模型显式启用）不得读取、复制、改写或通过该 snapshot 路由。
- Task 8 修订：原 V2-090F rework-entry integration（返工入口集成）改为 V2-100F governed orchestration（治理编排）：raw RunManifest context（原始运行清单上下文）→ CEO provider output 创建 `verify-blackbox` ticket patch（黑盒验证工单补丁）→ TicketGraph reducer/projection（工单图归约器/投影）进入 ready queue（就绪队列）→ graph-assigned AgentSeat（工单图派工席位）产出 provider-backed BlackboxVerificationPlan（模型支撑黑盒验证计划）→ runner 记录真实 facts（事实）→ EvidenceVerifier / Checker / CloseoutGate（证据验证器 / 检查者 / 收尾门禁）判定 `passed` / `rework_required` / `blocked_or_escalated`。
- 新增验收门禁：计划要求新增 forbidden dependency（禁止依赖）、graph mutation（图变更）、accepted blocker alignment（接受阻塞对齐）和 worker stub / `changed_files: []` fail-closed（失败关闭）负例；真实 provider opt-in 不能只证明“不 raw crash”，还必须证明 verify-blackbox ticket graph projection（工单图投影）、BlackboxVerificationPlan、BlackboxActionExecutionFact（黑盒动作执行事实）和 evidence/checker/closeout 链路。

## 2026-06-21 — V2-100F implementation plan 原生编排重写

- 工作包：V2-100F（RunManifest tolerant ingestion and native rework orchestration，运行清单宽容摄取与原生返工编排）
- 当前状态：REVIEW_REQUIRED，保持不变。本轮只按用户要求丢弃原 implementation plan（实施计划）并重写；未实施代码、未执行 provider rerun（模型供应商重跑）、未勾选 AC-V2-REWORK-006，也未改变 V2-090F golden sample（黄金样例）的 REVIEW_REQUIRED / BLOCKED 状态。
- 重写判断：旧 plan 虽然加入 P0 architectural boundary（架构边界），但主线仍容易把 `verify-blackbox`（黑盒验证）做成 RunManifest 摄取后的 helper/hook（辅助器/钩子）外接路径。正确顺序应先把返工流程纳入 native orchestration（原生编排），再验证返工能力，而不是先做一个可选验证入口让测试通过。
- 关键产出：整体替换 `doc/04-implementation/v2-100f-manifest-tolerant-ingestion-rework-entry-implementation-plan.md`；同步更新 `doc/04-implementation/INDEX.md`、`doc/04-implementation/backlog.md` 和 `doc/05-project-log/decisions.md`。
- 新计划结构：拆为 V2-100F-A~F：tolerant manifest context（宽容运行清单上下文）、first-class verify-blackbox ticket（一等黑盒验证工单）、provider-backed BlackboxVerificationPlan（模型支撑黑盒验证计划）、authoritative runner facts（权威运行事实）、rework routing/evidence reintegration（返工路由与证据重接入）、real provider end-to-end proof（真实模型供应商端到端证明）。
- 原生编排门槛：`verify-blackbox` 必须由 `TICKET_CREATED` / `SEAT_ASSIGNED`（工单创建 / 席位派工事件）进入 TicketGraphProjector / SeatAssignmentProjector / ExecutionPackageCompiler（工单图投影器 / 席位派工投影器 / 执行包编译器），BlackboxVerificationPlan 必须绑定 graph-assigned AgentSeat、ExecutionPackage 和 ProviderAttempt（图派工席位 / 执行包 / 模型调用尝试记录），runner 只执行 approved plan actions（已批准计划动作），EvidenceVerifier / Checker / CloseoutGate / ReworkReducer（证据验证器 / 检查者 / 收尾门禁 / 返工归约器）是唯一判定链。
- 真实 provider 配置路径：计划明确 completion proof（完成证明）必须使用 `config/boardroom-runtime.v2-090f.yaml`、`config/boardroom-providers.v2-090f.yaml`、`config/boardroom-roles.v2-090f.yaml`；secret（密钥）仅来自 `.env` 或 gitignored `.tmp/v2-100f-real-provider.env`，不得写入 tracked docs（受版本控制文档）。

## 2026-06-21 — V2-100F-B verify-blackbox 原生工单实施

- 工作包：V2-100F-B（Verify-blackbox As First-class Ticket，一等黑盒验证工单）
- 当前状态：V2-100F 整体 `IN_PROGRESS`。本轮只完成 B 子批次；V2-100F-C~F 尚未实施，未执行真实 provider proof（模型供应商证明），未勾选 AC-V2-REWORK-006，也未改变 V2-090F golden sample（黄金样例）的 REVIEW_REQUIRED / BLOCKED 状态。
- 关键产出：新增 `src/boardroom_os/orchestration/verification.py` 和 `src/boardroom_os/orchestration/__init__.py`；新增 `tests/orchestration/test_verify_blackbox_native_ticket.py` 与 `tests/negative/test_verify_blackbox_orchestration_fail_closed.py`；扩展 `src/boardroom_os/execution/compiler.py` 的 `VerificationExecutionContext`（验证执行上下文）和 `verify-blackbox` 编译路径；调整 `src/boardroom_os/graph/ticket.py` 与 `src/boardroom_os/execution/package.py`，仅允许 `verify-blackbox` 工单 / 执行包空写集，普通可写任务仍要求 `allowed_write_set`。
- 边界结论：`VerifyBlackboxTicketIntent`（黑盒验证工单意图）返回普通 `TicketCreatedPayload`（工单创建载荷），不是自定义 ready queue item（就绪队列项）；`verify-blackbox` 必须经 `TicketGraphProjector -> SeatAssignmentProjector -> SeatAssignmentGraph.ready_queue -> ExecutionPackageCompiler`，缺 seat assignment（席位派工）、缺 verification context（验证上下文）、缺 raw manifest context（原始运行清单上下文）或不在 ready queue 均 fail closed。Review 修补后，`VerificationExecutionContext` 派生的 raw manifest / skeleton / contract / project doc / source surface / observed failure refs 必须全部属于 ticket `allowed_read_refs` 子集；`verify-blackbox` ticket 在 payload 和 compiler 层均强制 `RoleCategory.VERIFICATION` + `task.verify-blackbox` capability，且不允许扩大写集。
- 验证：`PYTHONPATH=src:. python -m pytest tests/orchestration/test_verify_blackbox_native_ticket.py tests/negative/test_verify_blackbox_orchestration_fail_closed.py tests/execution/test_execution_package_compiler.py tests/negative/test_execution_package_compiler_fail_closed.py -q` 通过（41 passed）；安全边界负例 `PYTHONPATH=src:. python -m pytest tests/negative/test_verify_blackbox_orchestration_fail_closed.py tests/negative/test_verify_blackbox_security_boundary.py -q` 通过（12 passed）；相关 graph / seat / execution package / atomic invocation 回归 `$env:PYTHONPATH='src;.;D:\Projects\atomic-agent\src'; python -m pytest tests/orchestration/test_verify_blackbox_native_ticket.py tests/negative/test_verify_blackbox_orchestration_fail_closed.py tests/negative/test_verify_blackbox_security_boundary.py tests/execution/test_execution_package_compiler.py tests/negative/test_execution_package_compiler_fail_closed.py tests/reducers/test_ticket_graph_projection.py tests/reducers/test_seat_assignment_projection.py tests/execution/test_execution_package_schema.py tests/negative/test_execution_package_fail_closed.py tests/execution/test_atomic_agent_invocation_compiler.py tests/negative/test_atomic_agent_integration_fail_closed.py -q` 通过（179 passed）；禁用路径搜索无匹配；`git diff --check` 通过。

## 2026-06-22 — V2-100F-C provider-backed 黑盒验证计划来源链

- 工作包：V2-100F-C（Provider-backed BlackboxVerificationPlan，模型支撑黑盒验证计划）
- 当前状态：V2-100F 整体 `IN_PROGRESS`。本轮只完成 C 子批次；V2-100F-D~F 尚未实施，未执行真实 provider proof（模型供应商证明），未勾选 AC-V2-REWORK-006，也未改变 V2-090F golden sample（黄金样例）的 REVIEW_REQUIRED / BLOCKED 状态。
- 关键产出：新增 `src/boardroom_os/evidence/blackbox_plan.py`，定义 `BlackboxVerificationPlan`（黑盒验证计划）、`BlackboxPlanAction`（黑盒计划动作）、`BlackboxPlanApproval`（黑盒计划批准）和 plan / approval lineage validators（计划 / 批准来源链校验器）；更新 `src/boardroom_os/evidence/__init__.py` 导出公开 API；新增 `tests/evidence/test_blackbox_verification_plan.py` 与 `tests/negative/test_blackbox_plan_fail_closed.py`。
- 边界结论：BlackboxVerificationPlan 只能绑定 graph-assigned AgentSeat（图派工智能体席位）的 `ExecutionPackage`（执行包）和 succeeded primary `ProviderAttempt`（成功主模型调用尝试记录）。BlackboxPlanApproval 只批准计划内 action 可执行，不代表 evidence passed（证据通过）或 closeout ready（收尾就绪）。缺 ProviderAttempt、错 seat、fallback attempt、stale package、stale role hook、越界 acceptance/context/action ref、计划外 approved action、缺 action expected observations（预期观察）或计划 / 批准内 `passed` / `approved` / `closeout_ready` 成功声明均 fail closed。现有 `ProviderAttempt` / `ExecutionPackage` / `EvidenceVerifier` 已具备 C 所需来源字段和 hook 快照检查，因此本轮未为“碰文件”改写 provider executor、work product 或 verifier routing。
- 验证：先运行新增测试得到 RED（缺 `boardroom_os.evidence.blackbox_plan` 模块）；实现后 `$env:PYTHONPATH='src;.'; python -m pytest tests/evidence/test_blackbox_verification_plan.py tests/negative/test_blackbox_plan_fail_closed.py -q` 通过（20 passed）；C 聚焦验证 `$env:PYTHONPATH='src;.'; python -m pytest tests/evidence/test_blackbox_verification_plan.py tests/negative/test_blackbox_plan_fail_closed.py tests/evidence/test_evidence_verifier.py tests/negative/test_synthetic_evidence_rejected.py -q` 通过（66 passed）。
