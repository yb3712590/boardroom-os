# Project Log: 2026-05

## 2026-05-13

- 确认 V2 采用近似全新项目路线。
- 旧代码默认作为历史归档教训，不作为 V2 实现依据。
- 建立文档基座、legacy boundary、AI/code/test conventions、PRD、建设方案、架构草案、roadmap/backlog/acceptance criteria。
- 第一个 proving scenario 定为 tiny anonymous book availability tracker。

## 2026-05-14

- 确认 V2-000 clean foundation 已完成：根入口、doc 全目录、实现占位目录、legacy boundary、roadmap/backlog/acceptance criteria 均已存在。
- 将 `doc/04-implementation/backlog.md` 调整为后续 AI 启动入口，顶部维护 TL;DR、当前任务文件、当前验收文件、V2-000 完成依据、进度总览和当前约束摘要。
- 根据项目复杂度，将 backlog 从 8 个粗阶段扩展为 49 个可执行工作包，显式覆盖 role/seat/provider 接入链、ExecutionPackage、ProviderAttempt、runtime 边界、evidence、workspace、closeout、replay 和 tiny proving scenario。

### 独立审计与修复（同日，对应 DEC-0008）

- 完成对 backlog 首版的独立架构审计。识别 4 项阻塞缺口：(1) `BoardDirective` 工作包缺位；(2) `MethodologyProfile` 工作包缺位；(3) `acceptance-criteria.md` 没有分批 checkbox 和产出清单，人类无法做批次验收；(4) V2-070C 没有把 `30-audit/` 10 项产物拆为逐项 negative test。另识别 1 项幂等性缺口：每次会话启动提示词接近，但 backlog 缺少可重复执行的"完成后更新协议"，跨会话状态会发散。
- 修复动作（同日完成）：
  - `backlog.md` 新增"实施幂等性 / 工作包完成更新协议"段：定义 Pre-flight 一致性检查、6 项完成更新清单、幂等保证、里程碑 vs 工作包区分。
  - `backlog.md` 插入 V2-010B（BoardDirective intake）和 V2-010F（MethodologyProfile）；原 V2-010B/C/D/E 顺延为 V2-010C/D/E/G。
  - `backlog.md` 同步更新跨阶段依赖：V2-020A 依赖 V2-010E（原 V2-010D）；V2-060A 依赖 V2-010D（原 V2-010C）；V2-080A 依赖 V2-010G（原 V2-010E）。
  - `backlog.md` 进度总览 Phase 1 由 5 改为 7，总数由 49 改为 51；统一状态枚举（V2-000 完成情况表 `NOT STARTED` → `TODO`）。
  - `backlog.md` 扩展 V2-070C，把 `30-audit/` 10 项产物显式列入逐项 negative test。
  - `acceptance-criteria.md` 顶部加双层结构说明；末尾新增 9 段"分批验收"（Phase 0 ~ Phase 8），每段包含 AC checkbox（绑定到 AC-V2-XXX 抽象 AC + 具体 negative / happy test）、本批产出清单和进入下一 Phase 前置。
  - `decisions.md` 新增 DEC-0008 记录上述结构性变更。
- 当前未完成工作包仍为 V2-001A；本次修复属于 V2-001B 工作包计划审计的产物。下一步等待用户对修复结果的确认（V2-001C），通过后进入 V2-010A。

### Foundation 清理与 SESSION_PROMPT 留档（同日）

- 删除一次性产物 `MANIFEST.md`（首次提交清单，提交 `aa65ac0` 已完成）和 `TREE.md`（目录快照，权威源为 `git ls-files`）。两文件本次修复后均已 stale 且无外部引用。
- 新增 `SESSION_PROMPT.md`（仓库根）：压缩版每轮启动提示词，删去阶段顺序列表 / 测试策略细节 / 文档同步规则（这些已由 backlog.md / 各工作包内嵌负例 / 幂等更新协议承载），保留分支锚点、6 条硬约束、forensic 例外、worktree 收尾和中文沟通约束。
- 同步更新 `doc/04-implementation/phase-0-plan.md` 根目录文件清单：移除已删除文件，加入 `SESSION_PROMPT.md`。

### Phase 0 冻结（同日）

- 用户在审阅 commit `578c7a8`（完善 V2 工作包、分批验收与幂等协议）后明确指示推进工作包状态。V2-001A / V2-001B / V2-001C 全部翻 DONE，里程碑 V2-001 翻 DONE。
- backlog.md TL;DR "当前未完成工作包" 由 V2-001A 推进到 V2-010A；进度总览 Phase 0 由 1/4（进行中）变 4/4（完成）；合计 4/51；项目状态由 "foundation-only" 变 "Phase 0 完成 / Phase 1 待启动"。
- acceptance-criteria.md Phase 0 验收段全部 checkbox 勾选，"进入 Phase 1 前置" 四项全部满足。
- 下一步：从 V2-010A（建立 contracts 包和基础值对象）开始 Phase 1 Contract Kernel 实施。

### V2-010A Contract Kernel 起步（同日）

- 完成 V2-010A：新增 `src/boardroom_os/contracts/__init__.py`、`src/boardroom_os/contracts/types.py`、`tests/contracts/test_contract_types.py`。
- 关键实现：合同基础值对象采用 Pydantic model（Pydantic 模型）和冻结配置，覆盖 `ContractId`、`AcceptanceRef`、`AcceptanceRefSet`、`SourceSurfaceRef`、`EvidenceObligationRef`、`ContractStatus`。
- Negative tests：空 ID、空 acceptance refs、未知状态均 fail closed。
- Happy path：合法 ID、active 状态和 acceptance refs 可构造并序列化。
- 验证证据：`PYTHONPATH=src pytest tests/contracts/test_contract_types.py` 通过（4 passed）。下一工作包推进到 V2-010B。

### V2-010B BoardDirective intake（同日）

- 完成 V2-010B：新增 `src/boardroom_os/contracts/directive.py`、`src/boardroom_os/contracts/project.py`、`tests/contracts/test_board_directive.py`、`tests/negative/test_board_directive_fail_closed.py`，并更新 `src/boardroom_os/contracts/__init__.py` 导出入口。
- 关键实现：`BoardDirective`（董事会指令）支持 `natural_language` / `prd_file` / `human_review_update` 三种来源；`DirectiveRegistry`（指令注册表）提供显式存在性校验；`ProjectCharter`（项目章程）采用完整最小字段并通过 `create_project_charter` 要求 `board_directive_ref` 指向已注册 directive。
- Negative tests：缺 `source_type` / `content_ref` / `received_at` / `requester_ref`、未知 `source_type`、无时区 `received_at`、未知字段、ProjectCharter 公开构造缺 directive registry、ProjectCharter 引用不存在 BoardDirective 均 fail closed。
- Happy path：三种 BoardDirective 来源均可构造、序列化并被 ProjectCharter 引用；contracts 包入口导出新对象。
- 验证证据：`PYTHONPATH=src pytest tests/contracts/test_board_directive.py tests/negative/test_board_directive_fail_closed.py` 通过（15 passed）；`PYTHONPATH=src pytest tests/contracts tests/negative` 通过（19 passed）。下一工作包推进到 V2-010C。

### V2-010C ProjectCharter 与 AcceptanceContract（同日）

- 完成 V2-010C：新增 `src/boardroom_os/contracts/acceptance.py`、`tests/contracts/test_acceptance_contract.py`、`tests/negative/test_acceptance_contract_fail_closed.py`，并扩展 `src/boardroom_os/contracts/project.py` 与 `src/boardroom_os/contracts/__init__.py`。
- 关键实现：`ProjectCharterRegistry`（项目章程注册表）提供 ProjectCharter 存在性校验；`AcceptanceContract`（验收合同）必须绑定已注册 ProjectCharter，携带 `ContractStatus`（合同状态）和动态 `AcceptanceCriterion`（验收项）；只有 active 合同暴露 blocking criteria（阻塞验收项）。
- Negative tests：空 criteria、非 blocking criteria 全覆盖、criterion 缺 `evidence_required`、criterion 缺 `source_surface_refs`、直接构造 AcceptanceContract 缺 project charter registry、未知 ProjectCharter 引用、ProjectCharter 缺 `board_directive_ref` 均 fail closed。
- Happy path：动态 criteria 可绑定 ProjectCharter 并序列化；active AcceptanceContract 仅返回 blocking criteria；draft AcceptanceContract 不向 gate consumers 暴露 blocking criteria；contracts 包入口导出新对象。
- 验证证据：`PYTHONPATH=src pytest tests/negative/test_acceptance_contract_fail_closed.py` 通过（7 passed）；`PYTHONPATH=src pytest tests/contracts/test_acceptance_contract.py` 通过（4 passed）；`PYTHONPATH=src pytest tests/contracts tests/negative` 通过（30 passed）。下一工作包推进到 V2-010D。

### V2-010D PackageContract 与 SourceSurface（同日）

- 完成 V2-010D：新增 `src/boardroom_os/contracts/package.py`、`src/boardroom_os/contracts/source_surface.py`、`tests/contracts/test_package_contract.py`、`tests/negative/test_package_contract_fail_closed.py`，并扩展 `src/boardroom_os/contracts/__init__.py` 与基础 value object（值对象）未知字段校验。
- 关键实现：`PackageContract`（包合同）采用最小 `PackageProjectType`（包项目类型）枚举 `software` / `documentation` / `mixed`；`software` 与 `mixed` 必须声明 run/test commands（运行/测试命令）；`SourceSurface`（源码实现面）必须携带 paths（路径）和 acceptance_refs（验收引用）；合同值对象与包模型均拒绝 unknown fields（未知字段），布尔字段采用 strict bool（严格布尔）。
- Negative tests：缺 `package_root`、缺 `source_surfaces`、software/mixed package 缺 run commands、software/mixed package 缺 test commands、SourceSurface 缺 acceptance_refs、SourceSurface 缺 paths、PackageContract / PackageCommand unknown fields、非严格 bool 均 fail closed。
- Happy path：tiny full-stack PackageContract 可声明 backend、frontend、tests、docs、run manifest surfaces，并保留 run/test commands 与 integration boundaries（集成边界）；documentation package 可无 run/test commands。
- 验证证据：`PYTHONPATH=src pytest tests/contracts/test_package_contract.py tests/negative/test_package_contract_fail_closed.py -v` 通过（15 passed）；`PYTHONPATH=src pytest tests/contracts tests/negative -v` 通过（46 passed）。下一工作包推进到 V2-010E。

### V2-010E EvidenceObligation 与 contract gate（同日）

- 工作包：V2-010E
- 状态：DONE
- 关键产出：`src/boardroom_os/contracts/evidence_obligation.py`、`src/boardroom_os/contracts/gates.py`、`tests/contracts/test_evidence_obligation.py`、`tests/negative/test_contract_gate.py`
- Negative tests：inactive AcceptanceContract（验收合同）、缺 PackageContract（包合同）、acceptance_ref 不属于 active contract、blocking criterion 无 blocking obligation、显式传入的 obligation set 与 active contract 不一致、criterion 引用不存在 SourceSurface（源码实现面）均 fail closed。
- Happy path：active AcceptanceContract + PackageContract 生成完整 EvidenceObligation（证据义务）集合，按 blocking criterion × evidence_required 展开，并返回 ContractGateResult（合同门禁结果）。
- 验证证据：`PYTHONPATH=src pytest tests/contracts/test_evidence_obligation.py tests/negative/test_contract_gate.py -q` 通过（10 passed）；`PYTHONPATH=src pytest tests/contracts tests/negative -q` 通过（56 passed）。

### V2-010F MethodologyProfile 与 workspace template 绑定（2026-05-15）

- 工作包：V2-010F
- 状态：DONE
- 关键产出：`src/boardroom_os/contracts/methodology.py`、`tests/contracts/test_methodology_profile.py`、`tests/negative/test_methodology_profile_fail_closed.py`；`PackageContract`（包合同）新增 `methodology_profile_ref`、`docs_template_key` 与 `documentation_obligations` 绑定。
- Negative tests：缺 `template_kind` / `documentation_density`、未知模板、template/density 冲突、docs_required 缺 methodology profile 绑定、缺 registry context、文档义务为空、docs_required=False 却存在 required docs、PackageContract 文档义务 / template key / project charter 与 MethodologyProfile 不一致、重复或非法 MethodologyProfileRegistry 均 fail closed。
- Happy path：Minimal / Agile / Compliance / Hybrid 四种 template_kind（模板类型）均可构造 MethodologyProfile（方法论配置）并绑定 PackageContract（包合同），且导出 V2-060 可消费的 `docs_template_key`（文档模板键）与 `documentation_obligations`（文档义务）。
- 验证证据：`PYTHONPATH=src pytest tests/contracts/test_methodology_profile.py tests/negative/test_methodology_profile_fail_closed.py -q` 通过（26 passed）；`PYTHONPATH=src pytest tests/contracts tests/negative -q` 通过（82 passed）。

### V2-010G 合同 fixture 与 schema 文档化（2026-05-15）

- 工作包：V2-010G
- 状态：DONE
- 关键产出：`tests/fixtures/contracts/tiny_fullstack_contract.py`、`tests/contracts/test_tiny_fullstack_contract_fixture.py`、`tests/negative/test_tiny_fullstack_contract_fixture_fail_closed.py`；Phase 1 Contract Kernel（合同内核）七个工作包全部完成。
- Negative tests：fixture 缺任一 blocking acceptance_ref（阻塞验收引用）的 AC-V2 显式绑定、绑定未知 AC-V2 抽象原则、或将 fallback implementation evidence（降级实施证据）纳入 fixture 均 fail closed。
- Happy path：TinyFullstackContractFixture（微型全栈合同夹具）构造完整 BoardDirective（董事会指令）→ ProjectCharter（项目章程）→ MethodologyProfile（方法论配置）→ AcceptanceContract（验收合同）→ PackageContract（包合同）→ ContractGateResult（合同门禁结果）链路，并声明 backend、frontend、persistence、tests、docs、run-manifest source surfaces（源码实现面）和 run/test commands（运行/测试命令）。
- 验证证据：`PYTHONPATH=src:. pytest tests/contracts/test_tiny_fullstack_contract_fixture.py tests/negative/test_tiny_fullstack_contract_fixture_fail_closed.py -q` 通过（6 passed）。

### V2-020A typed event record（2026-05-15）

- 工作包：V2-020A
- 状态：DONE
- 关键产出：`src/boardroom_os/events/record.py`、`src/boardroom_os/events/types.py`、`tests/reducers/test_event_record.py`。
- Negative tests：缺 actor_ref（参与者引用）、timestamp（时间戳）、graph_version（图版本）、payload_refs（载荷引用），以及空 actor、graph_version 非正数、空 payload refs、无时区 timestamp、未知 event_type（事件类型）均 fail closed。
- Happy path：EventRecord（事件记录）可稳定序列化，并保留 causation_refs（因果引用）与 correlation_refs（关联引用）。V2-020A 采用更窄事件范围，只覆盖 Phase 2 / runtime boundary（运行时边界）已明确需要的 ticket 与 execution 事实事件；后续阶段在实际消费时扩展自己的 EventType（事件类型），见 DEC-0010。
- Review 修复：补充 EventType 保持枚举类型的测试，避免后续 reducer（状态归约器）按字符串分支；补充 stable dump（稳定转储）round-trip 测试，确保转储结果可直接回放为 EventRecord。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/reducers/test_event_record.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.events.record'`）；review 修复前新增 round-trip / enum 测试得到预期 RED（3 failed, 9 passed）；修复后同命令通过（12 passed）。

### V2-020B in-memory event log（2026-05-15）

- 工作包：V2-020B
- 状态：DONE
- 关键产出：`src/boardroom_os/events/log.py`、`tests/reducers/test_event_log.py`。
- Negative tests：同一 project_ref（项目引用）内 graph_version（图版本）回退必须失败；重复 event_id（事件 ID）必须失败；通过 `EventRecord.model_construct` 不安全构造出的未知 event_type（事件类型）必须在 append（追加）阶段失败。
- Happy path：不同 project_ref 可各自从 graph_version 1 开始；append 后可按 project_ref、from_graph_version 和 to_graph_version 读取稳定版本范围内的事件。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/reducers/test_event_log.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.events.log'`）；新增 read（读取）测试后得到预期 RED（`AttributeError: 'InMemoryEventLog' object has no attribute 'read'`）；最终 `PYTHONPATH=src pytest tests/reducers/test_event_record.py tests/reducers/test_event_log.py -q` 通过（17 passed）。

### V2-020C TicketGraph projection（2026-05-15）

- 工作包：V2-020C
- 状态：DONE
- 关键产出：`src/boardroom_os/graph/ticket.py`、`src/boardroom_os/graph/projection.py`、`tests/reducers/test_ticket_graph_projection.py`。
- Negative tests：缺 `acceptance_refs`、`source_surface_refs`、`evidence_obligations`、`allowed_write_set` 的 ticket payload 必须失败；`TicketNode`（任务节点）直接构造也必须执行同等校验；无法解析 `payload_ref`、重复 ticket、unknown dependency（未知依赖）、unsupported ticket event（未支持任务事件）、`TICKET_CREATED` 携带 completed 状态均 fail closed。
- Happy path：合法 ticket 可进入 ready_queue（就绪队列）；blocked ticket 不可执行；依赖完成后 dependent ticket（依赖方任务）进入 ready_queue；`TICKET_CREATED` / `TICKET_BLOCKED` 经 `TicketPayloadResolver`（任务载荷解析器）可投影为当前 TicketGraph（任务图）；non-ticket event（非任务事件）不改变 graph。
- 验证证据：`PYTHONPATH=src pytest tests/reducers/test_event_record.py tests/reducers/test_event_log.py tests/reducers/test_ticket_graph_projection.py -q` 通过（77 passed）；`PYTHONPATH=src:. pytest tests/contracts tests/reducers tests/negative -q` 通过（167 passed）。

### V2-020D reducer 状态转换（2026-05-16）

- 工作包：V2-020D
- 状态：DONE
- 关键产出：`src/boardroom_os/reducers/ticket_reducer.py`、`src/boardroom_os/reducers/errors.py`、`tests/reducers/test_ticket_reducer_transitions.py`、`tests/negative/test_executor_cannot_complete_ticket.py`。
- Negative tests：executor/runtime actor（执行器/运行时参与者）提交 `TICKET_COMPLETED`、provider_attempt_count 为 0、completion snapshot 自带 blocker、历史 checker blocker 未清除、无 checker blocker 发起 rework 均 fail closed。
- Happy path：lease/work submitted 事件只记录生命周期事实不完成 ticket；checker blocker 可触发 rework blocked projection；reworked ticket 经后续 checker approved 可恢复 READY 并完成；provider attempt recorded + evidence complete + checker approved + no blocking issues 的 completion snapshot（完成快照）可使 reducer 产生 completed projection（完成投影）。
- Review 修复：补充跨事件状态测试，防止历史 checker blocker 被 completion snapshot 绕过；补齐 rework recovery path（返工恢复路径），approved check 会解除 BLOCKED 状态。
- 后续继承：V2-020D 只固化 reducer completion boundary（完成边界）作为稳定抽象，不提前实现 V2-050 的 FinalEvidenceTable（最终证据表）或 CheckerVerdict（检查结论）；V2-050F 应适配正式 evidence/checker 模型到该边界而非替换边界。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/reducers/test_ticket_reducer_transitions.py tests/negative/test_executor_cannot_complete_ticket.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.reducers.errors'`）；补充 lifecycle tests 后得到预期 RED（缺 `TicketCheckSnapshot`）；review 修复新增跨事件测试得到预期 RED（2 failed, 9 passed）；修复后 `PYTHONPATH=src pytest tests/reducers/test_ticket_reducer_transitions.py tests/negative/test_executor_cannot_complete_ticket.py -q` 通过（12 passed）；`PYTHONPATH=src pytest tests/reducers/test_event_record.py tests/reducers/test_event_log.py tests/reducers/test_ticket_graph_projection.py tests/reducers/test_ticket_reducer_transitions.py tests/negative/test_executor_cannot_complete_ticket.py -q` 通过（89 passed）；`PYTHONPATH="src;." pytest tests/contracts tests/reducers tests/negative -q` 通过（179 passed）。

### V2-020E seat assignment 事件入口（2026-05-16）

- 工作包：V2-020E
- 状态：DONE
- 关键产出：`src/boardroom_os/graph/seat_assignment.py`、`tests/reducers/test_seat_assignment_projection.py`，并扩展 `EventType.SEAT_ASSIGNED`（席位分配事件类型）。
- Negative tests：ticket 缺 `owner_seat_ref`、缺 `SEAT_ASSIGNED`（席位分配）事件、`SEAT_ASSIGNED` 早于 `TICKET_CREATED`（任务创建事件）、`SEAT_ASSIGNED` 多 payload_refs（载荷引用）、assignment 引用未知 ticket、SeatDefinition（席位定义快照）缺 `model_execution_profile_ref`（模型执行配置引用）、未知 seat、inactive seat、seat 声明 capability（能力）与 assignment `required_capability_tags` 不一致、ticket owner 与 assignment seat 不一致均 fail closed 或保持 blocked。
- Happy path：CEO / Architect / Worker / Checker seat（席位）可分别分配到治理、架构、实施和验证 ticket（任务），并进入 ready_queue（就绪队列）；happy path 事件可进入 InMemoryEventLog（内存事件日志），避免重复 event_id 与 event log 语义冲突。
- 边界说明：V2-020E 只在 graph（任务图）层表达 seat assignment（席位分配）事实和阻塞原因，不提前创建 V2-030 agents（智能体）包，不调用 provider（模型供应商）；capability 校验的可信输入限于 assignment `required_capability_tags`，ticket type（任务类型）可信来源留给 V2-030/V2-080 扩展。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/reducers/test_seat_assignment_projection.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.graph.seat_assignment'`）；补充缺 SEAT_ASSIGNED 事件测试得到预期 RED（ready_queue 非空）；审计修复前补充 early assignment / unknown ticket / multi payload_refs 得到预期 RED（缺 `SeatAssignmentProjectionError`）；实现后同命令通过（11 passed）。

### V2-020F projection replay（2026-05-16）

- 工作包：V2-020F
- 状态：DONE
- 关键产出：`src/boardroom_os/graph/replay.py`、`tests/reducers/test_projection_replay.py`。
- Negative tests：空事件历史、graph_version 缺失、事件输入乱序、project mismatch（项目不匹配）、non-positive expected_graph_version（非正预期图版本）、from_graph_version 中段重放缺 snapshot/base projection contract（快照/基准投影合同）、projection version mismatch（投影版本不匹配）以及 unresolved payload ref（未解析载荷引用）均 fail closed。
- Happy path：ProjectionReplay（投影重放器）可从 event log（事件日志）重建 audit replay（审计重放）范围内的 `seat_assignment_graph` projection summary（席位分配图投影摘要），并生成 deterministic `summary_hash`（确定性摘要哈希）；等价 summary payload 的字段顺序变化不会改变 hash。
- 边界说明：当前只覆盖 audit replay（审计重放），证明 runtime（运行时）内存状态不是事实源；V2-070 再扩展 branchable governance replay（可分叉治理重放），本阶段不提前实现分叉治理能力。
- 验证证据：`PYTHONPATH=src pytest tests/reducers/test_projection_replay.py -q` 通过（11 passed）；`PYTHONPATH=src pytest tests/reducers/test_event_record.py tests/reducers/test_event_log.py tests/reducers/test_ticket_graph_projection.py tests/reducers/test_ticket_reducer_transitions.py tests/reducers/test_seat_assignment_projection.py tests/reducers/test_projection_replay.py tests/negative/test_executor_cannot_complete_ticket.py -q` 通过（111 passed）。

### Phase 2 审计延期项归档（2026-05-16）

- 工作包：跨 V2-020 / V2-030 / V2-040 / V2-050 / V2-070 的审计接续记录，不改变 V2-020A~F DONE 状态。
- 关键产出：`doc/05-project-log/decisions.md` 新增 DEC-0011；`doc/04-implementation/backlog.md` 将 seat lifecycle 事件流、role-aware completion boundary、capability registry、正式 evidence/checker completion gate、event log hash chain / replay bundle、增量 closeout reducer 的 work product 历史要求绑定到对应后续工作包。
- 边界说明：Phase 2 只修补已可独立验证的 P2 kernel 缺口（依赖环 fail closed、completion 前必须存在 `WORK_PRODUCT_SUBMITTED` 事实）；需要 RoleProfile / AgentSeat / EvidenceVerifier / CloseoutPackage 支撑的能力不回填 Phase 2。

### V2-030A RoleProfile / SkillBinding / ModelExecutionProfile（2026-05-16）

- 工作包：V2-030A
- 状态：DONE
- 关键产出：`src/boardroom_os/agents/__init__.py`、`src/boardroom_os/agents/profiles.py`、`src/boardroom_os/agents/skills.py`、`tests/execution/test_agent_profiles.py`。
- 关键实现：新增 RoleProfile（角色模板）、SkillBinding（技能绑定）、ModelExecutionProfile（模型执行配置）和 CapabilityRegistry（能力标签注册表）；SkillBinding 首版纳入 `skill_file_ref`、`prompt_refs`、`mcp_interface_refs`，并新增 PromptSourceRegistry（提示词来源注册表）、SkillFileSourceRegistry（技能文件来源注册表）、McpInterfaceRegistry（MCP 接口注册表），支持 `version: 1` YAML-shaped（YAML 形状）人工配置模板解析。
- Negative tests：role 泄漏 provider 字段、role/skill 引用未知 capability tag、skill 绑定未知 role、skill 引用未知 skill file / prompt / MCP interface、skill 缺 MCP interface 所需 capability、model profile 缺 provider/model 或携带 api_key 均 fail closed。
- Happy path：分层命名 capability tags 经 registry 校验；人工配置模板可编译为 role/skill/model registries；同一 RoleProfile 可被多个 ModelExecutionProfile 支撑，供后续 AgentSeat policy 消费。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/execution/test_agent_profiles.py -q` 得到预期 RED（缺 `boardroom_os.agents.profiles`）；补充 YAML-shaped 配置测试后得到预期 RED（拒绝 `version` 与裸字符串 refs）；实现后 `PYTHONPATH=src pytest tests/execution/test_agent_profiles.py -q` 通过（10 passed）；`PYTHONPATH="src;." pytest tests/execution/test_agent_profiles.py tests/reducers/test_seat_assignment_projection.py tests/contracts tests/negative -q` 通过（112 passed）。

### V2-060F agent asset import 规划追加（2026-05-16）

- 决策：新增 DEC-0012，明确 ExecutionPackage compiler（执行包编译器）必须保持 0 外部文件输入；外部 role config / skill file / prompt file / MCP interface manifest 的同步放到 V2-060F workspace/package 阶段。
- 关键产出：`doc/04-implementation/backlog.md` 将 Phase 6 从 5 个工作包扩展为 6 个，新增 V2-060F；`doc/04-implementation/acceptance-criteria.md` 同步 Phase 6 checkbox 与产出清单。
- 验收口径：外部 agent assets（智能体资产）必须物化为 generated project workspace（生成项目工作区）内的 `00-boardroom/agents/` 快照，并通过 `asset-import-manifest.yaml` 记录 source lineage（来源链），不能成为 execution package 编译时的隐式外部依赖。

### V2-030B AgentSeat 与 seat policy（2026-05-17）

- 工作包：V2-030B
- 状态：DONE
- 关键产出：新增 `src/boardroom_os/agents/categories.py`、`src/boardroom_os/agents/seat.py`、`src/boardroom_os/agents/policy.py`、`tests/execution/test_agent_seat_policy.py`；更新 `src/boardroom_os/agents/profiles.py`、`src/boardroom_os/agents/__init__.py`、`src/boardroom_os/events/types.py`、`src/boardroom_os/graph/ticket.py`、`src/boardroom_os/graph/seat_assignment.py` 和相关 reducer / negative tests。
- 关键实现：`AgentSeat`（智能体席位）携带 `actor_ref`、`role_profile_ref`、`role_category`、`capability_tags`、`model_execution_profile_ref`、`skill_refs`、`context_budget_tokens` 和 lifecycle status（生命周期状态）；`SeatLifecycleProjector`（席位生命周期投影器）从 seat lifecycle events（席位生命周期事件）投影 `active_seats`；`SeatPolicy`（席位策略）按 `SeatDemand`（席位需求）匹配 active seats 并强制 checker / worker role category 独立。
- Governance 边界：`BOOTSTRAP_GOVERNANCE_AUTHORITY` 是可重放事实，且仅由 `GovernanceAuthorityProjector`（治理授权投影器）校验。bootstrap actor（创世参与者）只在有限 bootstrap sequence（创世序列）中拥有 authority：注册首个 governance RoleProfile（治理角色模板）并激活首个 CEO AgentSeat；一旦 active CEO seat 存在，后续 role / seat 演化必须通过 active governance seat + `team.composition` capability。
- Ticket / assignment 迁移：`TicketCreatedPayload`（任务创建载荷）与 `TicketNode`（任务节点）一次性退役 `owner_seat_ref`，改用 `seat_demand` 作为需求权威；`SeatAssignmentProjector`（席位分配投影器）消费 `SeatLifecycleProjection.active_seats`，不再使用 graph-local `SeatDefinition` 快照，也不自动 follow replacement chain（替换链）。
- Negative tests：bootstrap fact 缺失/重复/载荷解析失败、bootstrap actor 长期超级用户、非 bootstrap actor 注册首个 governance role、AgentSeat 缺 actor/model/skills/capabilities/positive budget、capability subset 越界、lifecycle event 改写静态字段、未激活或 unknown seat 被分配、default worker fallback、checker/worker 同 seat 或同 role category 均 fail closed 或产生 team composition blocker。
- Happy path：CEO、Architect、Worker、Checker seats 可被创建、激活并映射到 ticket demand；frontend role / seat 可由 CEO 演化后匹配 frontend demand；active seats 可供 demand-first assignment 消费。
- 验证证据：`PYTHONPATH=src pytest tests/reducers/test_projection_replay.py tests/reducers/test_ticket_reducer_transitions.py tests/negative/test_executor_cannot_complete_ticket.py -q` 通过（24 passed）；`PYTHONPATH=src pytest tests/execution/test_agent_profiles.py tests/execution/test_agent_seat_policy.py tests/reducers/test_ticket_graph_projection.py tests/reducers/test_seat_assignment_projection.py tests/reducers/test_projection_replay.py -q` 通过（128 passed）；`PYTHONPATH="src;." pytest tests/contracts tests/reducers tests/execution tests/negative -q` 通过（248 passed）。
- 收尾审查处理：新增 DEC-0013，要求 V2-030D 在 ExecutionPackage compiler（执行包编译器）前引入 AgentTeamProjector（智能体团队投影器）或等价编排入口；`domain-model.md` 同步 ticket → seat_demand → AgentSeat 的依赖方向；`SeatLifecycleProjector`（席位生命周期投影器）依赖类型从裸 `Any` 收紧为 protocol（协议）；`OwnerSeatRef`（源码实现面归属标签）增加说明，避免与退役的 ticket owner seat 字段混淆。

### V2-030C ExecutionPackage schema（2026-05-17）

- 工作包：V2-030C
- 状态：DONE
- 关键产出：新增 `src/boardroom_os/execution/__init__.py`、`src/boardroom_os/execution/package.py`、`tests/execution/test_execution_package_schema.py`、`tests/negative/test_execution_package_fail_closed.py`。
- 关键实现：`ExecutionPackage`（执行包）采用 frozen Pydantic model（冻结 Pydantic 模型）与 `version: Literal[1] = 1`；任务字段锁定为 `ticket_ref`（任务引用）并复用 `TicketId`（任务 ID），拒绝 `ticket_id` alias；`seat_ref` 复用 `AgentSeatRef`（智能体席位引用）；执行包嵌入完整 `ModelExecutionProfile`（模型执行配置）与 `EvidenceObligation`（证据义务）快照；`commands` 复用 `PackageCommand`（包命令）；顶层 `fallback_policy_ref` 表达本次执行实际生效的降级策略引用，不解释 fallback classification（降级策略分类）。
- Negative tests：缺 `ticket_ref` / `graph_version` / `seat_ref` / `model_execution_profile` / `acceptance_refs` / `allowed_write_set` / `evidence_obligations` / `fallback_policy_ref`、非正 graph_version、空 required tuple、空引用元素、空命令元素、unknown fields、只传 `ticket_id` 或只传 `model_execution_profile_ref` 均 fail closed；`allowed_read_refs` 可为空。
- Happy path：直接构造完整对象图，不调用任何上游 resolver（解析器）或 compiler（编译器），即可表达 ready ticket + active contracts + seat assignment 已解析后的 worker input snapshot（执行智能体输入快照）。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/negative/test_execution_package_fail_closed.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.execution.package'`）；实现后同命令通过（28 passed）；`PYTHONPATH=src pytest tests/execution/test_execution_package_schema.py tests/negative/test_execution_package_fail_closed.py -q` 通过（30 passed）；`PYTHONPATH="src;." pytest tests/execution tests/reducers tests/negative -q` 通过（247 passed）。

### V2-030D ExecutionPackage compiler（2026-05-17）

- 工作包：V2-030D
- 状态：DONE
- 关键产出：新增 `src/boardroom_os/agents/team.py`、`src/boardroom_os/execution/compiler.py`、`tests/negative/test_agent_team_projector_fail_closed.py`、`tests/execution/test_agent_team_projector.py`、`tests/negative/test_execution_package_compiler_fail_closed.py`、`tests/execution/test_execution_package_compiler.py`；更新 `src/boardroom_os/agents/__init__.py`、`src/boardroom_os/agents/seat.py`、`src/boardroom_os/execution/__init__.py`、`src/boardroom_os/graph/seat_assignment.py`、`tests/execution/test_agent_seat_policy.py`。
- 关键实现：新增 AgentTeamProjector（智能体团队投影器）按 graph_version 交织消费 RoleProfile change facts（角色模板变更事实）与 Seat lifecycle facts（席位生命周期事实），为 compiler 提供单一 AgentTeamProjection（智能体团队投影）；新增 ExecutionWorkspaceContext（执行工作区上下文）和 ExecutionPackageCompiler（执行包编译器），从 ready ticket（就绪任务）、active contracts（活跃合同）、SeatAssignmentGraph（席位分配图）和 AgentTeamProjection 编译 ExecutionPackage（执行包），显式转换 TicketNode 字符串字段到 typed refs（类型化引用）并把 ModelExecutionProfile.fallback_policy_ref 的 ContractId 转为 FallbackPolicyRef。
- Negative tests：AgentTeamProjector 覆盖 unsorted events、project mismatch、bootstrap 缺失/重复、role payload 解析/schema/graph_version/actor mismatch、unsupported role action、unknown role、seat lifecycle payload 缺口、governance seat deactivated 后注册、重复 role_profile_id；ExecutionPackageCompiler 覆盖 ticket not ready、缺派工、unknown/inactive assigned seat、capability mismatch、graph_version mismatch、inactive acceptance contract、acceptance_ref outside active contract、unknown source surface、source surface acceptance mismatch、criterion source surface scope mismatch、write path 越界、absolute/drive-relative/parent escape unsafe path、missing evidence obligation、evidence obligation scope mismatch、blocking coverage 缺口、evidence_required artifact mismatch、required_tests 缺失或未映射 declared command、unknown role/model profile 均 fail closed。
- Happy path：AgentTeamProjector 正例证明 CEO governance seat 激活后可注册并激活 backend worker seat；ExecutionPackageCompiler 正例证明 backend worker ticket 可编译出包含 commands、context_refs、constraints、allowed_write_set、required_outputs、evidence_obligations、fallback_policy_ref 和 audit_requirements 的 ExecutionPackage。
- 验证证据：`PYTHONPATH="src;." pytest tests/negative/test_agent_team_projector_fail_closed.py tests/execution/test_agent_team_projector.py tests/negative/test_execution_package_compiler_fail_closed.py tests/execution/test_execution_package_compiler.py -q` 通过（44 passed）；`PYTHONPATH="src;." pytest tests/execution/test_execution_package_schema.py tests/execution/test_agent_profiles.py tests/execution/test_agent_seat_policy.py tests/reducers/test_seat_assignment_projection.py tests/reducers/test_projection_replay.py -q` 通过（71 passed）；`PYTHONPATH="src;." pytest tests/contracts tests/reducers tests/execution tests/negative -q` 通过（325 passed）。

### V2-030E fallback policy 分类（2026-05-17）

- 工作包：V2-030E
- 状态：DONE
- 关键产出：`src/boardroom_os/execution/fallback.py`、`tests/negative/test_fallback_cannot_satisfy_implementation.py`、`tests/execution/test_fallback_policy.py`；同步更新 `src/boardroom_os/execution/__init__.py`。
- 关键实现：新增 EvidencePurpose（证据用途）、FallbackKind（降级类型）、FallbackPolicy（降级策略）、FallbackEvidenceRequest（降级证据请求）、FallbackEvidenceDecision（降级证据判定）和 evaluate_fallback_evidence（降级证据判定函数）；复用 FallbackPolicyRef（降级策略引用）、RequiredArtifactType（必需产物类型）和 AcceptanceRef（验收引用）；decision（判定）携带 fallback_policy_ref / applied_kind / evaluated_purpose 审计字段。
- Negative tests：证明 `PROVIDER_UNAVAILABLE`、`TEST_ONLY_SIMULATION`、`DETERMINISTIC_GOVERNANCE_DRAFT`、`TOOLING_PREFLIGHT` 和越界 `CONTRACT_ALLOWED_DETERMINISTIC_TRANSFORM` 均不能满足 implementation evidence（实施证据）；覆盖非 contract transform 携带 allow-list、contract transform 缺 allow-list、request 缺 purpose / artifact_type / acceptance_refs、空 acceptance_refs 和 unknown extra fields 的 fail closed。
- Happy path：证明合同显式允许的 deterministic transform（确定性转换）只能满足窄范围 deterministic evidence（确定性证据），tooling preflight（工具预检）只能满足 diagnostic evidence（诊断证据）。
- 决策：新增 DEC-0014，明确 `TEST_ONLY_SIMULATION` 不再保留“失败路径测试例外”。
- 验证证据：`PYTHONPATH=src pytest tests/negative/test_fallback_cannot_satisfy_implementation.py tests/execution/test_fallback_policy.py -q` 通过（22 passed）；`PYTHONPATH="src;." pytest tests/execution tests/negative -q` 通过（201 passed）。
- 审查修订：V2-030E 仅提供 typed evaluator（类型化判定器），尚未被生产 verifier（验证器）调用；AC-V2-EXECUTION-003 checkbox 回退为未勾选，并新增 V2-050A1 作为 FallbackPolicyRegistry（降级策略注册表）与 verifier wiring（验证器接入）责任工作包。补充 truth table（真值表）全负例后，`PYTHONPATH=src pytest tests/negative/test_fallback_cannot_satisfy_implementation.py -q` 通过（24 passed）。

### V2-030F agent context index 草稿（2026-05-17）

- 工作包：V2-030F
- 状态：DONE
- 关键产出：`src/boardroom_os/execution/context_index.py`、`tests/execution/test_agent_context_index.py`、`src/boardroom_os/execution/__init__.py`。
- Negative tests：证明缺 `execution_package_ref`、`model_execution_profile`、`allowed_write_set`、`provider_attempt_refs`、执行包 ID alias 绕过、snapshot fingerprint（快照指纹）篡改、snapshot fingerprint 非 SHA-256 hex digest、重复 provider attempt ref（模型调用尝试引用）、同一 provider attempt ref 被多个 entry（条目）认领均 fail closed（失败关闭）。
- Happy path：证明 ExecutionPackage（执行包）可幂等生成完整 AgentContextSnapshot（智能体上下文快照），保留 objective（任务目标）、context refs（上下文引用）、constraints（约束）、acceptance refs（验收引用）、source surface refs（源码实现面引用）、allowed read/write 边界、required outputs（必需产物）、commands（命令）、evidence obligations（证据义务）、fallback policy ref（降级策略引用）和 audit requirements（审计要求）；provider retry（模型供应商重试）可按顺序绑定多个 ProviderAttemptRef 且不重新打包上下文。
- 验证证据：`PYTHONPATH="src;." pytest tests/execution/test_agent_context_index.py -q` 通过（46 passed）；`PYTHONPATH="src;." pytest tests/execution -q` 通过（100 passed）；`PYTHONPATH="src;." pytest tests/contracts tests/reducers tests/execution tests/negative -q` 通过（400 passed）。

### V2-040A ProviderAdapter 与 ProviderAttempt（2026-05-18）

- 工作包：V2-040A
- 状态：DONE
- 关键产出：`src/boardroom_os/providers/attempt.py`、`src/boardroom_os/providers/adapter.py`、`tests/execution/test_provider_attempt.py`。
- Negative tests：证明 ProviderAttempt（模型调用尝试记录）缺 `provider`、`model`、`reasoning_effort`、`input_package_ref`、`seat_ref`、`status`、`outcome`，缺 raw/parsed output ref（原始/解析产物引用）、无时区 timestamp（时间戳）、`finished_at` 早于 `started_at`、成功 attempt 携带 `failure_kind`、失败 attempt 缺 `failure_kind`、fallback outcome（降级结果）缺 typed `fallback_kind`（类型化降级类型）、primary outcome（主路径结果）携带 `fallback_kind`、unknown extra fields（未知额外字段）均 fail closed（失败关闭）。
- Happy path：证明 FakeProviderTransport（模拟传输）可从 ProviderRequest（模型供应商请求）生成 succeeded ProviderAttempt（成功模型调用尝试记录），绑定 ExecutionPackageRef（执行包引用）、AgentSeatRef（智能体席位引用）、raw output ref、parsed output ref，并显式标记为 primary/non-fallback outcome（主路径/非降级结果）。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/execution/test_provider_attempt.py -q` 得到预期 RED（缺 provider 模块 / 缺 ProviderAttemptOutcome）；实现后 `PYTHONPATH=src pytest tests/execution/test_provider_attempt.py -q` 通过（24 passed）；`PYTHONPATH="src;." pytest tests/execution -q` 通过（124 passed）。

### V2-040B provider executor boundary（2026-05-18）

- 工作包：V2-040B
- 状态：DONE
- 关键产出：`src/boardroom_os/execution/provider_executor.py`、`tests/execution/test_provider_executor.py`、`tests/negative/test_provider_executor_fail_closed.py`。
- Negative tests：证明缺 ExecutionPackage（执行包）、RoleProfile / TicketNode shortcut（角色模板/任务节点捷径）、不可调用 provider adapter（模型供应商适配器）、错绑 `input_package_ref` / `seat_ref` / provider / model / reasoning_effort 的 ProviderAttempt（模型调用尝试记录）均 fail closed。
- Happy path：证明合法 ExecutionPackage（执行包）经 ProviderExecutor（模型供应商执行器）调用 FakeProviderTransport（模拟传输）可产生绑定正确的 ProviderAttempt（模型调用尝试记录）；AgentContextSnapshot（智能体上下文快照）由 `build_agent_context_snapshot` 生成；ProviderRequest.prompt（模型供应商请求提示词）可由 snapshot 确定性重算；failed ProviderAttempt（失败模型调用尝试记录）作为可审计事实返回。
- TDD 证据：先运行 provider executor 输入边界、prompt 渲染和 executor 正例测试分别得到缺 `boardroom_os.execution.provider_executor`、缺 `render_prompt_from_snapshot`、缺 `ProviderExecutor` 的预期 RED；实现后对应测试转为 GREEN。
- 验证证据：`PYTHONPATH="src;." pytest tests/execution/test_provider_attempt.py tests/execution/test_agent_context_index.py tests/execution/test_provider_executor.py tests/negative/test_provider_executor_fail_closed.py -q` 通过（82 passed）；`PYTHONPATH="src;." pytest tests/execution tests/negative -q` 通过（290 passed）。

### V2-040C WorkProduct 解析和提交事件（2026-05-18）

- 工作包：V2-040C
- 状态：DONE
- 关键产出：`src/boardroom_os/execution/work_product.py`、`tests/execution/test_work_product_submission.py`。
- Negative tests：证明 WorkProduct（工作产物）缺 producer_attempt_ref（生产者尝试引用）/ artifact_refs（产物引用）/ claim_refs（声明引用）、WorkProductClaimDraft（工作产物证据声明草稿）缺 acceptance_refs（验收引用）/ source_surface_refs（源码实现面引用）、WorkProductSubmission（工作产物提交包）缺 claim_drafts（声明草稿）或 claim_refs 与 claim_drafts 不一致、失败 ProviderAttempt（模型调用尝试记录）、错绑 execution_package_ref（执行包引用）/ seat_ref（席位引用）、缺 raw/parsed output ref（原始/解析输出引用）、空白 summary（摘要）、事件工厂接收 completion event_type（完成事件类型）均 fail closed。
- Happy path：fake provider transport（模拟模型传输）可生成绑定 ExecutionPackageRef（执行包引用）、TicketId（任务 ID）、ProviderAttemptRef（模型调用尝试引用）、raw+parsed artifact refs（原始+解析产物引用）与 claim draft refs（声明草稿引用）的 WorkProduct；WorkProductClaimDraft 不具备 verified_evidence_refs / verdict / status；`build_work_product_submitted_event`（构建工作产物已提交事件）只生成 `WORK_PRODUCT_SUBMITTED`（工作产物已提交）事实事件，事件 `payload_refs` 指向 WorkProductRef（工作产物引用）；TicketReducer（任务状态归约器）通过 `resolve_work_product_ticket_ref`（解析工作产物对应任务引用）把 WorkProduct payload（工作产物载荷）解析回 ticket_ref（任务引用），登记“已提交工作产物”但不会单独完成 ticket（任务），只有追加独立 `TICKET_COMPLETED`（任务完成）事件且 completion gate（完成门禁）满足后才会完成。
- 验证证据：`PYTHONPATH="src;." pytest tests/execution/test_work_product_submission.py -q` 通过（38 passed）；`PYTHONPATH="src;." pytest tests/execution/test_provider_attempt.py tests/execution/test_provider_executor.py tests/execution/test_agent_context_index.py tests/execution/test_work_product_submission.py tests/negative/test_provider_executor_fail_closed.py -q` 通过（120 passed）；`PYTHONPATH="src;." pytest tests/contracts tests/reducers tests/execution tests/negative -q` 通过（474 passed）。

### V2-040D CommandRunner 与 VerificationRun（2026-05-18）

- 工作包：V2-040D
- 状态：DONE
- 关键产出：`src/boardroom_os/adapters/process_runner.py`、`src/boardroom_os/execution/verification_run.py`、`tests/execution/test_command_runner.py`，并更新 `src/boardroom_os/execution/__init__.py` 导出入口；新增同行评审 spec：`doc/04-implementation/v2-040d-command-runner-spec.md`。
- 关键实现：`CommandRunner`（命令执行器）只执行 `ExecutionPackage.commands` 与 `PackageContract.run_commands/test_commands` 中同 `command_id` 且完整相等的 `PackageCommand`；`SubprocessExecutor`（子进程执行器）捕获真实 exit code/stdout/stderr；`VerificationRun`（验证运行）记录 command、cwd、exit_code/status、stdout/stderr refs、duration、时区时间戳、runner/environment/workspace refs，并强制 status 与 exit_code 一致。
- Negative tests：缺 exit_code、缺 stdout/stderr refs、status 与 exit_code 不一致、naive timestamp、duration 超过 wall-clock window、命令不在 package contract / execution package、PackageCommand label/command/cwd 不一致、cwd parent traversal、absolute cwd、malformed ProcessResult、executor 异常、naive clock、clock rollback 均 fail closed。
- Happy path：合法声明命令可生成 deterministic `VerificationRunRef` 与 stdout/stderr refs，捕获 stdout/stderr/duration；非零 exit code 记录为 failed fact 而非合成成功。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/execution/test_command_runner.py -q` 通过（36 passed）；`PYTHONPATH="src;." python -m pytest tests/execution tests/negative -q` 通过（364 passed）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/reducers tests/execution tests/negative -q` 通过（510 passed）。

### V2-040E RuntimeExecutor 事实事件边界（2026-05-19）

- 工作包：V2-040E
- 状态：DONE
- 关键产出：`src/boardroom_os/execution/runtime_executor.py`、`tests/execution/test_runtime_executor_boundary.py`、`tests/negative/test_runtime_cannot_govern.py`；设计依据为 `doc/04-implementation/v2-040e-runtime-executor-spec.md`。
- 关键实现：RuntimeExecutor（运行时执行器）组合 ProviderExecutor（模型供应商执行器）、WorkProduct builder（工作产物构建器）和 CommandRunner（命令执行器），只返回 `EXECUTION_STARTED`、`PROVIDER_ATTEMPT_RECORDED`、`WORK_PRODUCT_SUBMITTED`、`COMMAND_RUN_RECORDED` 等 runtime fact events（运行时事实事件），不 append event log（事件日志）也不调用 reducer（状态归约器）完成 ticket。
- Negative tests：证明 runtime emit `TICKET_COMPLETED`、保留治理事件名 `project_completed` / `closeout_committed`、未知事件名、runtime_actor_ref 等于任一 active AgentSeat.actor_ref、缺 active execution seat、GOVERNANCE / ARCHITECTURE / AUDIT seat 执行包、first_fact_graph_version 未大于执行包 graph_version 均 fail closed。
- Happy path：证明 ready ExecutionPackage 可记录 provider attempt、work product 和 command run 事实；provider failed 只记录 failed ProviderAttempt 且不生成 WorkProduct/command run；command failed 记录 failed VerificationRun 而不抛治理错误；事件 graph_version 连续递增且不包含 completion event。
- 验证证据：`PYTHONPATH=src:. python -m pytest tests/negative/test_runtime_cannot_govern.py -q`（15 passed）；`PYTHONPATH=src:. python -m pytest tests/execution/test_runtime_executor_boundary.py -q`（10 passed）；`PYTHONPATH=src:. python -m pytest tests/execution/test_provider_executor.py tests/execution/test_work_product_submission.py tests/execution/test_command_runner.py tests/execution/test_runtime_executor_boundary.py tests/negative/test_provider_executor_fail_closed.py tests/negative/test_runtime_cannot_govern.py -q`（111 passed）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/negative -q`（535 passed）。

### V2-050A EvidenceClaim（2026-05-19）

- 工作包：V2-050A
- 状态：DONE
- 关键产出：`src/boardroom_os/evidence/claim.py`、`src/boardroom_os/evidence/__init__.py`、`tests/evidence/test_evidence_claim.py`；同行评审 spec 为 `doc/04-implementation/v2-050a-evidence-claim-spec.md`。
- 关键实现：新增 EvidenceClaim（证据声明）、EvidenceClaimSourceKind（证据声明来源类型）、EvidenceArtifactRef（证据产物引用）、FallbackLineageMarker（降级来源链标记）和 EvidenceClaimBuildError（证据声明构建错误）；`build_evidence_claim_from_work_product`（从工作产物构建证据声明）绑定 WorkProduct（工作产物）与 WorkProductClaimDraft（工作产物证据声明草稿）并校验 producer/包/任务/产物 lineage（一致来源链）；`build_evidence_claim_from_verification_run`（从验证运行构建证据声明）要求显式 ProviderAttemptRef（模型调用尝试引用），把 stdout/stderr refs 作为 EvidenceArtifactRef（证据产物引用），并通过 verification_run_refs（验证运行引用）关联完整命令事实。
- Negative tests：证明 EvidenceClaim 缺 producer_attempt_ref、evidence_obligation_ref、acceptance_refs、source_surface_refs、artifact_refs、expected_purpose、required_artifact_type、summary 或 malformed scalar refs 均 fail closed；WorkProduct builder 拒绝 claim draft 不属于 work product、producer/execution package/ticket/artifact mismatch、fallback 缺 fallback_policy_ref、primary 携带 fallback_policy_ref、acceptance/source coverage 不足、expected_purpose 非 EvidencePurpose；VerificationRun builder 拒绝缺/错 producer_attempt_ref、acceptance/source coverage 不足、expected_purpose 非 EvidencePurpose、malformed scalar refs，以及 source_ref 与当前 verification_run_refs 不一致。
- Happy path：证明 succeeded ProviderAttempt（成功模型调用尝试记录）经 build_work_product_from_provider_attempt（从模型尝试构建工作产物）生成 WorkProductSubmission（工作产物提交包）后可生成 implementation EvidenceClaim；VerificationRun（验证运行）可生成 command evidence claim，且不复制 command_id；fallback WorkProduct claim 只记录 typed fallback marker（类型化降级标记），不记录 allowed/decision；两个 builder 均保留调用方传入的 expected_purpose 与 explicit claim_id。
- 边界说明：EvidenceClaim 仍只是 producer declaration（产出者声明），不是 verified evidence（已验证证据）；V2-050A 不实现 EvidenceVerifier（证据验证器）、FallbackPolicyRegistry（降级策略注册表）、FinalEvidenceTable（最终证据表）、CheckerVerdict（检查结论）或 completion gate（完成门禁）。Phase 5 checkbox 保持未勾选，等待 V2-050A1/B/C/D/E/F 与 V2-060C 闭合。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/evidence/test_evidence_claim.py -q`（60 passed）；`PYTHONPATH="src:." python -m pytest tests/execution/test_work_product_submission.py tests/execution/test_command_runner.py tests/evidence/test_evidence_claim.py -q`（134 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（595 passed）。

### 2026-05-20 — V2-050A1 FallbackPolicyRegistry（降级策略注册表）

- 关键产出文件：`src/boardroom_os/evidence/fallback_registry.py`、`tests/evidence/test_fallback_policy_registry.py`、`tests/negative/test_fallback_registry_fail_closed.py`。
- Negative tests：覆盖 registry 为空、重复 policy ref、非 policy 项、未知/非法 ref 解析、危险 fallback kind 注册、registry 缺位、fallback_marker 缺失、policy kind 与 marker 不一致、decision policy/kind/purpose 不对齐、空 acceptance_refs、naive evaluated_at，以及 implementation purpose 下 fallback decision 必须 blocked。
- Happy tests：覆盖 `FallbackPolicyRegistry`（降级策略注册表）按 ref 解析 `FallbackPolicy`（降级策略）、`evaluate_fallback_claim`（降级声明判定函数）为 deterministic transform（确定性转换）与 tooling preflight（工具预检）生成可审计 `FallbackDecisionRecord`（降级判定记录），支持默认 deterministic record id、显式 record id 和审计友好的 JSON dump。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/negative/test_fallback_registry_fail_closed.py -q`（20 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_fallback_policy_registry.py -q`（7 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_evidence_claim.py tests/evidence/test_fallback_policy_registry.py tests/negative/test_fallback_registry_fail_closed.py tests/negative/test_fallback_cannot_satisfy_implementation.py tests/execution/test_fallback_policy.py -q`（116 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（622 passed）。
- 边界说明：A1 只产出 fallback decision record（降级判定记录），不实现 EvidenceVerifier（证据验证器）、FinalEvidenceTable（最终证据表）、event log 接线或 completion gate（完成门禁）；AC-V2-EXECUTION-003 仍等待 V2-050B 由 verifier 实际消费 registry + decision record 后闭合。

### 2026-05-20 — V2-050B EvidenceVerifier（证据验证器）

- 关键产出文件：`src/boardroom_os/evidence/verifier.py`、`tests/evidence/test_evidence_verifier.py`、`tests/negative/test_synthetic_evidence_rejected.py`、`src/boardroom_os/evidence/__init__.py`。
- Negative tests：覆盖 missing artifact/hash/provider、inactive/unknown acceptance refs、EvidenceObligation（证据义务）不对齐、EvidencePurposePolicy（证据用途策略）缺规则或 purpose mismatch、ArtifactManifest（产物清单）多余 artifact、VerificationRun（验证运行）缺失/失败/stdout·stderr 不一致、primary/fallback ProviderAttempt（模型调用尝试记录）不匹配、fallback registry/decision/recorded ref 缺失、unresolved policy、decision scope mismatch、allowed=False，以及 `evaluate_fallback_claim`（降级声明判定函数）必须用完整 claim scope（声明作用域）和 verified_at（验证时间）调用一次。
- Happy tests：覆盖 primary WorkProduct（主路径工作产物）claim、command VerificationRun（命令验证运行）claim、allowed deterministic fallback（允许的确定性降级）claim 均可生成 VerifiedEvidence（已验证证据）；blocked fallback decision（被阻断的降级判定）返回可审计 blocker；VerifiedEvidence 具备 deterministic default id（确定性默认 ID）、audit-friendly JSON（审计友好 JSON），EvidenceVerificationResult（证据验证结果）保持 success-or-blockers XOR（成功或阻断互斥）。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/negative/test_synthetic_evidence_rejected.py -q`（33 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_evidence_verifier.py -q`（7 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_evidence_claim.py tests/evidence/test_fallback_policy_registry.py tests/evidence/test_evidence_verifier.py tests/negative/test_synthetic_evidence_rejected.py tests/negative/test_fallback_registry_fail_closed.py -q`（127 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（662 passed）。
- 边界说明：V2-050B 只把单条 EvidenceClaim（证据声明）验真为 VerifiedEvidence（已验证证据）或 blockers（阻断项），不实现 FinalEvidenceTable（最终证据表）、CheckerVerdict（检查结论）、SourceInventory（源码清单）或 reducer completion gate（完成门禁）；AC-V2-EXECUTION-003 已由 verifier 实际消费 fallback registry + decision record 后闭合，AC-V2-EVIDENCE-002 仍等待 V2-060C SourceInventory lineage（源码清单来源链）。

### 2026-05-21 — V2-050C FinalEvidenceTable（最终证据表）

- 关键产出文件：`src/boardroom_os/evidence/table.py`、`tests/evidence/test_final_evidence_table.py`、`tests/negative/test_missing_acceptance_map_blocks_closeout.py`。
- Negative tests：覆盖 inactive AcceptanceContract（非活跃验收合同）、空 acceptance map（验收映射）、missing blocking criterion（阻塞验收项缺证据）、未知 verified evidence / failed blocker acceptance_ref、notes 输入、EvidenceClaim / EvidenceVerificationResult 误入、重复 acceptance rows、非确定性 table id，以及 satisfied / failed / complete 不变量破坏均 fail closed。
- Happy tests：覆盖单个与多个 blocking criteria（阻塞验收项）由 VerifiedEvidence（已验证证据）满足、单条 VerifiedEvidence 覆盖多个 acceptance_ref、missing / failed row 使 table incomplete、failed blocker 保留 audit refs 且不能被 VerifiedEvidence 覆盖、audit-friendly JSON（审计友好 JSON）序列化与确定性 `final-evidence-table.<contract_id>`。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/evidence/test_evidence_verifier.py tests/evidence/test_final_evidence_table.py tests/negative/test_synthetic_evidence_rejected.py tests/negative/test_missing_acceptance_map_blocks_closeout.py -q`（70 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（692 passed）。
- 边界说明：V2-050C 只做 FinalEvidenceTable（最终证据表）聚合，不消费 notes（备注）或 EvidenceVerificationResult（证据验证结果），也不替代 CheckerVerdict（检查结论）或 closeout gate（收尾门禁）。

### 2026-05-21 — V2-050D CheckerVerdict（检查结论）

- 关键产出文件：`src/boardroom_os/checker/verdict.py`、`src/boardroom_os/checker/checker.py`、`src/boardroom_os/checker/__init__.py`、`tests/evidence/test_checker_verdict.py`。
- Negative tests：覆盖 dict / raw EvidenceClaim（证据声明）/ VerifiedEvidence（已验证证据）/ EvidenceVerificationResult（证据验证结果）/ final_evidence_blockers（最终证据阻断项）/ override 字段输入拒绝；inactive AcceptanceContract（非活跃验收合同）、contract mismatch（合同错配）、ticket mismatch（任务错配）、空 SourceDiffRef（源码差异引用）、WorkProduct（工作产物）缺 artifact_refs / claim_refs、FinalEvidenceTable（最终证据表）complete 不一致、FAILED row 无 blocker、row status 非枚举、row acceptance_ref 越界，以及 copied CheckerServiceInput（复制后的检查服务输入）篡改均 fail closed。
- Happy tests：完整 FinalEvidenceTable 无 notes 返回 `approved`；完整 FinalEvidenceTable 携带非阻断 CheckerNote（检查备注）返回 `approved_with_non_blocking_notes`；CheckerVerdict 输出 deterministic id（确定性 ID）和 audit-friendly JSON（审计友好 JSON）。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/evidence/test_checker_verdict.py -q`（43 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_final_evidence_table.py tests/evidence/test_checker_verdict.py tests/negative/test_missing_acceptance_map_blocks_closeout.py -q`（73 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（735 passed）。
- 边界说明：V2-050D 只产出 CheckerVerdict（检查结论）和 CheckerService（检查服务），不生成 V2-050E rework ticket（返工任务）、不新增 checker event（检查事件），也不替代 EvidenceVerifier（证据验证器）或 closeout gate（收尾门禁）。

### 2026-05-21 — V2-050E ReworkTicketGenerator（返工任务生成器）

- 关键产出文件：`src/boardroom_os/checker/rework.py`、`src/boardroom_os/checker/__init__.py`、`tests/evidence/test_rework_ticket_generation.py`。
- Negative tests：覆盖 approved / approved_with_notes / escalate verdict（批准/带备注批准/升级结论）不生成 rework、ticket/verdict mismatch（任务/结论不匹配）、缺 blockers（阻断项）、completed original ticket（已完成原任务）、blocker_id 缺失、blocker 文本字段为空、final evidence blocker 缺 acceptance_ref（验收引用）、未知 acceptance_ref、原 ticket 缺 acceptance/source/evidence/write scope（验收/源码/证据/写入范围）、allowed_read_refs 非字符串、allowed_write_set override 扩展、naive generated_at（无时区生成时间）、result/payload mismatch（结果/载荷不一致）、extra input fields（额外输入字段）和 EventRecord 越界。
- Happy tests：missing / failed / manual checker blocker（缺失/失败/手动检查阻断项）可生成 deterministic rework ticket（确定性返工任务）；rework ticket 默认 `depends_on=()`，通过 ReworkTicketPlan.original_ticket_ref（返工任务计划原任务引用）和 allowed_read_refs（允许读取引用）表达 lineage（来源链）；allowed_read_refs 保存 `.value` 字符串并包含 CheckerVerdictRef（检查结论引用）、FinalEvidenceTableRef（最终证据表引用）、WorkProductRef（工作产物引用）和 CheckerBlockerRef（检查阻断引用）；seat_demand（席位需求）、source_surface_refs（源码实现面引用）和 evidence_obligations（证据义务）继承原 ticket；purpose override（目的覆盖）保留 original/blocker trace（原任务/阻断项追踪）；TicketReducer（任务状态归约器）集成证明原 ticket BLOCKED 且 rework ticket READY。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/evidence/test_rework_ticket_generation.py -q`（42 passed）；`PYTHONPATH="src:." python -m pytest tests/evidence/test_checker_verdict.py tests/evidence/test_rework_ticket_generation.py -q`（85 passed）；`PYTHONPATH="src:." python -m pytest tests/reducers/test_ticket_reducer_transitions.py tests/evidence/test_rework_ticket_generation.py -q`（54 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（777 passed）。
- 边界说明：V2-050E 只生成 ReworkTicketPlan（返工任务计划）和 reducer 可消费 payload（归约器可消费载荷），不 append EventRecord（追加事件记录）、不修改 TicketReducer（任务状态归约器）、不实现 V2-050F completion gate（完成门禁）。

### V2-050F CompletionGate 接入 reducer（2026-05-22）

- 工作包：V2-050F
- 状态：DONE
- 关键产出文件：`src/boardroom_os/reducers/completion_gate.py`、`tests/reducers/test_completion_gate_with_evidence.py`
- 关键实现：新增 CompletionGate（完成门禁）、CompletionGateInput（完成门禁输入）和 CompletionGateResult（完成门禁结果），把 VerifiedEvidence（已验证证据）、FinalEvidenceTable（最终证据表）与 CheckerVerdict（检查结论）适配到 TicketReducer（任务状态归约器）的 completion boundary（完成边界），保持 `WORK_PRODUCT_SUBMITTED`、provider attempt 和 fallback lineage（降级来源链）门禁不被绕过。
- Negative tests：覆盖 checker/evidence/provider/work_product/fallback/reducer history（检查结论/证据/模型调用/工作产物/降级/归约历史）失败路径，验证 evidence table 缺失、checker blocker、attempt count 为 0、缺 `WORK_PRODUCT_SUBMITTED` 历史事实、fallback decision `allowed=False`、缺 `FALLBACK_DECISION_RECORDED` lineage 均阻断 completion。
- Happy path：覆盖 primary evidence（主路径证据）、`approved_with_non_blocking_notes`（带非阻断备注批准）、allowed deterministic fallback lineage（允许的确定性降级来源链）以及 TicketReducer completion（任务状态归约器完成）通过路径。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/reducers/test_completion_gate_with_evidence.py -q`（28 passed）；`PYTHONPATH="src:." python -m pytest tests/reducers/test_ticket_reducer_transitions.py tests/evidence/test_final_evidence_table.py tests/evidence/test_checker_verdict.py tests/evidence/test_fallback_policy_registry.py tests/evidence/test_evidence_verifier.py tests/reducers/test_completion_gate_with_evidence.py -q`（111 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/negative -q`（805 passed）。

### V2-060A WorkspaceManifest（工作区清单）（2026-05-22）

- 工作包：V2-060A
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/__init__.py`、`src/boardroom_os/workspace/manifest.py`、`tests/proving/test_workspace_manifest.py`
- 关键实现：新增 WorkspaceManifest（工作区清单）模型，固定四区 `00-boardroom` / `10-project` / `20-evidence` / `30-audit`；WorkspacePath（工作区路径）拒绝绝对路径、Windows drive、`..`、`.`、空 segment、反斜杠和尾部斜杠；WorkspaceManifest 拒绝缺 project/evidence、重复 section、非 canonical section path、extra fields、workspace_root 落入框架 repo layout（`src`、`src/boardroom_os`、`tests`、`doc`、`scripts`、`examples`、`backend`），以及 workspace_manifest_id 与 workflow_ref 不匹配；`build_workspace_manifest`（构建工作区清单）拒绝 `package_contract.package_root != 10-project`，成功时生成 deterministic `workspace-manifest.<workflow_ref>` 并按固定顺序输出四区。
- Negative tests：覆盖缺 `10-project` / `20-evidence`、package root 非 `10-project`、repo layout misuse（仓库布局误用）、cache/secrets/scratch section（缓存/密钥/临时区段）、路径越界和 ID/refs 不匹配等 fail-closed 场景。
- Happy path：tiny PackageContract（微型包合同）可构建 manifest 并定位 boardroom/package/evidence/audit roots、`full_section_path`；`model_dump` section order 稳定，重复构建稳定。
- 验证证据：`PYTHONPATH="src:." python -m pytest tests/proving/test_workspace_manifest.py -q`（46 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts/test_package_contract.py tests/proving/test_workspace_manifest.py -q`（49 passed）；`PYTHONPATH="src:." python -m pytest -q`（853 passed）。
- 边界说明：V2-060A 只定义 durable/auditable workspace manifest（可持久/可审计工作区清单）；不创建目录、不写 package 文件、不构建 SourceInventory（源码清单）、不导出 evidence（证据）。副作用检查确认未创建 `workspace/`、`00-boardroom/`、`10-project/`、`20-evidence/`、`30-audit/`。

### V2-060B PackageAssembler（项目包装配器）（2026-05-23）

- 工作包：V2-060B
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/assembler.py`、`tests/proving/test_package_assembler.py`
- 关键实现：新增 PackageArtifact（包产物）、PackageArtifactPath（包产物路径）、PackageArtifactKind（包产物类型）、PackageAssembly（项目包装配结果）和 assemble_package（装配项目包函数），基于 WorkspaceManifest（工作区清单）与 PackageContract（包合同）装配 `10-project` 视图；保持纯装配计划 + 校验边界，不创建目录、不写文件、不计算 hash、不构建 SourceInventory（源码清单）。
- Negative tests：覆盖缺 package-contract.json、缺 run-manifest.json、source artifact（源码产物）写到 package root（包根）外、unsafe path（不安全路径）、framework repo prefix（框架仓库前缀）、manifest/contract mismatch（清单/合同不匹配）、缺 README/AGENTS、软件包缺 source/test、docs_required 缺 DOC artifact（文档产物）、未知 source_surface_ref（源码实现面引用）、acceptance_ref 越界、source surface path 未覆盖、重复 artifact path（产物路径）和错误 artifact_kind（产物类型）等 fail-closed 场景。
- Happy path：tiny full-stack package（微型全栈包）包含 README、AGENTS、package-contract、run-manifest、backend/frontend/tests/docs，并生成 deterministic PackageAssembly（确定性项目包装配结果）。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/proving/test_package_assembler.py -q`（40 passed in 0.13s）；`PYTHONPATH="src;." python -m pytest tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py -q`（86 passed in 0.21s）；`PYTHONPATH="src;." python -m pytest tests/contracts/test_package_contract.py tests/negative/test_package_contract_fail_closed.py tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py -q`（101 passed in 0.18s）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py -q`（117 passed in 0.21s）。
- 边界说明：副作用检查未发现创建目录、写文件或复制文件；扫描仅命中 PurePosixPath / PureWindowsPath 路径校验。

### V2-060C SourceInventory（源码清单）（2026-05-23）

- 工作包：V2-060C
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/source_inventory.py`、`tests/negative/test_source_inventory_ref_only_rejected.py`、`tests/proving/test_source_inventory.py`；同步更新 `src/boardroom_os/workspace/__init__.py` 导出入口。
- 关键实现：新增 SourceInventory（源码清单）、SourceFileRecord（源码文件记录）、SourceLineageRecord（源码来源链记录）、SourceInventoryEntry（源码清单条目）、PackageCommitRef（包提交引用）和 build_source_inventory（构建源码清单函数）；复用 PackageAssembly（项目包装配结果）、PackageContract（包合同）、TicketId（任务 ID）、ProviderAttemptRef（模型调用尝试引用）和 VerifiedEvidenceRef（已验证证据引用）形成 implementation lineage（实现来源链）。
- Negative tests：覆盖 ref-only package assembly（只有引用的项目包装配结果）、缺 sha256、缺 producer_ticket_ref、缺 producer_attempt_ref、缺 acceptance_refs、缺 evidence_refs、unsafe source path（不安全源码路径）、重复 source/lineage path、source/lineage/package artifact 不一致、未知 source surface、surface/acceptance 覆盖不一致、scalar tuple refs（标量元组引用）和 package contract mismatch（包合同错配）等 fail-closed 场景。
- Happy path：package files（包文件）可稳定映射到 source_surface、producer ticket、provider attempt、acceptance refs 和 verified evidence refs；entries（条目）按 path 确定性排序，`model_dump()` 输出审计友好的 `{"value": ...}` ref 形状；run-manifest 由 V2-060D 证明，不进入 V2-060C SourceInventory。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/negative/test_source_inventory_ref_only_rejected.py tests/proving/test_source_inventory.py -q`（36 passed in 0.18s）；`PYTHONPATH="src;." python -m pytest tests/contracts/test_package_contract.py tests/negative/test_package_contract_fail_closed.py tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py tests/negative/test_source_inventory_ref_only_rejected.py tests/proving/test_source_inventory.py -q`（137 passed in 0.27s）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py tests/negative/test_source_inventory_ref_only_rejected.py tests/proving/test_source_inventory.py -q`（153 passed in 0.27s）。
- 边界说明：V2-060C 保持纯领域模型和 adapter-ready input shape（预留适配器输入形状），不读取文件系统、不调用 git、不写 `20-evidence`；PackageCommitRef 当前只证明非空，Git commit 存在性留给 V2-070D GitVersionAudit（Git 版本审计）。

### V2-060D RunManifest（运行清单）（2026-05-23）

- 工作包：V2-060D
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/run_manifest.py`、`tests/proving/test_run_manifest.py`；同步更新 `src/boardroom_os/workspace/__init__.py` 导出入口，并调整 `src/boardroom_os/execution/runtime_executor.py` 的 CommandRunner（命令执行器）延迟导入边界。
- 关键实现：新增 RunManifest（运行清单）、RunManifestCommand（运行清单命令）、RunManifestBinding（运行清单绑定）和 build_run_manifest / validate_run_manifest_binding（运行清单构建/绑定校验入口），把 PackageContract（包合同）的 run/test commands（运行/测试命令）转为可审计镜像，并在 CommandRunner（命令执行器）运行前提供显式绑定证明。
- Negative tests：覆盖 software/mixed package（软件/混合包）缺 run/test commands、未声明 command、manifest/contract command label/command/cwd/kind 不一致、manifest 缺/多 declared command、重复 command_id、空 command / command item / cwd、extra fields、workspace/package contract ref mismatch（工作区/包合同引用不匹配）、package root mismatch（包根不匹配）、非 canonical `10-project` package root 和伪造 run_manifest_id 均 fail closed；documentation package（文档包）可无 commands。
- Happy path：declared command 先通过 validate_run_manifest_binding（运行清单绑定校验），再以 binding.command_id 串联 CommandRunner.run（命令执行器运行），生成真实 VerificationRun（验证运行）以及 stdout/stderr refs（标准输出/错误引用）。
- 验证证据：`PYTHONPATH="src;." python -c "from boardroom_os.adapters.process_runner import CommandRunner; print(CommandRunner.__name__)"`（输出 `CommandRunner`）；`PYTHONPATH="src;." python -m pytest tests/proving/test_run_manifest.py -q`（24 passed）；`PYTHONPATH="src;." python -m pytest tests/execution/test_command_runner.py -q --basetemp=.pytest_cache/tmp-command-runner`（36 passed）；`PYTHONPATH="src;." python -m pytest tests/execution/test_command_runner.py tests/proving/test_run_manifest.py -q --basetemp=.pytest_cache/tmp-run-manifest-command-runner`（60 passed）；`PYTHONPATH="src;." python -m pytest tests/proving/test_workspace_manifest.py tests/proving/test_package_assembler.py tests/proving/test_source_inventory.py tests/proving/test_run_manifest.py -q --basetemp=.pytest_cache/tmp-workspace-proving`（112 passed）。
- 边界说明：V2-060D 不修改 CommandRunner 签名，不创建 workspace 目录，不写 `run-manifest.json`，不导出 `20-evidence`；V2-070A 已追踪后续负例：closeout 时任一 declared command 未经 RunManifestBinding 即被用作 final command evidence 必须失败。

### V2-060E WorkspaceEvidenceExport（工作区证据导出）（2026-05-23）

- 工作包：V2-060E
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/evidence_export.py`、`tests/proving/test_workspace_evidence_export.py`、`doc/04-implementation/v2-060e-workspace-evidence-export-spec.md`、`doc/04-implementation/v2-060e-workspace-evidence-export-implementation-plan.md`；同步更新 `src/boardroom_os/workspace/__init__.py` 导出入口。
- 关键实现：新增 WorkspaceEvidenceBundle（工作区证据包）、EvidenceBundleArtifact（证据包产物）、EvidenceBundleArtifactPath（证据包产物路径）、EvidenceBundleArtifactKind（证据包产物类型）和 build_workspace_evidence_bundle（构建工作区证据包函数），把 WorkspaceManifest（工作区清单）、PackageAssembly（项目包装配结果）、SourceInventory（源码清单）、RunManifest（运行清单）、VerificationRun（验证运行）、VerifiedEvidence（已验证证据）与 FinalEvidenceTable（最终证据表）同步为 deterministic `20-evidence` bundle plan（确定性证据包计划）。
- Negative tests：覆盖 FinalEvidenceTable missing / failed rows、complete 派生不一致、SourceInventory 空 entries 和缺 lineage 字段、SourceInventory/package assembly mismatch、VerificationRun 缺 stdout_ref 模型层失败、failed status、重复 verification_run_id、RunManifest workspace mismatch、INV-X1 source inventory evidence refs 未被 final table 承认、FinalEvidenceTable orphan VerifiedEvidenceRef、INV-X2 orphan VerificationRun、非 `20-evidence` artifact path 和伪造 closeout_ready without artifacts 均 fail closed。
- Happy path：生成五类 logical artifacts（逻辑产物）：`20-evidence/source-inventory/source-inventory.json`、`20-evidence/tests/verification-runs.json`、`20-evidence/tests/run-manifest.json`、`20-evidence/closeout/final-evidence-table.json`、`20-evidence/closeout/evidence-bundle-manifest.json`；refs 在 source inventory / verified evidence / verification runs / final evidence table 间闭合，bundle id、artifact order 和 `model_dump()` 均确定性稳定。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/proving/test_workspace_evidence_export.py -q`（17 passed in 0.22s）；`PYTHONPATH="src;." python -m pytest tests/proving/test_workspace_evidence_export.py tests/proving/test_source_inventory.py tests/proving/test_run_manifest.py tests/evidence/test_final_evidence_table.py tests/evidence/test_evidence_verifier.py -q`（65 passed in 0.25s）；`PYTHONPATH="src;." python -m pytest tests/proving -q`（129 passed in 0.28s）。
- 边界说明：V2-060E 保持纯领域模型边界，不创建目录、不写 `20-evidence` JSON、不复制 stdout/stderr、不重新运行 CommandRunner（命令执行器）、不调用 git、不重新验证 EvidenceClaim（证据声明）；`20-evidence/closeout/` 仅占用 final evidence table 与 evidence bundle manifest 两个路径，其它 closeout 命名留给 V2-070。

### V2-060F AgentAssetImport（智能体资产导入）（2026-05-23）

- 工作包：V2-060F
- 状态：DONE
- 关键产出文件：`src/boardroom_os/workspace/agent_asset_import.py`、`tests/proving/test_agent_asset_import.py`、`doc/04-implementation/v2-060f-agent-asset-import-spec.md`、`doc/04-implementation/v2-060f-agent-asset-import-implementation-plan.md`；同步更新 `src/boardroom_os/workspace/__init__.py` 与 `doc/04-implementation/INDEX.md`。
- 关键实现：新增 AgentAssetImportManifest（智能体资产导入清单）、AgentAssetImportBatch（智能体资产导入批次）、AgentAssetImportEntry（智能体资产导入条目）、AgentAssetMaterializationResult（智能体资产物化结果）、validate_agent_asset_registry_bindings（校验智能体资产注册表绑定函数）、dump_agent_asset_import_manifest（导出资产导入清单函数）和 materialize_agent_assets（物化智能体资产函数），将本地 role/skill/prompt/MCP 资产物化为 `00-boardroom/agents/` 快照并生成 canonical JSON 内容的 `asset-import-manifest.yaml`。
- Negative tests：覆盖 unsafe source/target/manifest paths、Windows drive/backslash、缺 manifest/batch/entry 字段、naive imported_at、sha256 mismatch、source missing/directory/symlink、registry binding gap、target overwrite、existing manifest 非 prefix、mutated historical batch 和 historical target missing/changed 均 fail closed。
- Happy path：四类 role/skill/prompt/MCP asset 可写入快照；项目可只导入实际使用的 asset kind 子集；相同 manifest 幂等重跑；追加新 source_ref batch 不改写历史；canonical JSON dump 稳定且不包含 host absolute source/workspace path。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/proving/test_agent_asset_import.py -q --basetemp=.pytest-tmp`（45 passed, 2 skipped）；`PYTHONPATH="src;." python -m pytest tests/proving -q --basetemp=.pytest-tmp-proving`（174 passed, 2 skipped）；`PYTHONPATH="src;." python -m pytest tests/execution/test_execution_package_compiler.py tests/execution/test_agent_profiles.py tests/proving/test_agent_asset_import.py -q --basetemp=.pytest-tmp-compiler`（58 passed, 2 skipped）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/negative -q --basetemp=.pytest-tmp-all`（1015 passed, 2 skipped）。
- 边界说明：ExecutionPackage compiler（执行包编译器）保持 0 外部文件输入；V2-060F 是 workspace/package 阶段的可审计物化边界，不解析资产语义、不远端 fetch、不支持 force overwrite、不调用 git、不进入 closeout。

### V2-070A CloseoutGate（收尾门禁）（2026-05-24）

- 工作包：V2-070A
- 状态：DONE
- 关键产出文件：`src/boardroom_os/closeout/gate.py`、`src/boardroom_os/closeout/__init__.py`、`tests/negative/test_closeout_fail_closed.py`、`tests/closeout/test_closeout_gate.py`；同步更新 `doc/04-implementation/backlog.md` 与 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：新增 CloseoutGateInput（收尾门禁输入）、CloseoutGateResult（收尾门禁结果）、CloseoutGateBlocker（收尾门禁阻断项）、ReplayBundleReadiness（重放包就绪摘要）、GitAuditReadiness（Git 审计就绪摘要）、ProcessAuditReadiness（流程审计就绪摘要）和 CloseoutCommandEvidenceBinding（收尾命令证据绑定）；gate（门禁）保持纯领域判定，不读文件系统、不调用 git、不运行命令，只消费 typed facts（类型化事实）。
- Negative tests：覆盖缺 replay readiness、replay/hash chain false、FinalEvidenceTable（最终证据表）不完整/缺 acceptance map（验收映射）、CheckerVerdict（检查结论）open blocker、ProviderAttemptRef（模型调用尝试引用）为空或不闭合、git dirty、source inventory hash mismatch、final command evidence 不在 final commit、VerificationRun（验证运行）失败/重复/为空、缺 RunManifestBinding（运行清单绑定）、SourceInventory.package_commit_ref 与 Git final commit 不一致、orphan VerifiedEvidence（孤儿已验证证据）、WorkspaceEvidenceBundle（工作区证据包）不可收尾或 artifact kind 缺失、ProcessAuditReadiness（流程审计就绪摘要）不完整均 fail closed。
- Happy path：所有 closeout 输入 ready 时 verdict 为 passed；CheckerVerdict 的 non-blocking notes（非阻断备注）不阻断；blocked result 仍保留 checked_refs（已检查引用集合），并包含 replay summary hash、git final commit、source inventory hash、10 项 process audit artifact paths 和 fallback_decision_record_ref（降级判定记录引用）。
- 验证证据：先运行 `PYTHONPATH=src pytest tests/negative/test_closeout_fail_closed.py -q` 得到预期 RED（`ModuleNotFoundError: No module named 'boardroom_os.closeout.gate'`）；实现后 `PYTHONPATH=src pytest tests/negative/test_closeout_fail_closed.py tests/closeout/test_closeout_gate.py -q` 通过（27 passed）；相邻回归 `PYTHONPATH=src pytest tests/negative/test_closeout_fail_closed.py tests/closeout/test_closeout_gate.py tests/proving/test_workspace_evidence_export.py tests/reducers/test_completion_gate_with_evidence.py tests/evidence/test_final_evidence_table.py tests/evidence/test_checker_verdict.py -q` 通过（129 passed）；全量 `PYTHONPATH="src;." pytest -q --basetemp=.pytest_tmp` 通过（1042 passed, 2 skipped）。
- 边界说明：V2-070A 只实现最后收尾前的消费型 gate，不构建 replay bundle（V2-070B）、不生成 30-audit 产物（V2-070C）、不读取 git（V2-070D）、不创建 CloseoutPackage（V2-070E）、不推进 reducer terminal success（V2-070F）。

### V2-070B ReplayBundle（重放包）（2026-05-24）

- 工作包：V2-070B
- 状态：DONE
- 关键产出文件：`src/boardroom_os/audit/replay_bundle.py`、`src/boardroom_os/audit/__init__.py`、`tests/closeout/test_replay_bundle.py`、`doc/04-implementation/v2-070b-replay-bundle-spec.md`；同步更新 `doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 与 `doc/04-implementation/INDEX.md`。
- 关键实现：新增 ReplayBundle（重放包）、ReplayAttestation（重放证明条目）、ReplayPayloadManifest（重放载荷清单）、ReplayArtifactManifest（重放产物清单）、ReplayHashManifest（重放哈希清单）、ReplayReport（重放报告）、ReplayEventWindow（重放事件窗口）、ReplayEventHashNode（重放事件哈希节点）、build_replay_bundle（构建重放包函数）和 replay_bundle_readiness（重放包就绪摘要投影函数）。首版采用轻量 attestations（证明条目）列表但只允许 `seat_assignment_graph`（席位分配图），复用 CloseoutGate（收尾门禁）的 ReplaySummaryHash / EventRangeRef / ProjectionVersionRef 类型，不定义 plugin registry（插件注册表）或未实现 projection 分支。
- Negative tests：覆盖缺 event range、projection version mismatch（投影版本不匹配）、projection kind mismatch（投影类型不匹配）、projection_summary.graph_version mismatch、artifact/hash/report 缺失、payload manifest 缺载荷、event graph gap / unordered / project mismatch、placeholder 或 synthetic sha256、unsafe content_ref、summary_hash 非 sha256、attestation/hash_manifest/event_window_hash 不闭合，以及 event_hash（事件哈希）被篡改后同步重算 chain/hash_manifest/artifact hashes 仍失败、EVENT_WINDOW content_ref（事件窗口内容引用）被替换为安全但错误 ref 后同步重算仍失败。
- Happy path：事件 + payload manifest + artifact manifest 输入可构造 replay bundle；bundle 归档 EventRecord（事件记录）切片，readiness 从 canonical EventRecord（规范事件记录）重算 event hash chain（事件哈希链）、terminal_event_chain_hash（终端事件链哈希）、event_window_hash（事件窗口哈希）、payload/artifact/replay report/attestation hashes，并返回 CloseoutGate 可消费的 ReplayBundleReadiness（重放包就绪摘要）。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/closeout/test_replay_bundle.py -q` 通过（59 passed）；`PYTHONPATH="src;." python -m pytest tests/reducers/test_projection_replay.py tests/closeout/test_replay_bundle.py -q` 通过（70 passed）；`PYTHONPATH="src;." python -m pytest tests/negative/test_closeout_fail_closed.py tests/closeout/test_closeout_gate.py tests/closeout/test_replay_bundle.py -q` 通过（86 passed）；完整套件首次因 Windows pytest 临时目录权限失败，改用 `--basetemp=/tmp/boardroom-os-pytest` 后 `PYTHONPATH="src;." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=/tmp/boardroom-os-pytest` 通过（1101 passed, 2 skipped）。
- 边界说明：V2-070B 只构建 replay bundle 与 readiness，不生成 30-audit 人类可读产物（V2-070C）、不读取 git（V2-070D）、不创建 CloseoutPackage（V2-070E）、不推进 reducer terminal success（V2-070F）；hash chain / hash manifest 留在 replay bundle 层，不回填 InMemoryEventLog（内存事件日志）。

### V2-070C ProcessAudit（流程审计）（2026-05-24）

- 工作包：V2-070C
- 状态：DONE
- 关键产出文件：`src/boardroom_os/audit/process_audit.py`、`src/boardroom_os/audit/__init__.py`、`tests/closeout/test_process_audit_artifacts.py`、`tests/closeout/test_process_audit.py`。
- 关键实现：新增 ProcessAuditBundle（流程审计包）、ProcessAuditArtifact（流程审计产物）、ProcessAuditArtifactManifest（流程审计产物清单）、ProcessAuditHashManifest（流程审计哈希清单）、ProcessAuditReport（流程审计报告）、build_process_audit_bundle（构建流程审计包函数）和 process_audit_readiness（流程审计就绪摘要投影函数），以 V2-070B ReplayBundle（重放包）一致风格物化 `30-audit/` 十项流程审计产物，并重新闭合 content_ref（内容引用）、sha256（内容哈希）、artifact manifest（产物清单）、hash manifest（哈希清单）和 CloseoutGate（收尾门禁）可消费的 ProcessAuditReadiness（流程审计就绪摘要）。
- Negative tests：覆盖十项必需 artifact 缺失、额外/重复/unsafe path、timeline key event 缺失、decision log 缺 CEO / human board decision、agent context index 缺 execution package / model profile / provider attempts、主 artifact lineage（产物来源链）与 fallback lineage（降级来源链）缺失、evidence map（证据映射）与 FinalEvidenceTable（最终证据表）不一致、git version audit markdown 缺 final commit / dirty status / source inventory hash、replay bundle report mismatch 和 hash manifest mismatch。
- Happy path：证明 bundle materialization（包物化）、hash manifest closure（哈希清单闭合）、CloseoutGate readiness projection（收尾门禁就绪投影）、audit-friendly JSON（审计友好 JSON）、report indexing（报告索引）、标准 Markdown human readability（人类可读 Markdown）、真实 EventRecord timeline projection（事件记录时间线投影）、typed builder boundary（类型化构建输入边界）以及 JSON/Markdown artifact format（产物格式）划分。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/closeout/test_process_audit_artifacts.py tests/closeout/test_process_audit.py -q` 通过（41 passed in 0.53s）；`PYTHONPATH="src;." python -m pytest tests/negative/test_closeout_fail_closed.py tests/closeout/test_replay_bundle.py tests/closeout/test_process_audit_artifacts.py tests/closeout/test_process_audit.py -q` 通过（124 passed in 0.66s）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2070c-final` 通过（1142 passed, 2 skipped in 1.91s）。
- 边界说明：V2-070C 只构建内存 typed object（类型化对象）和 readiness projection（就绪投影），不写 filesystem（文件系统）、不调用 git（V2-070D）、不创建 CloseoutPackage（收尾包，V2-070E）、不推进 reducer terminal success（归约器终态成功，V2-070F）。

### V2-070D GitVersionAudit（Git 版本审计）（2026-05-24）

- 工作包：V2-070D
- 状态：DONE
- 关键产出文件：`src/boardroom_os/audit/git_version_audit.py`、`src/boardroom_os/adapters/git_audit.py`、`tests/closeout/test_git_version_audit.py`、`doc/04-implementation/v2-070d-git-version-audit-spec.md`；同步更新 `src/boardroom_os/audit/__init__.py`、`doc/04-implementation/backlog.md` 与 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：新增 GitVersionAuditBundle（Git 版本审计包）、GitVersionAuditFactSet（Git 版本审计事实集）、GitCommandEvidenceBinding（Git 命令证据绑定）、GitVersionAuditReport（Git 版本审计报告）、GitVersionAuditHashManifest（Git 版本审计哈希清单）、build_git_version_audit_bundle（构建 Git 版本审计包函数）、git_version_audit_readiness（Git 版本审计就绪摘要投影函数）和 GitAuditAdapter（Git 审计适配器）。核心 builder（构建器）只消费 typed facts（类型化事实），adapter（适配器）只通过可注入 transport（传输层）采集 read-only Git facts（只读 Git 事实）。
- Negative tests：覆盖 dirty package（脏项目包）、source inventory hash mismatch（源码清单哈希不匹配）、final command evidence（最终命令证据）不在 final commit（最终提交）、SourceInventory.package_commit_ref（源码清单项目提交引用）不匹配、缺/孤儿 command evidence binding（命令证据绑定）、未声明 RunManifest command（运行清单命令）、失败 VerificationRun（验证运行）、hash manifest mismatch（哈希清单不匹配）、raw dict / scalar tuple input（原始字典/标量元组输入）、无时区 generated_at（生成时间）、placeholder source inventory hash（占位源码清单哈希）、git command failure（Git 命令失败）和 write git command（写 Git 命令）均 fail closed。
- Happy path：clean package commit（干净项目包提交）可生成 GitVersionAuditBundle，并投影为 CloseoutGate（收尾门禁）可消费的 GitAuditReadiness（Git 审计就绪摘要）；source_inventory_hash（源码清单哈希）确定性重算，hash manifest（哈希清单）闭合，JSON 不含宿主绝对路径 / Windows drive / 反斜杠 / 旧 runtime path；GitAuditAdapter 可从 transport 采集 final commit、branch_ref、tag、dirty status 和 diff summary。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/closeout/test_git_version_audit.py -q` 通过（21 passed in 0.32s）；`PYTHONPATH="src;." python -m pytest tests/closeout -q` 通过（124 passed in 0.71s）；`PYTHONPATH="src;." python -m pytest tests/negative -q` 通过（304 passed in 0.43s）；初次 proving/full run 受 Windows pytest temp 权限阻断，改用仓库内 basetemp 后 `PYTHONPATH="src;." python -m pytest tests/proving -q --basetemp=.pytest-tmp-v2-070d-proving` 通过（174 passed, 2 skipped in 0.56s），`PYTHONPATH="src;." python -m pytest tests -q --basetemp=.pytest-tmp-v2-070d-all` 通过（1163 passed, 2 skipped in 1.83s）。
- 边界说明：V2-070D 不创建 CloseoutPackage（收尾包，V2-070E）、不推进 reducer terminal success（归约器终态成功，V2-070F）、不写文件系统、不修改 Git 仓库；transport 层可接收宿主 cwd（当前目录）执行只读命令，但 typed object（类型化对象）只保留审计友好的逻辑路径。

### V2-070E CloseoutPackage（收尾包）（2026-05-25）

- 工作包：V2-070E
- 状态：DONE
- 关键产出文件：`src/boardroom_os/closeout/package.py`、`tests/closeout/test_closeout_package.py`、`doc/04-implementation/v2-070e-closeout-package-spec.md`；同步更新 `src/boardroom_os/closeout/__init__.py`、`src/boardroom_os/audit/process_audit.py`、`tests/closeout/test_process_audit.py` 和 `tests/closeout/test_process_audit_artifacts.py`。
- 关键实现：新增 CloseoutPackage（收尾包）V1 typed envelope（类型化封套）与 `build_closeout_package`（构建收尾包函数），把 CloseoutGateResult（收尾门禁结果）、SourceInventory（源码清单）、FinalEvidenceTable（最终证据表）、ReplayBundle（重放包）、ProcessAuditBundle（流程审计包）和 GitVersionAuditBundle（Git 版本审计包）强一致绑定；`closeout_package_id` 由 canonical JSON/hash（规范 JSON/哈希）确定性派生；`checked_refs`（检查引用）采用 append-then-dedupe（追加后去重）保持稳定唯一完整；`CloseoutPackage` V1 只接受 passed verdict（通过结论）。ProcessAudit（流程审计）同步升级为 required 消费完整 GitVersionAuditBundle，并从完整 bundle 渲染 `30-audit/git-version-audit.md`。
- Negative tests：CloseoutPackageBuilderInput（收尾包构建输入）拒绝 raw dict（原始字典）typed inputs、scalar checked_refs（标量检查引用）、blocked gate result（阻断门禁结果）、verdict mismatch（结论不一致）、version 非 1、naive generated_at（无时区时间）、closeout_gate_result_id 重算不一致、package_commit_ref mismatch（包提交引用不一致）、git/replay/process readiness bundle mismatch（就绪摘要与完整包不一致）、gate checked_refs gap（门禁检查引用缺口）、audit bundle project_ref mismatch（审计包项目引用不一致）和 unsafe refs/paths（不安全引用/路径）。
- Happy path：passed CloseoutPackage 可稳定绑定完整 closeout/audit/evidence/replay/source inventory/git version audit 产物，`checked_refs` 稳定唯一完整，JSON 不包含宿主绝对路径、Windows drive、反斜杠、`.pytest` 或旧 runtime path。
- 验证证据：`powershell -Command '$env:PYTHONPATH="src;."; python -m pytest tests/closeout/test_closeout_package.py -q'` 通过（18 passed）；`powershell -Command '$env:PYTHONPATH="src;."; python -m pytest tests/closeout/test_closeout_package.py tests/closeout/test_closeout_gate.py tests/negative/test_closeout_fail_closed.py -q'` 通过（45 passed）；`powershell -Command '$env:PYTHONPATH="src;."; python -m pytest tests/closeout/test_process_audit.py tests/closeout/test_process_audit_artifacts.py tests/closeout/test_git_version_audit.py tests/closeout/test_closeout_package.py -q'` 通过（83 passed）；`powershell -Command '$env:PYTHONPATH="src;."; python -m pytest tests/closeout/test_replay_bundle.py tests/closeout/test_process_audit.py tests/closeout/test_process_audit_artifacts.py tests/closeout/test_git_version_audit.py tests/closeout/test_closeout_package.py -q'` 通过（142 passed）；默认 `%TEMP%` 目录权限导致 pytest tmp_path setup 报 `PermissionError: [WinError 5]` 后，改用仓库本地临时目录环境变量复跑 `powershell -Command '$tmp = Join-Path (Get-Location) ".pytest-tmp"; New-Item -ItemType Directory -Force -Path $tmp | Out-Null; $env:TMP=$tmp; $env:TEMP=$tmp; $env:PYTHONPATH="src;."; python -m pytest tests/proving/test_source_inventory.py tests/negative/test_source_inventory_ref_only_rejected.py tests/proving/test_run_manifest.py tests/execution/test_command_runner.py tests/closeout/test_closeout_package.py -q'` 通过（114 passed）；同样使用本地临时目录运行 `powershell -Command '$tmp = Join-Path (Get-Location) ".pytest-tmp"; New-Item -ItemType Directory -Force -Path $tmp | Out-Null; $env:TMP=$tmp; $env:TEMP=$tmp; $env:PYTHONPATH="src;."; python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q'` 通过（1184 passed, 2 skipped）。

- 边界说明：V2-070E 不改变 CloseoutGate（收尾门禁）API，不提前实现 V2-070F CloseoutReducer（收尾归约器），不引入 per-source-ticket Git audit（逐源码任务 Git 审计）；GitAuditReadiness（Git 审计就绪摘要）仍由 CloseoutGate 消费，CloseoutPackage 只绑定完整 GitVersionAuditBundle 作为持久审计对象。

### V2-070F CloseoutReducer（收尾归约器）（2026-05-25）

- 工作包：V2-070F
- 状态：DONE
- 关键产出文件：`src/boardroom_os/reducers/closeout_reducer.py`、`tests/closeout/test_closeout_reducer.py`、`doc/04-implementation/v2-070f-closeout-reducer-spec.md`、`doc/04-implementation/v2-070f-closeout-reducer-implementation-plan.md`；同步更新 `src/boardroom_os/events/types.py`、`src/boardroom_os/execution/runtime_executor.py` 和 `doc/04-implementation/INDEX.md`。
- 关键实现：新增 `EventType.CLOSEOUT_COMMITTED`（收尾已提交事件）作为 governance event（治理事件），由 `CloseoutReducer`（收尾归约器）消费 `CloseoutCommitPayload`（收尾提交载荷）与 typed `CloseoutPackage`（类型化收尾包），在显式 `WORK_PRODUCT_SUBMITTED`（工作产物已提交）历史存在且 payload/package refs（载荷/包引用）闭合后，产出 `CloseoutProjection.terminal_status=succeeded`（收尾投影终态成功）。`RuntimeEventBoundary`（运行时事件边界）继续拒绝 runtime emit（运行时发出）`CLOSEOUT_COMMITTED`。
- Negative tests：runtime/executor closeout（运行时/执行器收尾）、workflow completed 替代 closeout、缺 work product history（工作产物历史）、增量 base history（基准历史）缺 `WORK_PRODUCT_SUBMITTED`、只存在 ticket completed refs（任务完成引用）、重复 closeout、已 succeeded base history 再 closeout、graph_version 乱序、cross project（跨项目）、缺 closeout gate binding（收尾门禁绑定）、缺 replay bundle binding（重放包绑定）、source/evidence/process/git/package commit ref mismatch（引用不一致）、closeout event 早于 package graph_version、raw dict payload/package（原始字典载荷/包）、checked_refs gap（检查引用缺口）和非 passed terminal verdict（非通过提交结论）均 fail closed。
- Happy path：当前 events（事件）包含 `WORK_PRODUCT_SUBMITTED` + 合法 `CLOSEOUT_COMMITTED` 时可投影为 project terminal success（项目终态成功）；增量 replay/reducer 输入只包含 `CLOSEOUT_COMMITTED` 时，必须通过 `CloseoutHistoryProjection`（收尾历史投影）显式携带 work product refs（工作产物引用）才能成功；projection JSON（投影 JSON）保持审计友好，不含宿主绝对路径、Windows drive、反斜杠、`.pytest` 或旧 runtime path。
- 验证证据：`PYTHONPATH="src;." python -m pytest tests/closeout/test_closeout_reducer.py -q` 通过（24 passed in 1.09s）；`PYTHONPATH="src;." python -m pytest tests/closeout/test_closeout_reducer.py tests/closeout/test_closeout_package.py tests/closeout/test_closeout_gate.py tests/negative/test_closeout_fail_closed.py -q` 通过（69 passed in 1.57s）；`PYTHONPATH="src;." python -m pytest tests/reducers/test_completion_gate_with_evidence.py tests/reducers/test_ticket_reducer_transitions.py tests/closeout/test_closeout_reducer.py -q` 通过（64 passed in 0.99s）；`PYTHONPATH="src;." python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2070f-all` 通过（1208 passed, 2 skipped in 3.39s）。
- 边界说明：V2-070F 不重新运行 CloseoutGate（收尾门禁）、不重新审计 evidence/replay/process/git bundle（证据/重放/流程/Git 包），不引入 `PROJECT_COMPLETED`（项目已完成）正式事件；terminal success（终态成功）只能由 `CLOSEOUT_COMMITTED` 治理事件 + passed CloseoutPackage（通过收尾包）经 reducer 投影得到。

### V2-070 外部独立审计 + 设立 V2-071 fact-chain 重构阶段（2026-05-25）

- 范围：跨 V2-070A~G 的事实链 / 隐式 fallback / 命名空间 / 确定性哈希审查，由外部独立审计在 V2-070G 修补批次合并后执行。
- 关键产出：审计报告 `doc/04-implementation/v2-070-batch-review-report.md`（用户提供，作为本次决策的事实输入）；DEC-0016 / DEC-0017；`backlog.md` 新增 Phase 7.5 / V2-071A~F 共 6 个工作包；`acceptance-criteria.md` Phase 7 验收段追加"重审说明"并新增 Phase 7.5 验收段；抽象 AC 层新增 AC-V2-CLOSEOUT-004 ~ AC-V2-CLOSEOUT-010 共 7 条原则；`v2-080A` 依赖追加 `v2-071F`。
- 审计识别的 18 项缺口分类：
  - **P0 结构性问题（5 项）**：ProcessAudit 与 CloseoutPackage 构造环（ProcessAudit 要求事件流含 `closeout_committed`，CloseoutPackage 又要求先有 ProcessAuditBundle）；ReplayBundle 接受外部 `ProjectionReplaySummary` 内容字段未从 events 重新投影；ProcessAudit.events 与 ReplayBundle.events 仅校验首尾未做内容级一致性比对；GitAuditAdapter `base_commit_sha or final_commit_sha` 与 `worktree_ref or f"worktree.{package_root}"` 隐式 fallback（直接违反 CLAUDE.md `No silent fallbacks` 硬规则）；CloseoutPackage.graph_version 允许超过 ReplayBundle 已证明的 last_graph_version。
  - **P1 事实绑定 / 占位 fallback（5 项）**：ReplayPayloadManifest.entries[*].sha256 未通过 payload resolver 重算；ProcessAudit `getattr(ticket, "status", "unknown")` / `getattr(ticket, "owner_seat_ref", "unknown")` 占位；artifact-lineage.json `consumer_ticket_ref` 直接等于 `producer_ticket_ref`（伪事实）；`expected_fallback_decision_refs` 为空时不拒绝额外 `fallback_lineages`；`fact_set_id` / `artifact_ref` / `content_ref` 缺 project / run / hash 命名空间，存在跨 bundle 串包风险。
  - **P2 确定性 / 解析正确性（5 项）**：GitVersionAudit `verification_runs` / `command_evidence_bindings` 未 canonical sort；Replay payload/artifact manifest entries 未 canonical sort；ProcessAudit `provider_attempt_refs` / `verification_runs` / `verified_evidence` 顺序影响 `checked_refs`；`git status --porcelain` 未使用 `-z`，C-style quoting 文件名解析错误；`git diff --stat` regex 未锚定 summary footer，含 "insertions" 关键字的文件名可污染统计；`AgentContextIndexEntry` 真实字段在 `entry.snapshot.*` 下，ProcessAudit 通过 `getattr(entry, "execution_package_ref", None)` 取顶层平铺字段（V2-070G spec P2-11 已记录但未落地修复）。
  - **额外发现（3 项）**：`provider_attempt_refs` 子集校验缺失（V2-070G P2-12）；SHA value object 不强制格式 + placeholder 拒绝白名单硬编码（V2-070G P2-13/15）；`GitAuditAdapter._require_read_only_command` 仅对 `diff` 拦截 `--output`，`git show --output=` / `git log --output-indicator-new=` 仍可写文件（V2-070G P2-14）。
- 决策路径选择：审计报告给出三条路线 — (A) 18 项串行补丁，(B) 推倒重写 V2-070，(C) 有边界事实链重构 + 串行修复。本会话采纳 (C)：保留 V2-070A~G DONE 状态作为各包独立交付依据，整体 Phase 7 进入"待重构"，新增 Phase 7.5 / V2-071A~F 6 个工作包专门负责事实链权威源、ReplayBundle re-replay、ProcessAudit 解构构造环、Git 审计强化、CloseoutPackage 边界严格化和端到端 fail-closed 回归。
- V2-071 工作包概览：
  - **V2-071A**：事实链权威源设计 + 跨包命名空间 helper（`src/boardroom_os/contracts/refs.py`），冻结构造顺序与禁止外部传入字段清单。
  - **V2-071B**：ReplayBundle re-replay 重构（删除 `ReplayBundleBuilderInput.projection_summary`，builder 内部调用 `ProjectionReplay.replay_events(...)`，manifest entries canonical sort）。
  - **V2-071C**：ProcessAudit 解构构造环（移除 `closeout_committed` 必需事件、强制复用 `replay_bundle.events`、消除 `unknown` 占位、修正 `consumer_ticket_ref`、fallback 排他校验、checked_refs canonical sort）。
  - **V2-071D**：Git 审计强化（GitAuditAdapter 强制显式 `base_commit_sha` / `worktree_ref`，`git status --porcelain=v1 -z`，`git diff --shortstat`，verification_runs / command_evidence_bindings canonical sort）。
  - **V2-071E**：CloseoutPackage 边界严格化（graph_version 严格 == replay_last_graph_version，引入 `ReplayPayloadResolver` 校验 payload sha256，所有跨包引用使用 V2-071A 命名空间 helper）。
  - **V2-071F**：端到端 fact-chain 集成测试 + 18 项 P0/P1/P2 逐项回归 + Phase 7 重锁 + Phase 8 解锁。
- 进度总览影响：Phase 7 从 6/6 完成保留，但增加"待重构"说明；新增 Phase 7.5 0/6；总数 53 → 59；当前未完成工作包 V2-080A → V2-071A；Phase 8 进入前置增加"V2-071F 闭合 + 18 项缺口逐项 negative test 通过 + 端到端集成测试通过"。
- 不在本会话执行：本会话仅完成文档补齐（backlog / acceptance-criteria / decisions / 本日志 / INDEX / 6 份 spec 文件），不修改 V2-070 任何源码或测试；V2-071A~F 代码实施在后续会话按工作包顺序推进。

### 2026-05-25 — V2-071A Fact-chain 设计与引用 helper

- 工作包：V2-071A（事实链权威源设计 + 跨包命名空间 helper）
- 关键产出：`doc/04-implementation/v2-071a-fact-chain-design-spec.md`、`src/boardroom_os/contracts/refs.py`、`tests/contracts/test_namespaced_refs.py`、`tests/negative/test_namespaced_refs_fail_closed.py`
- Negative tests：拒绝 unsafe namespace segment、短/占位 SHA-256、无 `run_id` 的 `extra_suffix` 单尾段、非 dict / 非 JSONable payload、重复 canonical sort key、非字符串 sort key。
- Happy path：同一 namespaced ref 输入稳定、不同 hash / run_id / extra_suffix 产生不同 ref、乱序集合 canonical sort 后稳定、BaseModel / StrEnum / tuple/list payload canonicalization 稳定、UTF-8 payload canonical hash 稳定。
- 验证：`PYTHONPATH="src:." python -m pytest tests/contracts/test_namespaced_refs.py tests/negative/test_namespaced_refs_fail_closed.py -q --basetemp=.pytest-tmp-v2071a-review-final-focused` 通过（22 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/closeout -q --basetemp=.pytest-tmp-v2071a-review-regression` 通过（257 passed）。

### 2026-05-26 — V2-071B ReplayBundle re-replay

- 工作包：V2-071B（ReplayBundle re-replay，从 events 重新投影）
- 关键产出：`doc/04-implementation/v2-071b-replay-bundle-rereplay-spec.md`、`src/boardroom_os/audit/replay_bundle.py`、`tests/closeout/test_replay_bundle_rereplay.py`、`tests/negative/test_replay_bundle_external_summary_rejected.py`；同步迁移 `tests/closeout/test_replay_bundle.py` 与下游 ProcessAudit（流程审计）fixture。
- 关键实现：删除外部 `projection_summary` 输入，ReplayBundle（重放包）内部调用 `ProjectionReplay.replay_events(...)` 重新投影 `ProjectionReplaySummary`（投影回放摘要）；payload/artifact manifest entries（载荷/产物清单条目）使用 canonical sort（规范排序）后参与 hash；`replay_bundle_id` 改为基于 `namespaced_ref` 的 project/run/hash 命名空间引用。
- Negative tests：`ReplayBundleBuilderInput` 拒绝调用方传入 `projection_summary`，且 `model_copy(update=...)` 注入额外 `projection_summary` 也会被构建入口拒绝；events payload 篡改会改变内部重投影 summary/hash；payload_manifest / artifact_manifest 输入乱序仍产生稳定 hash，不再因输入顺序影响 bundle id 或 manifest hash。
- Happy path：同一组 events 可稳定构造 ReplayBundle，`ReplayReport`（重放报告）与 attestation（证明条目）只使用内部重投影的 `summary_hash`，不再存在 external projection_summary 第二事实源。
- 验证：`PYTHONPATH="src:." python -m pytest tests/closeout/test_replay_bundle.py tests/closeout/test_replay_bundle_rereplay.py tests/negative/test_replay_bundle_external_summary_rejected.py -q --basetemp=.pytest-tmp-v2071b-final-targeted` 通过（74 passed）；`PYTHONPATH="src:." python -m pytest tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071b-final-closeout-negative` 通过（519 passed）；`PYTHONPATH="src:." python -m pytest tests/contracts tests/reducers tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071b-final-full` 通过（732 passed）。

### 2026-05-27 — V2-071C ProcessAudit fact-chain hardening

- 工作包：V2-071C（ProcessAudit 解构构造环 + events 单一来源）
- 关键产出：`doc/04-implementation/v2-071c-process-audit-fact-chain-spec.md`、`src/boardroom_os/audit/process_audit.py`、`src/boardroom_os/workspace/source_inventory.py`、`tests/closeout/test_process_audit_fact_chain.py`、`tests/negative/test_process_audit_construction_loop_rejected.py`；同步调整 `tests/closeout/test_process_audit.py` 与 `tests/closeout/test_process_audit_artifacts.py` fixture。
- 关键实现：ProcessAudit（流程审计）删除外部 `events` 输入并统一复用 `ReplayBundle.events`（重放包事件）；不再要求 `CLOSEOUT_COMMITTED`（收尾已提交）事件；ticket graph（任务图）与 AgentContextIndex（智能体上下文索引）缺字段 fail closed，不再生成 `"unknown"` / `"ticket"` 占位；SourceInventory（源码清单）显式保存 `consumer_ticket_refs`（消费任务引用集合），artifact-lineage.json 分离 producer/consumer；fallback lineage（降级来源链）actual refs 与 expected fallback decisions 必须完全一致；集合语义输入 canonical sort（规范排序）后进入 `checked_refs`（已检查引用）与 hash。
- Negative tests：覆盖外部 `events` 字段被拒绝、无 `CLOSEOUT_COMMITTED` 时可先构造 ProcessAudit、ReplayBundle event window gap / graph_version 非严格递增失败、ticket graph / AgentContextIndex 缺字段失败、缺 consumer ticket refs 失败、consumer 不得默认等于 producer、fallback lineage expected/actual 不一致失败、集合语义输入乱序仍稳定。
- Happy path：ProcessAudit 可在 CloseoutPackage（收尾包）之前构造；timeline 与 `ReplayBundle.events` 的 event id / type / graph_version / actor / payload refs 完全一致；artifact-lineage 明确分离 producer ticket 与 consumer tickets；ticket graph markdown 与 agent context index 使用真实字段；`checked_refs` 与相关 hash 在乱序输入下稳定。
- 验证：`PYTHONPATH="src:." python -m pytest tests/negative/test_process_audit_construction_loop_rejected.py tests/closeout/test_process_audit_fact_chain.py tests/closeout/test_process_audit.py tests/closeout/test_process_audit_artifacts.py -q --basetemp=.pytest-tmp-v2071c-audit` 通过（125 passed in 1.30s）；`PYTHONPATH="src:." python -m pytest tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071c-audit-regression` 通过（602 passed in 2.46s）。全量套件随后暴露一个非 V2-071C 遗留失败：V2-060F AgentAssetImport（智能体资产导入）symlink（符号链接）检测先落入 sha256 mismatch，本轮继续修复。

### 2026-05-27 — V2-071D Git audit hardening

- 工作包：V2-071D（Git 审计 fallback 移除 + status/diff 解析强化 + GitVersionAudit 确定性哈希）
- 关键产出：`doc/04-implementation/v2-071d-git-audit-hardening-spec.md`、`src/boardroom_os/adapters/git_audit.py`、`src/boardroom_os/audit/git_version_audit.py`、`tests/negative/test_git_audit_fallback_rejected.py`、`tests/closeout/test_git_audit_hardening.py`；同步迁移 `tests/closeout/test_git_version_audit.py` fixture。
- 关键实现：GitAuditAdapter（Git 审计适配器）缺 `base_commit_sha` / `worktree_ref` / `source_inventory_hash` 直接 fail closed，不再 fallback 到 final commit 或 `worktree.{package_root}`；Git status 改为 `--porcelain=v1 -z` 并按 NUL 解析特殊文件名与 rename；Git diff 改为 `--shortstat`，避免文件名污染 insertions/deletions；`fact_set_id` 使用 V2-071A `namespaced_ref` / `hash_namespaced_payload` 并绑定 final commit payload；GitVersionAudit（Git 版本审计）对 `verification_runs` / `command_evidence_bindings` 使用 `canonical_sort_for_hash`，并将 `command_binding_hashes` key 改为唯一 `binding_id`。
- Negative tests：覆盖缺 `base_commit_sha`、缺 `worktree_ref`、缺 `source_inventory_hash`、旧 `status --porcelain` 和旧 `diff --stat` 命令不再被调用。
- Happy path：特殊文件名（换行、引号、制表符、` -> ` 文本）与 rename 可按 `-z` 正确解析；`docs/12 insertions.md` 不污染 diff 统计；verification_runs / command_evidence_bindings 乱序后 `command_evidence_refs`、`checked_refs` 与 `bundle_hash` 稳定；同一 project 不同 final commit 得到不同 namespaced `fact_set_id`。
- 验证：`PYTHONPATH=src:. python -m pytest tests/closeout/test_git_version_audit.py tests/closeout/test_git_audit_hardening.py tests/negative/test_git_audit_fallback_rejected.py -q --basetemp=.pytest-tmp-v2071d-git` 通过（34 passed in 0.24s）；`PYTHONPATH=src:. python -m pytest tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071d-closeout-negative` 通过（614 passed in 2.50s）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071d-all` 通过（1390 passed in 2.98s）。

### 2026-05-28 — V2-071E CloseoutPackage boundary hardening

- 工作包：V2-071E（CloseoutPackage 边界严格化 + payload 内容绑定）
- 关键产出：`doc/04-implementation/v2-071e-closeout-package-boundary-spec.md`、`doc/04-implementation/v2-071e-closeout-package-boundary-implementation-plan.md`、`src/boardroom_os/audit/replay_bundle.py`、`src/boardroom_os/audit/process_audit.py`、`src/boardroom_os/audit/git_version_audit.py`、`src/boardroom_os/closeout/gate.py`、`src/boardroom_os/closeout/package.py`、`src/boardroom_os/contracts/refs.py`、`tests/closeout/test_closeout_package_boundary.py`、`tests/negative/test_closeout_package_graph_version_overflow_rejected.py`、`tests/negative/test_replay_payload_manifest_tampering_rejected.py`。
- 关键实现：ReplayBundleReadiness（重放包就绪摘要）新增 payload sha256 verified（载荷摘要已验证）、payload manifest ref/hash（载荷清单引用/摘要）字段；`replay_bundle_readiness(...)` 要求显式传入 ReplayPayloadResolver（重放载荷解析器）并用真实 payload 内容重算 sha256；CloseoutPackage（收尾包）`graph_version` 必须严格等于 ReplayBundle proof boundary（重放包证明边界）；ProcessAudit（流程审计）与 CloseoutPackage 只校验 typed readiness binding（类型化就绪摘要绑定），不重新解析 payload；ProcessAudit artifact/content refs（产物/内容引用）、ProcessAudit bundle/report/manifest refs（包/报告/清单引用）、GitVersionAudit bundle/report/hash-manifest refs（Git 版本审计包/报告/哈希清单引用）和 CloseoutPackage id（收尾包标识）均采用 project/hash/run namespaced ref（命名空间引用）。
- Negative tests：覆盖 graph_version 高于或低于 replay boundary、缺 ReplayPayloadResolver、缺 payload content、payload sha256 篡改、旧式 process audit artifact/content refs、非命名空间 closeout package id、跨 project / 跨 run process audit refs 均 fail closed。
- Happy path：canonical JSON payload hash（规范 JSON 载荷摘要）可通过 resolver 验证；CloseoutPackage graph_version 等于 replay boundary 时可构造；checked_refs（已检查引用）包含 payload manifest ref/hash；run_id 改变会改变 GitVersionAudit、ProcessAudit 和 CloseoutPackage 的持久引用。
- 验证：`PYTHONPATH=src:. python -m pytest tests/closeout/test_git_version_audit.py tests/closeout/test_git_audit_hardening.py -q --tb=short -rf --basetemp=.pytest-tmp-v2071e-git` 通过（30 passed in 0.27s）；`PYTHONPATH=src:. python -m pytest tests/closeout/test_process_audit.py tests/closeout/test_process_audit_artifacts.py tests/closeout/test_process_audit_fact_chain.py tests/closeout/test_git_version_audit.py tests/closeout/test_git_audit_hardening.py tests/closeout/test_closeout_package.py tests/closeout/test_closeout_package_boundary.py tests/negative/test_process_audit_construction_loop_rejected.py -q --tb=short -rf --basetemp=.pytest-tmp-v2071e-audit` 通过（187 passed in 2.38s）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071e-all` 通过（1410 passed in 3.68s）。

### 2026-05-29 — V2-071F fact-chain end-to-end regression

- 工作包：V2-071F（端到端 fail-closed 回归 + Phase 7 重锁）
- 关键产出：`doc/04-implementation/v2-071f-fact-chain-regression-spec.md`、`tests/closeout/test_v2_070_fact_chain_end_to_end.py`、`tests/negative/test_v2_070_audit_report_p0_regressions.py`、`tests/negative/test_v2_070_audit_report_p1_regressions.py`、`tests/negative/test_v2_070_audit_report_p2_regressions.py`；同步更新 `doc/04-implementation/backlog.md`、`doc/04-implementation/acceptance-criteria.md` 与 `doc/04-implementation/INDEX.md`。
- 关键实现：新增共享 V2-070 fact-chain fixture（事实链夹具），从 ReplayBundle（重放包）、ProcessAuditBundle（流程审计包）、GitVersionAuditBundle（Git 版本审计包）、CloseoutGateResult（收尾门禁结果）、CloseoutPackage（收尾包）串到 `CLOSEOUT_COMMITTED` governance event（治理事件）、CloseoutReducer（收尾归约器）和 CloseoutClosure（收尾闭包辅助校验）；spec mapping（回归映射）按测试函数级别列出 18 项 P0/P1/P2 executable checks（可执行检查）。
- Negative tests：P0 覆盖构造顺序、外部 ProjectionReplaySummary 拒绝、ProcessAudit timeline 篡改、GitAuditAdapter 缺 `base_commit_sha` / `worktree_ref`、CloseoutPackage graph_version 越界；P1 覆盖 payload sha256 篡改、ProcessAudit 缺字段直接 fail closed 不生成 `unknown` / `ticket` 占位、producer/consumer ticket 分离、unexpected fallback lineage 拒绝、跨 project/run namespace 串包拒绝；P2 覆盖 GitVersionAudit / ReplayBundle / ProcessAudit 集合输入乱序 hash 稳定、特殊 Git status 文件名解析、含 `insertions` 文件名不污染 diff shortstat，且 dirty facts 不能进入 GitVersionAuditBundle。
- Happy path：真实 EventLog（事件日志）在不含 `CLOSEOUT_COMMITTED` 时先构造 replay/process/git/gate/package，之后追加治理事件并由 CloseoutReducer 投影 `terminal_status=SUCCEEDED`；CloseoutClosure helpers 验证 checked_refs、SourceInventory evidence refs 和 WorkspaceEvidenceBundle verification runs 闭合；相同输入重复构造得到稳定 refs/readiness/hash。
- 验证：`PYTHONPATH=src:. python -m pytest tests/negative/test_v2_070_audit_report_p0_regressions.py tests/negative/test_v2_070_audit_report_p1_regressions.py tests/negative/test_v2_070_audit_report_p2_regressions.py tests/closeout/test_v2_070_fact_chain_end_to_end.py -q --basetemp=.pytest-tmp-v2071f` 通过（20 passed in 1.27s）；`PYTHONPATH=src:. python -m pytest tests/closeout/test_replay_bundle_rereplay.py tests/closeout/test_process_audit_fact_chain.py tests/closeout/test_git_audit_hardening.py tests/closeout/test_closeout_package_boundary.py tests/closeout/test_closeout_reducer.py tests/closeout/test_closeout_closure_hardening.py -q --basetemp=.pytest-tmp-v2071f-history` 通过（69 passed in 1.36s）；`PYTHONPATH=src:. python -m pytest tests/contracts tests/reducers tests/execution tests/evidence tests/proving tests/closeout tests/negative -q --basetemp=.pytest-tmp-v2071f-all` 通过（1432 passed in 4.74s）。

### 2026-05-29 — V2-080A tiny scenario active contracts

- 工作包：V2-080A（tiny scenario active contracts，微型证明场景活跃合同）
- 关键产出：`tests/proving/fixtures/tiny_fullstack_contracts.py`、`tests/proving/fixtures/__init__.py`、`tests/proving/test_tiny_contracts.py`；同步更新 `doc/04-implementation/backlog.md` 与 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：新增 TinyScenarioActiveContracts（微型场景活跃合同）fixture，复用 V2-010G TinyFullstackContractFixture（微型全栈合同夹具）作为合同内核产物，并增加 proving-specific validator（证明场景专用校验器），要求 API / UI / persistence / run-test acceptance refs（验收引用）全部来自 active AcceptanceContract（活跃验收合同），PackageContract（包合同）source surfaces（源码实现面）覆盖 full-stack package，ContractGateResult（合同门禁结果）证据义务与 active contract 完全一致。
- Negative tests：缺 API / UI / persistence / run-test acceptance refs、inactive AcceptanceContract、PackageContract source surfaces 缺 active refs、ContractGate evidence obligations 缺口均 fail closed。
- Happy path：ProjectCharter（项目章程）/ AcceptanceContract / PackageContract / ContractGateResult 活跃链路闭合；backend / frontend / persistence / tests / docs / run-manifest source surfaces 覆盖 tiny book availability tracker；EvidenceObligation（证据义务）覆盖所有 blocking criteria（阻塞验收项）的 evidence_required（必需证据）。
- 验证：先运行 `PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_contracts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080a-red` 得到预期 RED（缺 `tests.proving.fixtures.tiny_fullstack_contracts`）；实现后 `PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_contracts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080a-strengthen` 通过（10 passed）；`PYTHONPATH=src:. python -m pytest tests/contracts/test_tiny_fullstack_contract_fixture.py tests/negative/test_tiny_fullstack_contract_fixture_fail_closed.py tests/proving/test_tiny_contracts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080a-contracts` 通过（16 passed）；`PYTHONPATH=src:. python -m pytest tests/proving -q --tb=short -rf --basetemp=.pytest-tmp-v2080a-proving` 通过（186 passed）。

### 2026-05-29 — V2-080B tiny ticket graph 与 seat assignment

- 工作包：V2-080B（tiny ticket graph 与 seat assignment，微型任务图与席位分配）
- 关键产出：`tests/proving/fixtures/tiny_ticket_graph.py`、`tests/proving/test_tiny_ticket_graph.py`；同步更新 `doc/04-implementation/backlog.md` 与 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：新增 TinyTicketGraphFixture（微型任务图夹具），复用 V2-080A TinyScenarioActiveContracts（微型场景活跃合同），创建 CEO governance（治理）、Architect architecture（架构）、backend/frontend/test/docs implementation workers（实施工人）和 Checker verification（验证）active seats（活跃席位）；通过 EventRecord（事件记录）+ payload resolver（载荷解析器）驱动 TicketGraphProjector（任务图投影器）与 SeatAssignmentProjector（席位分配投影器），不手写最终 projection（投影）。
- Negative tests：worker ticket 缺 seat assignment 时从 ready queue 移除并产生 blocker；checker ticket 错分配给 worker seat 时 fail closed，证明 checker 与 worker 独立；ticket 缺 evidence obligations 时 Pydantic validation fail closed。
- Happy path：所有 ticket 均有 active seat assignment；implementation tickets 的 acceptance refs / source surface refs / evidence obligations 均来自 active contracts；初始 ready queue 只包含无依赖且正确派工的 governance / architecture / implementation tickets；checker ticket 被 implementation dependencies 阻塞；完成依赖后的 graph 可推进 checker ticket ready。
- 验证：`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_ticket_graph.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080b-red` 得到预期 RED（缺 `tests.proving.fixtures.tiny_ticket_graph`）；`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_ticket_graph.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080b` 通过（8 passed）；`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_contracts.py tests/proving/test_tiny_ticket_graph.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080ab` 通过（18 passed）；`PYTHONPATH=src:. python -m pytest tests/reducers/test_ticket_graph_projection.py tests/reducers/test_seat_assignment_projection.py tests/proving/test_tiny_ticket_graph.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080b-graph` 通过（83 passed）。

### 2026-05-29 — V2-080C real provider attempts

- 工作包：V2-080C（真实 ProviderAttempt，模型调用尝试记录）
- 关键产出：`src/boardroom_os/providers/__init__.py`、`src/boardroom_os/providers/openai_adapter.py`、`.env.example`、`tests/execution/test_openai_provider_adapter.py`、`tests/proving/fixtures/tiny_provider_attempts.py`、`tests/proving/test_tiny_provider_attempts.py`；同步修正 `tests/proving/fixtures/tiny_ticket_graph.py`、`tests/proving/test_tiny_ticket_graph.py`、`tests/fixtures/contracts/tiny_fullstack_contract.py`、`doc/04-implementation/backlog.md` 与 `doc/04-implementation/acceptance-criteria.md`。
- 关键实现：新增 OpenAIProviderTransport（OpenAI 兼容模型传输适配器），从 ignored `.env.test` 读取真实 provider 配置但不输出或提交 secret；支持 Responses API（响应接口）与 Chat Completions（聊天补全接口）两种 OpenAI-compatible 协议，本 endpoint 配置为 `chat_completions`、`reasoning_effort=high`、`max_retries=0` 与短摘要 system instruction（系统指令），用于证明真实 ProviderAttempt 与 work product lineage（工作产物来源链），本轮仍不物化最终 `10-project/` 源码。
- Negative tests：缺 `.env.test` / `OPENAI_API_KEY` fail closed；provider zero-attempt 不能完成 implementation ticket；fallback source delivery、placeholder artifact refs 均被拒绝；RuntimeExecutor（运行时执行器）不得 emit `TICKET_COMPLETED`（任务完成事件）。Adapter 层额外证明 secret 不进入 settings repr / model_dump / failed ProviderAttempt。
- Happy path：backend / frontend / tests / docs-run-manifest 四个 implementation ExecutionPackage（执行包）均经真实 provider 产生 succeeded `PRIMARY_PROVIDER_OUTPUT` ProviderAttempt，并绑定 ExecutionPackageRef（执行包引用）、AgentSeatRef（智能体席位引用）、provider/model/reasoning_effort；runtime fact events（运行时事实事件）包含 `PROVIDER_ATTEMPT_RECORDED` 与 `WORK_PRODUCT_SUBMITTED`，graph_version 单调递增。
- 验证：`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_provider_attempts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080c` 通过（7 passed in 18.32s）；`PYTHONPATH=src:. python -m pytest tests/proving/test_tiny_contracts.py tests/proving/test_tiny_ticket_graph.py tests/proving/test_tiny_provider_attempts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080c-proving` 通过（25 passed in 19.06s）；`PYTHONPATH=src:. python -m pytest tests/execution/test_openai_provider_adapter.py tests/execution/test_runtime_executor_boundary.py tests/proving/test_tiny_provider_attempts.py -q --tb=short -rf --basetemp=.pytest-tmp-v2080c-regression` 通过（25 passed in 28.93s）。
