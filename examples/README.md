# examples

未来可放置示例输入、示例 generated project workspace 和示例 process audit。

示例不得伪装为真实通过的 proving scenario。

建议未来结构：

```text
examples/
├── directives/
├── contracts/
├── generated-workspaces/
└── process-audits/
```

当前 foundation package 不包含示例产物。

