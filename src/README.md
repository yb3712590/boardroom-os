# src

V2 实现代码未来应进入：

```text
src/boardroom_os/
```

当前 foundation package 不包含实现代码。

在开始写代码前，必须先完成并确认：

1. `doc/04-implementation/phase-0-plan.md`；
2. `doc/04-implementation/acceptance-criteria.md`；
3. `doc/CODE_CONVENTIONS.md`；
4. `doc/TEST_CONVENTIONS.md`。

禁止在此处复制旧 `backend/app/core` 实现。

