[scenario]
slug = "library_management_full_integration"
description = "Run the full library-management live integration scenario."
goal = "实现一个完全匿名的、纯粹用于记录实体书本当前是否在馆的单机版终端系统。系统只回答馆里有什么书，以及这本书现在能不能拿走。"
workflow_profile = "CEO_AUTOPILOT_FINE_GRAINED"
force_requirement_elicitation = false
budget_cap = 1500000
constraints = [
  "系统固定为匿名、单机、单租户，不区分管理员和普通读者。",
  "唯一数据模型只允许 books 一张表，字段固定为 id、title、author、status。",
  "status 只允许 IN_LIBRARY 和 CHECKED_OUT。",
  "核心动作只允许 Add、Check Out、Return、Remove。",
  "禁止 Auth、RBAC、预约、罚金、借阅历史、分类表、标签表。",
  "界面固定为高信息密度 terminal/console 风格。"
]

[runtime]
seed = 17
max_ticks = 180
timeout_sec = 7200

[provider]
provider_id = "prov_openai_compat_integration"
base_url = "https://api.openai.com/v1"
api_key_env = "BOARDROOM_OS_INTEGRATION_TEST_API_KEY"
preferred_model = "gpt-5.4"
max_context_window = 270000
reasoning_effort = "high"
connect_timeout_sec = 10
write_timeout_sec = 20
first_token_timeout_sec = 300
stream_idle_timeout_sec = 300
fallback_provider_ids = []

[[provider.role_bindings]]
target_ref = "ceo_shadow"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 270000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:architect_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 270000
reasoning_effort_override = "xhigh"

[[provider.role_bindings]]
target_ref = "role_profile:frontend_engineer_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:checker_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:backend_engineer_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:database_engineer_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:platform_sre_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[[provider.role_bindings]]
target_ref = "role_profile:cto_primary"
provider_model_entry_refs = ["prov_openai_compat_integration::gpt-5.4"]
max_context_window_override = 220000
reasoning_effort_override = "high"

[workspace_paths]
source = "10-project/src/*"
docs = "10-project/docs/*"
tests = "20-evidence/tests/*"
git = "20-evidence/git/*"
governance = "reports/governance/{ticket_id}/*"
check = "reports/check/{ticket_id}/*"
closeout = "20-evidence/closeout/{ticket_id}/*"

[assertions]
profile = "minimalist_book_tracker"
